## v1.7.25 全局快捷键可自定义 + 播放队列升级成队列工作台

- 发布版本从 `1.7.24` 提升为 `1.7.25`；已安装 `1.7.24` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **全局快捷键确认默认绑定并补齐可用性与失败反馈。** 默认全局键为 `Ctrl+Alt+←` 上一首、`Ctrl+Alt+→` 下一首、`Ctrl+Alt+↑` 音量增加、`Ctrl+Alt+↓` 音量降低、`Ctrl+Alt+Space` 播放/暂停，另有 `Ctrl+Alt+M` 静音、`Ctrl+Alt+F` 全屏、`Ctrl+Alt+H` 显示/隐藏主窗口、`Ctrl+Alt+L` 桌面歌词；九个动作全部可在设置里逐个改键或清空。说明：需求里 `Ctrl+Alt+↓` 同时被列为「播放/暂停」和「音量降低」，一个组合键只能绑一个动作，播放/暂停保留在 `Ctrl+Alt+Space`、`↑/↓` 留给音量；想换成方向键的，把音量键改掉再把播放/暂停设成 `Ctrl+Alt+↓` 即可。
- 录制快捷键时先做 Electron 加速键语法校验：不受支持的键位（单独的字母、纯修饰键、Electron 不认的词元）在录制当场给出中文原因，不会先写进配置再静默注册失败。全局注册被系统或其它软件占用时，主进程回报占用类型，界面合并成一条提示并点出具体是哪几个动作没抢到。
- 主进程侧动作（显示/隐藏主窗口等）即使主窗口没有焦点也能执行；`,` 这类字面标点、小键盘键和专用媒体键（媒体播放/上一首/下一首、音量键）都可直接作为全局键，媒体键允许不带修饰键单独注册。
- **播放队列升级为完整的队列工作台。** 「当前队列」页新增「队列循环 / 单曲循环 / 随机」三个互斥直达按钮（点当前模式不会重新洗牌），以及「播完即停」一次性开关：本曲播完就停在这首歌上并把进度拨回开头，再按播放是重听本曲而不是跳下一首，触发后开关自动复位。
- **新增「下一首」预览行。** 按当前模式实时推算下一首（单曲循环显示本曲、播完即停显示「本曲播完后停止」），点一下直接跳过去；预览结果进入队列 DOM 签名，切模式或开关播完即停会立刻重绘。
- **队列支持拖动重排。** 每行左侧新增拖动握把，拖动时按行中线显示插入位置；队列内部拖动不再触发「拖文件进来」的全屏导入遮罩。
- **新增队列存档（保存 / 恢复 / 删除）。** 「存队列」把当前顺序、播放模式和当前曲目存成一份存档，最多 12 份、每份最多 3000 首；存档只保存歌曲身份（ID、路径键、曲名、歌手），不写封面、歌词和文件句柄，恢复时按「ID → 路径键 → 曲名+歌手」三级回落到当前曲库重新解析，所以重扫曲库、换盘符后仍能对上，缺失的曲目会在提示里报数。恢复随机存档时保留存档里的乱序顺序，不会再洗一次。队列为空但存在存档时不再被自动切走，「恢复队列」入口始终可达。
- 修复一个已有缺陷：从队列移除位于当前歌曲之前的条目时，「正在播放」高亮会错位到别的歌上，现在指针跟着一起前移。
- 验证：新增 `tests/global-hotkeys.test.js`（18 例）与 `tests/playback-queue-power.test.js`（36 例），并扩写 `tests/queue-render-hot-path.test.js`、`tests/playback-shuffle-order.test.js`；全量 Node 回归 `709/709` 通过（上一版基线 `655`）。本轮未启动本机 Electron；Windows x64 安装资产由 GitHub Actions 远程构建。

## v1.7.24 音乐库升级成外层标签页（当前队列 / 歌单 / 音乐库）

- 发布版本从 `1.7.23` 提升为 `1.7.24`；已安装 `1.7.23` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **音乐库从歌单列表里的一张卡提到最外层。** 左侧面板顶部的标签栏现在是 `当前队列 / 歌单 / 音乐库` 三个并排入口，点「音乐库」直接进智能分类首页，不必先切到「歌单」再点一张卡。
- **两个标签页共用同一份列表与选中状态，切换时自动摆位。** 切到「音乐库」时若当前停在歌单侧，选中项自动落到分类首页；切回「歌单」时若停在某个分类里，选中项退回「全部音乐」，不会出现音乐库标签下铺着独立歌单的串页。
- **停在某位艺术家或某个分组时切到「当前队列」再回来，仍停在原来那一层**，选中项不会被切走。
- **工具条跟着标签页换。** 音乐库那一页说明文字为「音乐库智能分类」并隐藏「新建」（新建只对独立歌单有意义），「导入」保留；歌单那一页与之前完全一致。
- **音乐库首页不再重复写一遍标题。** 标签页本身就是「音乐库」，进去直接铺十个入口；分组层与歌曲层照旧带返回按钮，返回的是上一层。
- **「歌单」标签页恢复成 特别喜欢 → 独立歌单 → 全部音乐**，与 `1.7.22` 及更早版本一致。
- **原先只认「歌单」标签的内部判断统一收口。** 曲库变化后的面板刷新、面板展开动画、3D 歌单架重建都改走同一个判断，音乐库那一页不会出现改了标签却不刷新的情况。
- 三个标签复用既有 `.panel-tab` 样式，`public/app.css` 一行未动。
- 全量 Node 回归 `655/655` 通过（新增标签页行为测试：选中项迁移、面板显隐、工具条文案、深层停留不串页）。

## v1.7.23 音乐库智能分类（艺术家 / 专辑 / 专辑艺术家 / 流派 / 年代 / 播放记录）

- 发布版本从 `1.7.22` 提升为 `1.7.23`；已安装 `1.7.22` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **曲库面板不再只有「歌曲 / 歌单 / 喜欢」三条路。** 顶部新增一张「音乐库」卡片，点进去是完整的智能分类：`所有歌曲 / 最近添加 / 最近播放 / 播放最多 / 未播放` 五个歌曲视图，加 `艺术家 / 专辑 / 专辑艺术家 / 流派 / 年代` 五个分组浏览。
- **三层导航：音乐库 → 分组 → 歌曲。** 每一层都带返回按钮，回到的是上一层而不是直接弹回根目录；歌曲层与分组内的某一项都有「播放全部」，点单曲照旧从该分类内部开始连播。
- **年代按十年归档。** 从年份字段里抽四位数字算出 `1990年代` / `2000年代` / `2010年代`，列表按年代从新到旧排，读不出年份的归到「未知年代」并垫在最后。
- **专辑艺术家缺失时回落到艺术家**，流派、专辑、艺术家为空的歌统一归到「未知流派 / 未知专辑 / 未知艺术家」，不会凭空消失在分组之外。
- **最近播放、播放最多、未播放由本地收听统计驱动。** 播放最多按播放次数排，次数相同再比累计收听时长、最后才比最近播放时间；从来没播过的歌进「未播放」。
- **新增入库时间戳，「最近添加」才有意义。** 时间戳按文件路径记在 `mineradio-local-added-at-v1`（最多 `4000` 条，超出后先裁最旧的），改标签、换封面都不会让歌重新变成「最近添加」；只有相对已有索引确实新增的文件才盖章，所以第一次整库导入不会把两万首歌盖成同一时刻，老文件继续用文件修改时间兜底。文件夹自动监控期间新出现的歌也逐首盖章，落盘留到同步收尾一次写完。
- **「最近添加」封顶 `200` 首**，不会退化成「所有歌曲」的副本。
- **分类可以直接当播放来源。** 底部播放来源按钮会显示当前分类名，但智能分类只在内存里生效、不写进播放来源持久键，重启回到「全部音乐」；分类被清空时自动落回全部音乐，不会误弹「导入本地音乐」。
- **整库只扫一遍。** 五个分组与三个统计列表在同一次遍历里算完并缓存，只有曲库内容或收听统计变化才重建；分组卡片沿用面板既有的懒加载额度和「加载更多」，不会一次铺完几千位艺术家。
- 界面零新增样式：分类卡片、视图头与操作条全部复用既有的玻璃卡片类，`public/app.css` 一行未动。
- 新增 `tests/local-library-categories.test.js`（22 例，用 `node:vm` 跑 `public/app.js` 里的真实实现：三层 kind 的归一化与非法值回落、分组值里带冒号的切分、年代归档与排序、专辑艺术家回落、三个播放统计分类的排序与分区、入库时间盖章与裁剪、缓存失效、动态分类不落盘、渲染层卡片顺序与点击优先级）；全量 Node 回归 `652/652` 通过。

## v1.7.22 真正的音效链（预设 → EQ → Preamp → Limiter → Spatial → Output）

- 发布版本从 `1.7.21` 提升为 `1.7.22`；已安装 `1.7.21` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **新增完整音效链，链路顺序固定为 `预设 → EQ → Preamp → Limiter → Spatial → Output`。** DIY 面板高级页新增「音效链」折叠区：音效链总开关、自动预增益、限幅保护、立体声扩展四个开关，八个预设按钮，10 段均衡滑杆，预增益与限幅阈值滑杆，声场宽度滑杆，以及音效档案的导出 / 导入。
- **8 个预设：Normal / Rock / Pop / Classical / Jazz / Bass Boost / Vocal / 自定义。** 预设不单独存盘，而是从当前曲线反推：拖动任意频段后自动落到「自定义」，把曲线改回某个预设的形状也会自动认回那个预设，不会出现「显示 Rock 但声音已经不是 Rock」的错位。
- **10 段图示均衡，`31 Hz` 到 `16 kHz` 的 ISO 频段。** 两端用搁架滤波器（`lowshelf` / `highshelf`）托住整个低频与高频区，中间八段用 `peaking`（`Q = 1`），与常见硬件均衡器一致；每段 `±12 dB`、`0.5 dB` 步进。
- **自动预增益按最大提升量反向留余量。** 例如 Bass Boost 抬了 `8 dB`，就先垫 `-8 dB` 再进后级，抬高频段不会一进限幅就顶满刻度；用户预增益 `±12 dB` 叠加在自动余量之上，总量夹在 `-24 dB` 到 `+12 dB`。
- **末端限幅保护，阈值 `-12 dB` 到 `0 dB`。** 用 `DynamicsCompressorNode` 做硬比（`ratio = 20`、`knee = 0`、`attack = 3ms`、`release = 250ms`）压住瞬时过冲；关闭时把 `ratio` 推回 `1`，压缩曲线退化成直线、补偿增益恒为 `1`，等于旁通。
- **立体声扩展用真正的中/侧矩阵，不是伪立体声。** `mid = (L+R)/2`、`side = (L-R)/2`，输出 `L = mid + width·side`、`R = mid - width·side`；宽度 `1` 逐样本还原原始左右声道，`0` 并成单声道，最大 `2` 加宽声场，`0.05` 步进。
- **整条链常驻音频图，切换开关不会有咔哒声。** 每一级都有数学上完全透明的中性值（`0 dB` 频段、`ratio = 1` 限幅、`width = 1` 中/侧矩阵），所以关闭音效链不需要改接线；参数变化统一走 `80ms` 短斜坡，切歌与重建音频节点时立即生效。
- **音效档案可以存成 `xxx.eq.json` 带走。** 导出内容含 `format` / `version` / 档案名、10 段频率与增益、预增益与自动余量开关、限幅阈值、声场宽度；导入时校验 `format` 才接受，`{frequency, gain}` 形式的曲线按最接近的频段归位（`3.5 kHz → 4 kHz`），纯 dB 数组也能读，缺的频段补 `0`，不认识的文件一律拒绝。文件对话框复用既有的 JSON 导入导出通道，主进程零改动。
- **音效链挂在音量均衡之后、音量增益之前。** 音频链路变成 `source → analyser → 均衡增益 → EQ → Preamp → Limiter → Spatial → 音量增益 → 输出`：可视化与节拍频谱仍取原始电平（画面不会跟着 EQ 忽明忽暗），音量条与所有淡入淡出继续由原来的增益节点独占。
- 设置存进独立的 `mineradio-audio-chain-v1`，不写入视觉预设，导入别人的预设不会改掉你的 EQ；总开关默认关闭，存档损坏时回落默认值而不是抛错。
- 新增 `tests/audio-effect-chain.test.js`（14 例，用 `node:vm` 跑 `public/app.js` 里的真实实现：钉死 8 个预设曲线与反推、频段夹取与取整、自动预增益余量、限幅与声场的旁通语义、逐条比对整张音频图的接线与初始中性值、按真实写入的增益逐样本核对中/侧矩阵、`.eq.json` 往返与非法文件拒绝）；全量 Node 回归 `630/630` 通过。
- 本轮未修改任何既有界面布局、交互入口与样式规则（`public/index.html` 只新增一个与既有折叠区同构的设置区块，`public/app.css` 一行未动）。

## v1.7.21 音量均衡（ReplayGain）

- 发布版本从 `1.7.20` 提升为 `1.7.21`；已安装 `1.7.20` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **新增音量均衡，一张 FLAC 很大声、下一首老歌突然很小声的问题到此为止。** 读取文件里已有的 ReplayGain 标签，把不同来源的歌拉到同一响度水平；DIY 面板高级页新增「音量均衡」折叠区：音量归一化开关、整轨 / 整专辑基准、Preamp 预增益与防削波开关。
- **ReplayGain Track / Album 双基准。** 整轨（Track）让每首歌一样响，随机播放最稳；整专辑（Album）按整张专辑统一增益，保留专辑内部刻意做出的响度起伏。某一侧标签缺失时自动回退到另一侧，峰值也跟着回退，不会因为少一个标签就整首放弃均衡。
- **Preamp 预增益 ±12 dB。** 按 `0.5 dB` 步进、`0.1 dB` 取整，叠加在 ReplayGain 增益之上；最终线性增益夹在 `0.05` 到 `4` 之间，坏标签既不会把歌压成静音，也不会把开关变成放大器。
- **防削波按峰值封顶。** 提升后会削波时退回到刚好不削波的增益（`min(10^((gain+preamp)/20), 1/peak)`），不引入压缩器、不增加延迟、不改变音色；标签里没有峰值时与 foobar2000 一致不做额外衰减，设置区文案会如实说明当前是否被封顶。
- **八类格式的标签全部支持。** FLAC / OGG / OGA / OPUS 的 Vorbis comment `REPLAYGAIN_*`、MP3 的 ID3v2 `TXXX` 与 `RVA2`（只取主音量声道）、WAV 的 `id3 ` chunk、APEv2 文本项、M4A 的 `----` 自定义项，外加 Opus 的 `R128_TRACK_GAIN` / `R128_ALBUM_GAIN`（Q7.8 定点，按 `-23 LUFS → -18 LUFS` 补 `5 dB` 折算）。同一文件里真实 ReplayGain 标签优先于 R128 折算值；`iTunNORM` 的响度参考不同，故意不混入。
- **不需要重扫曲库。** 标签采集挂在既有解析链路上，新扫到的歌顺手带上；升级前就在库里的歌在第一次播放时惰性补齐一次并写回缓存，之后不再重复扫描。数据走缓存已有的 `extra` JSON 列，没有数据库迁移，也不会因为多一个字段让整库回落重解析。
- **均衡增益是独立节点，不动音量与淡入淡出。** 音频链路变成 `source → analyser → 均衡增益 → 音量增益 → 输出`：可视化与节拍频谱仍取原始电平（画面不会跟着均衡忽明忽暗），音量条与所有淡入淡出继续由原来的增益节点独占。切歌时增益立即生效，播放中补齐标签时用 `80ms` 短斜坡过渡，不会听到电平突跳。
- 设置存进独立的 `mineradio-replay-gain-v1`，不写入视觉预设，导入别人的预设不会改掉你的音量设定；开关默认关闭、防削波默认开启，存档损坏时回落默认值而不是抛错。
- 新增 `tests/replay-gain-tag-parsing.test.js`（10 例，用真实字节夹具覆盖 FLAC / MP3 `TXXX` / `RVA2` / WAV / APEv2 / M4A / R128 与轻量扫描未读全的情形）和 `tests/replay-gain-normalization.test.js`（12 例，用 `node:vm` 跑 `public/app.js` 里的真实增益实现，钉死 dB 换算、Preamp 夹取、防削波封顶、基准回退、斜坡与惰性补齐链路）；全量 Node 回归 `616/616` 通过。
- 本轮未修改任何既有界面布局、交互入口与样式规则（`public/index.html` 只新增一个与既有折叠区同构的设置区块，`public/app.css` 一行未动）；Windows x64 安装资产由 GitHub Actions 远程构建。

## v1.7.20 音乐文件夹自动监控

- 发布版本从 `1.7.19` 提升为 `1.7.20`；已安装 `1.7.19` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **音乐文件夹现在会被持续监控，改动即时生效。** 新增 `desktop/local-library-watcher.js`：`fs.watch(root, { recursive: true, persistent: false })` + `unref()` 挂载，`900ms` 防抖合并整张专辑的上百个事件，另有 `4500ms` 最长等待阀门保证长时间拷贝也能中途上报；递归监控不可用的平台自动降级成只看根目录一层；`EPERM` / `ENOENT`（权限拒绝、移动硬盘掉线）按 `5000ms → 60000ms` 指数退避重试，设备接回后自动恢复监控。
- **四类改动各走各的路径，不再等下次启动。** 新增歌曲自动入库、删除歌曲自动清理、修改标签自动更新、修改封面自动刷新。旧行为是启动约 `1.2s` 后检测到目录变化、但只要有播放队列就弹一句「下次启动会自动同步」然后作废本轮结果；现在改成就地增删改。
- **同步过程不打断播放。** `localLibrarySongs` 只做原地增删（`playbackSource === 'library'` 时它与 `playQueue` 是同一个数组，换数组会让所有归属判定失效）；改动过的歌只改字段、不换对象，`currentLocalSong`、`playQueue[currentIdx]`、迷你播放器与桌面歌词握着的引用全部继续有效；被删掉的文件如果正在播放则原位保留、`currentIdx` 按 `indexOf` 重算并夹紧，等下次启动再收尾。
- 接管一首歌时按 `localKey`（`路径 + 大小 + 修改时间`）把 `customCoverMap` 里用户手挑的封面迁到新键，否则一次改标签就会让它变成孤儿；`localUrl` / `localCoverObjectUrl` 清空后交给既有的 `revokeDiscardedLocalSongObjectUrls` 回收，但正在播放那首的 blob 仍是 `audio.src`、单独跳过。
- 标签被删除时如实回落：`album` / `albumArtist` / `genre` / `trackNumber` / `year` 清空、`name` / `artist` 退回文件名推导，不留上一版的旧值；`duration` 故意保留，避免刷新瞬间闪一下 `0:00`。元数据、封面或歌词解析还在途中（`localCoverPromise` 等标志）时本轮不接管，留到下一轮，避免过期结果写回。
- **右下角新增同步指示器：`已同步 12,431 首歌曲`。** 千分位分组，`4.2s` 后自动淡出，`role=status` / `aria-live=polite`，`pointer-events:none` 不吃鼠标；元素由渲染层懒建并挂在 `body` 上，`public/index.html` 一行未动。
- 监控根仍逐个走 `rememberLocalMusicRoot` 授权，未授权的根原样回报 `rejected`；两个新 IPC 通道 `mineradio-local-library-watch-set-roots` / `mineradio-local-library-watch-status` 均由 `trustedMainFrameHandler` 包裹，preload 侧只透传数组并返回退订函数。退出流程里监控句柄先于 SQLite 关闭。
- 修掉一个会让上面这个数字本身报错的老问题：`.ape` 与 `.dsf` 一直在扫描白名单里，却漏在 `LOCAL_LIBRARY_AUDIO_EXTS` 之外，导致 APE / DSD 被扫到但不计入 SQLite 的 audio 计数与曲库签名。
- 新增 `tests/local-library-watcher.test.js`（19 例，注入 `fs` 与定时器桩，覆盖防抖/最长等待双阀门、递归降级、指数退避、`overflow` 截断、增量 `setRoots` 与 `close` 后的静默）、`tests/local-library-auto-sync.test.js`（12 例，用 `node:vm` 跑 `public/app.js` 里的真实实现，钉死数组身份、播放保护、封面迁移与延迟接管）和 `tests/local-library-watch-wiring.test.js`（5 例，钉死扩展名集合与主进程/preload 的接线）；全量 Node 回归 `594/594` 通过。
- 本轮未启动本机 Electron，未修改任何界面布局、交互入口与既有样式规则（仅为新增指示器追加 3 条 CSS）；Windows x64 安装资产由 GitHub Actions 远程构建。

## v1.7.19 OGG / OPUS / APE / WAV / DSD(.dsf) 格式支持

- 发布版本从 `1.7.18` 提升为 `1.7.19`；已安装 `1.7.18` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **新增五类格式的标签、封面、歌词与时长解析。** 渲染层扩展到 Ogg Vorbis / Ogg Opus / Ogg FLAC、WAV（含 RF64 / BW64）、APE（Monkey's Audio）与 DSF（DSD Stream File）：`extractLocalMetadataTags`、`extractEmbeddedCoverSource` 与新增的 `extractEmbeddedLyricsText` 三个分发器统一按扩展名路由，能力判定 `canReadEmbeddedLyrics` / `canReadEmbeddedCover` 及对应的可重试判定同步扩展。
- **Ogg 系列**：按页读取并跨页拼接数据包，跳过同文件内其它逻辑流；Vorbis comment 支持 `METADATA_BLOCK_PICTURE` 与旧式 `COVERART` / `COVERARTMIME` 封面；时长优先取 Ogg FLAC STREAMINFO 的总采样数，没有时才回读尾部 64KB 找最后一个 granule，Opus 会扣掉 `pre-skip`。
- **WAV**：走 RIFF chunk 目录，`id3 ` chunk 优先、`LIST`/`INFO` 补齐缺失字段，时长由 `data` 大小除以 `fmt ` 的字节率得出；`RF64` / `BW64` 用 `ds64` 的 64 位 `dataSize` 替换 `0xFFFFFFFF`，超过 4GB 的文件也能算对时长。
- **APE**：3.99+ 读描述符后的头部块，3.98 及更早按版本号与压缩等级推导 `blocksPerFrame`；标签按 APEv2 页脚 → 文件头 ID3v2 → 尾部 ID3v1 的顺序合并，二进制封面项支持 `Cover Art (Front)` 优先。
- **DSF**：按 `DSD ` 头部的 metadata 指针读尾部 ID3v2，时长为 `fmt ` 里的单声道 1-bit 采样数除以 DSD 采样率；`metadataOffset` 为 0 视为正常无标签，不会浪费一次读取。
- **APE 与 DSD 现在能直接播放。** Chromium 不认识这两种格式，新增 `desktop/audio/wav-stream.js` 把它们包装成“虚拟 WAV 文件”——总长度可精确计算、任意字节区间按需解码，`/api/local-file` 的 Range 请求、416 响应与 `raw=1` 原始字节通道全部照常工作，拖动进度条不受影响。`desktop/audio/ape-decoder.js` 是纯 JS 的 Monkey's Audio 解码（3800–3990），`desktop/audio/dsf-decoder.js` 用字节查表的 FIR 抽取把 1-bit DSD 码流转成 PCM。两个解码器都不碰文件系统，只接受 `read(offset, length)` 读取器。
- 头部推导出的时长回填到展示字段，无损与高解析度曲目在首次播放前就能看到正确时长。
- ID3v2 读取保留原 MP3 的 `256KB` 探针语义：探针已经覆盖整个标签时直接切片复用，只有超出探针的大标签才发第二次 Range 读取，同一段字节不会被读两次。
- 标签体积超过本轮扫描预算（后台轻量 `4MB`、前台 `24MB`）时统一标记 `_mineradioScanComplete=false`，交由前台完整重试，而不是返回半截结果。
- `desktop/audio/ape-decoder.js` 是 FFmpeg `libavformat/ape.c` 与 `libavcodec/apedec.c` 的逐行移植，原始条款为 `LGPL-2.1-or-later`，按 LGPL v2.1 第 3 条在本项目内以 `GPL-3.0` 分发；新增根目录 `THIRD-PARTY-NOTICES.md` 记录该声明与其余第三方组件，并把它和 `LICENSE` 一起纳入安装包白名单。DST 压缩的 `.dff` 不在本轮范围内。
- 新增 `tests/local-format-tag-parsing.test.js`（18 例，用真实字节夹具驱动 `public/app.js` 里的解析实现，覆盖跨页数据包、旁路逻辑流、`ds64` 64 位长度、APE 新旧头部、DSF 探针复用与四种格式的超预算截断标记）；全量 Node 回归 `558/558` 通过。
- 本轮未启动本机 Electron，未修改任何界面布局、样式、文案与交互入口；Windows x64 安装资产由 GitHub Actions 远程构建。

## v1.7.18 本地曲库改用 SQLite + 文件指纹/路径索引

- 发布版本从 `1.7.17` 提升为 `1.7.18`；已安装 `1.7.17` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **本地曲库常驻 SQLite，几万首歌启动不再重放整包 JSON。** 新增 `desktop/local-library-store.js`，用 Electron 自带的 `node:sqlite` 建库（零新增依赖、无原生模块重编译），落在用户数据目录的 `local-library.db`；WAL 日志、`PRAGMA user_version` 迁移、预编译语句缓存，写入统一走 `BEGIN IMMEDIATE` 事务。
- 每行保存歌曲 ID（`song_key`）、绝对路径与归一化路径键、文件大小、修改时间、时长、格式、Artist / Album / Genre / Year、封面缓存、歌词缓存、播放次数、最近播放和收藏状态；行身份由「路径键 + 大小 + 修改时间」组成的文件指纹决定，指纹一致就保留已解析的元数据与封面歌词缓存，指纹变化才整组清空重解析。
- **启动与扫描改为按需增量。** 渲染层没带快照时主进程先问数据库，有历史行就只 `stat` 变化过的目录；扫描结果回写数据库后，快照以 `fromDb` 交接给渲染层。索引回写改成按行 FNV-1a 摘要的增量写入，不再每次防抖都整表重写。
- **修掉一个会让索引回写慢两个数量级的查询计划陷阱。** 只有单列索引时 `UPDATE files ... WHERE root_id=? AND song_key=?` 会退化成按 `root_id` 整根扫描（两万行实测 `295,319ms`），改用 `(root_id, song_key)` / `(root_id, fingerprint)` 组合索引后降到 `1,640ms`（约 180×），并用 `EXPLAIN QUERY PLAN` 断言永久钉死。
- **解除历史 `16000` 条截断。** 旧快照与旧索引都在第 16000 条后只计数不入库，签名却照样匹配，等于静默丢歌；SQLite 接管后全量落盘，仅在数据库不可用的回落路径保留旧上限。
- 播放次数、最近播放与收藏状态双写进库：播放结算只在原有有效收听门（`completed` / `listenMs ≥ 45s` / 进度 ≥ 50% / 无时长且 ≥ 30s）内累加一次，`localStorage` 的听歌统计与「特别喜欢」引用表仍是唯一权威来源，数据库只做可索引的持久镜像，没有回读路径，UI 与计数行为零改动。`song_stats` 按 `song_key` 独立存活，重扫、换库或删掉索引都不清零。
- 缓存回收顺序固定为「保护正在使用的键 → `saved_at` 过期（默认 180 天，未标注的记录跳过）→ 记录数 LRU → 封面字节上限」，字节上限用窗口函数 `SUM(cover_bytes) OVER (...)` 一次算完。
- `Genre` 是向前生效字段：本版之后新解析的曲目才带该标签，旧记录不会因为缺 `genre` 被判定为脏行重写。
- 缺 `node:sqlite` 的运行环境整层降级：主进程只标记一次不可用，渲染层探测同样一次性 latch，所有调用静默回落到原 IndexedDB 路径，播放结算与点赞不会因此抛错。
- 新增 `tests/local-library-sqlite-store.test.js`（12 例，跑真实 `node:sqlite`，含两万首落盘读回、查询计划回归、缓存回收与降级）与 `tests/local-library-db-bridge.test.js`（9 例，钉死探测 latch、参数原样转发、行摘要稳定性、播放统计用 `localKey` 而非 `local:` 队列键）；全量 Node 回归 `540/540` 通过。
- 本轮未启动本机 Electron，未修改任何界面布局、样式、文案与交互入口；Windows x64 安装资产由 GitHub Actions 远程构建。

## v1.7.17 迷你播放器封面命中与右键竞态修复

- 发布版本从 `1.7.16` 提升为 `1.7.17`；已安装 `1.7.16` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **修复收回态“命中封面后马上右键仍落到桌面”的竞态。** 封面热区恢复窗口交互改走同步 IPC，主进程在返回前完成 `setIgnoreMouseEvents(false)`；异步通道仍保留为旧 preload 的兼容回退，失败结果不污染已确认缓存并允许下一次重试。
- 标准迷你播放器收回态继续只让封面热区（外扩 `6px`）命中，封面外透明区域交还桌面；展开态与极简外壳整窗右键语义不变，仍使用与托盘一致的六项菜单。
- 两套迷你页面的 `.mini-shell` 固定留在 renderer 客户区，窗口移动改走独立 IPC，避免拖动和右键被 Windows 非客户区分流；系统右键监听保留为可信主 frame 的平台兜底。
- 补齐同步穿透通道、主 frame 信任门禁和“未等待 Promise 即可右键”的回归测试；全量 Node 回归 `519/519` 通过。
- 本轮未启动本机 Electron，未合成鼠标键盘输入，未修改 `Mineradio-sync`；Windows x64 安装资产由 GitHub Actions 远程构建。

## v1.7.16 迷你播放器右键命中与安全边界加固

- 发布版本从 `1.7.15` 提升为 `1.7.16`；已安装 `1.7.15` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **标准迷你播放器收回态只让封面参与命中。** 收回时封面热区（外扩 `6px`）可点击、可右键弹出完整应用菜单；封面以外透明区域交还桌面，桌面点击和右键不被吞。展开态整窗任意位置仍可右键。
- **极简迷你播放器整窗可右键。** 极简外壳无收回态和鼠标穿透，任意位置均走与任务栏托盘一致的六项菜单；标准展开态同样如此。
- 修复封面拖动首个增量丢失、换边跳位和跨显示器卡边；显式保存展开方向，保证靠边向左展开和重启后位置连续。
- 加固迷你窗口信任边界：仅允许当前本地端口的标准/极简页面，阻止外部导航、子 frame 和伪造 sender 访问控制 IPC；外部页面不再暴露 `window.miniPlayer` 特权桥。
- 极简模式不再传输或缓存封面、脉冲数据；失败穿透 IPC 可重试；异步主题最终状态和原生菜单设置同步修正。
- 全量 Node 回归 `514/514` 通过；本轮不启动本机播放器，Windows x64 安装包由 GitHub Actions 远程构建，结果与校验信息记录在 `RELEASE.md`。

## v1.7.15 主题插件差异化与雪昼白主题

- 发布版本从 `1.7.14` 提升为 `1.7.15`；已安装 `1.7.14` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **修复三份主题外观雷同。** `深海微光`、`暗焰余晖`、`冷杉夜雾` 不再只是三变量背景包，现分别提供完整的面板、卡片、按钮、文字、底栏和迷你播放器配色，蓝青、熔岩红、冷杉绿三套视觉签名明确区分。
- **新增浅色主题 `雪昼白`。** 浅色面板、深色文字和冷蓝灰层次适合白色界面阅读；与五份暗色主题一样，默认不启用并遵守主题互斥规则。
- 安装包内置主题与 `examples/plugins/` 示例文件逐字段同步，用户自定义背景、取色器强调色、玻璃质感和迷你播放器主题变量转发链路保持原有优先级与行为。
- 主题专项测试 `35/35` 通过；发布构建与完整回归结果记录在 `RELEASE.md`。

## v1.7.14 收回后的迷你播放器只在封面上吃鼠标

- 发布版本从 `1.7.13` 提升为 `1.7.14`；已安装 `1.7.13` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **标准迷你播放器收回后，只有封面那一小块参与命中，封面以外那截看不见的窗体交还桌面**。收回只是把面板收成 `54 × 54` 的封面，窗口本身一直是 `360 × 84`，`v1.7.12` 那版「指针进入窗口就整窗命中」会让这块透明区域吞掉桌面的点击和右键——现在恢复成「只有封面热区（外扩 `6px`）命中」。
- 右键行为因此明确为：**收回态右键封面弹播放器菜单，右键封面以外的空白处弹桌面菜单；展开态整窗任意位置都能右键**，菜单仍是与任务栏托盘完全一致的 6 项。
- **极简迷你播放器整窗任意位置都能点、都能右键**，本轮不变：它没有收回态，也从来没有鼠标穿透。
- 悬停封面展开、离开自动收回、封面拖动移动窗口、靠边向左展开、桌面歌词入口全部不受影响；`tests/mini-player-visual.test.js` 的两条穿透用例改回热区时序并写明右键后果。全量 483 例测试通过。

## v1.7.13 迷你播放器右键菜单与任务栏托盘完全一致

- 发布版本从 `1.7.12` 提升为 `1.7.13`；已安装 `1.7.12` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **迷你播放器右键菜单现在和任务栏托盘右键一模一样，每一项都能点**：显示 Mineradio、关闭按钮最小化到托盘、最小化时显示迷你播放器、迷你播放器样式（标准 / 极简）、开机自动启动、退出 Mineradio。此前迷你播放器只有 3 项（显示播放器 / 迷你播放器样式 / 退出播放器），勾选类设置一项都没有。
- 两处菜单改为共用同一份模板构建器 `buildAppContextMenuTemplate()`，托盘和迷你窗口从此不可能再各自漂移；勾选态一律实时读取真实设置，改完设置立刻重建托盘菜单。
- **修掉「拖拽区右键弹出的是几乎全灰的菜单」**：Windows 把 `-webkit-app-region: drag` 区域的右键当成非客户区处理，直接弹窗口系统菜单，而迷你窗口不可缩放 / 不可最小化 / 不可最大化，那份系统菜单里只剩「关闭」能点，renderer 连 `contextmenu` 事件都收不到。现在窗口侧监听 `system-context-menu` 并 `preventDefault()`，换成同一份应用菜单；非拖拽区的右键仍走 renderer 的 `context-menu`，两条路弹出的菜单完全相同。
- 标准与极简两套外壳的 `.mini-shell` 都是整块拖拽区，且共用同一个窗口工厂，所以**极简播放器同样整窗任意位置可右键**，菜单内容与标准外壳、任务栏托盘三者一致。
- `tests/mini-player-context-menu.test.js` 扩到 6 例：直接执行模板构建器并逐项点击，钉死项目顺序、勾选态跟随真实设置、开机自启失败回退勾选、每一项都可点不可灰，以及两条右键路径的挂接。全量 483 例测试通过。

## v1.7.12 迷你播放器整窗可右键

- 发布版本从 `1.7.11` 提升为 `1.7.12`；已安装 `1.7.11` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **收回态的迷你播放器现在整个窗口任意位置都能右键**，不再只有封面可以。`v1.7.11` 的右键菜单只在封面上能弹出来的根因是旧的穿透规则：收回后只有封面热区参与命中、其余区域全部交还桌面，右键根本落不到窗口上。现在规则改为「指针在窗口内 = 整窗保持可交互，指针离开窗口 = 立即恢复穿透」：转发/真实 `mousemove` 只会在指针位于窗口上时到达，收到即视为在窗；`document mouseleave` 清零在窗标记并恢复穿透，不影响窗口以外区域的正常点击。
- 配套行为：收回态空白区域在指针悬停期间参与命中后，双击空白处恢复主播放器（原有交互，此前只有指针在窗口上时才可用）；封面热区（外扩 `6px`）仍然单独负责「悬停展开」的触发与自动收回排定，展开/收回节奏不变；封面拖动、靠边镜像展开、桌面歌词入口全部不受影响。
- 极简外壳本来就没有鼠标穿透，整窗天然可右键，本轮无需改动；右键菜单 `v1.7.11` 已在两种外壳共用的窗口工厂上统一挂接。
- 收回态穿透的行为约定已按新规则重写（`.context/conventions/mineradio-mini-player-collapse.md`），`tests/mini-player-visual.test.js` 的两条穿透回归用例同步改写为新时序（进入窗口恢复交互、离窗恢复穿透、拖动结束后指针在窗不穿透）。全量 480 例测试通过。

## v1.7.11 迷你播放器支持右键菜单

- 发布版本从 `1.7.10` 提升为 `1.7.11`；已安装 `1.7.10` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **右键迷你播放器弹出原生菜单**，项目与托盘右键一致：`显示播放器`、`迷你播放器样式`（标准/极简）、`退出播放器`。标准与极简两种外壳都支持。
- `显示播放器` 走 `focusMainWindow()`，与迷你播放器上的恢复按钮完全同一条路径；`迷你播放器样式` 子菜单与托盘菜单同一套 radio 项（`标准（带封面）` / `极简（无封面）`），切换即重建成对应外壳；`退出播放器` 与托盘退出同路径（`appQuitting = true` + `app.quit()`），不会误触「关闭到托盘」。
- 右键由主进程 `webContents.on('context-menu')` 统一接管并阻止默认菜单，两种外壳共用同一窗口工厂天然覆盖；收回态鼠标穿透期间右键落不到窗口上，菜单只在可交互状态下出现，不影响穿透与热区恢复机制。迷你页面 DOM、布局、配色零改动。
- 新增 `tests/mini-player-context-menu.test.js`（3 例）：钉死 context-menu 监听与 `preventDefault`、菜单三项的接线（恢复同路径、样式 radio、退出同托盘路径）、两种外壳共用工厂；`.context/conventions/mineradio-mini-player-collapse.md` 补充右键菜单约定。全量 480 例测试通过，隔离实例冒烟验证迷你窗口创建加载健康。

## v1.7.10 搜索结果面板有了入场动画

- 发布版本从 `1.7.9` 提升为 `1.7.10`；已安装 `1.7.9` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **修掉搜索结果面板「砸出来」的生硬感。** 用户反馈点击搜索后界面出现得太僵硬：排查发现 `#search-results`（搜索下拉那块玻璃面板）一直是 `display:none → display:block` **硬切**，没有任何过渡——回车或点击的一瞬间面板凭空出现。现在 `.show` 挂上 `search-results-in` 入场动画：`260ms` 内 `opacity 0 → 1`、`translateY(-8px) → 0`，缓动用与搜索区下滑同一族 `cubic-bezier(.2,.7,.2,1)`，观感是面板轻轻浮现在搜索框下方，而不是突然砸出来。
- 动画只走 `transform` / `opacity` 合成器属性，不动画布局属性；玻璃模糊（`backdrop-filter: blur(40px) saturate(1.4)`）与边框、阴影、配色全部原样。面板已打开时重复添加 `.show` 不会重播动画（不会闪），只有关闭后再打开才播一次；`prefers-reduced-motion: reduce` 下直接跳过。
- 关闭侧保持原有的即时隐藏（下拉面板惯例，避免玻璃残留）；搜索区本体的下滑动画经逐帧采样确认本来就有 `.45s` 曲线，未改动。
- 新增 `tests/search-results-entrance.test.js`（3 例）：钉死默认 `display:none` 与玻璃滤镜不被改动、`.show` 必须带入场动画、关键帧只含 `transform`/`opacity`、降低动效偏好下跳过。全量 477 例测试通过。

## v1.7.9 每次启动省掉两趟隐藏窗口空转

- 发布版本从 `1.7.8` 提升为 `1.7.9`；已安装 `1.7.8` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **修掉一次纯空转的启动开销。** 排查发现 `migratePrimaryProfileState()` 在打包版每次启动、主窗口创建之前都会执行一遍「读出 `localStorage` 再原样写回」：这条路径各开一个隐藏 `BrowserWindow`、拦截 `http` 协议做一次完整页面导航再 `executeJavaScript`，最后还强制 `flushStorageData()` 刷盘。微基准实测一趟约 `113ms`（写）/ `20ms+`（读），而在没有旧档迁移待办、本地服务端口没变的稳态下，这次读写的结果和现状完全相同——每次启动白白多付约 `130~150ms`。
- 现在稳态直接短路：新增 `ui-state-origin-marker.json` 记录上次完整迁移写入的目标 `origin`，下次启动只要「旧档迁移已完成 + 端口没变」就跳过全部隐藏窗口探测，主窗口提前 `~130ms` 开始创建。端口变化（`localStorage` 按 `origin` 隔离，这是迁移机制存在的意义）或旧档迁移待办时仍走完整迁移路径，行为与 `v1.7.8` 一致；用户数据丢失时渲染层 `preload` 的 `restorePersistentUiState()` 仍会用 `desktop-ui-state.json` 兜底补齐缺失键，这层保险不变。
- 主进程新增的只有一个小 JSON 标记文件的读写（临时文件 + `rename` 原子落盘，沿用既有模式）；渲染进程、界面、交互零改动。
- `tests/profile-state-migration.test.js` 新增守卫：稳态短路必须发生在隐藏窗口 `localStorage` 读取之前、旧档迁移待办时不得短路、完整迁移收尾必须记录 `origin` 标记。全量 474 例测试通过。

## v1.7.8 启动首帧不再被 vendor 脚本卡住

- 发布版本从 `1.7.7` 提升为 `1.7.8`；已安装 `1.7.7` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **主窗口首帧提速。** `three.js`（约 `603KB`）和 `gsap.min.js`（约 `73KB`）原来作为阻塞脚本写在 `<head>`，HTML 解析到第 11 行就要停下来等这两个文件解析完，首帧被整体推迟。现在它们挪到 `</body>` 前、`app.js` 之前，脚本相对执行顺序完全不变（`app.js` 顶层就实例化 `THREE` 场景，这个先后是硬依赖），但 HTML 与 CSS 先解析完再跑脚本，冷启动首帧估计提前 `50~150ms`（热启动大部分被 V8 code cache 摊掉）。
- **vendor 库 7 天新鲜缓存。** 本地服务器原来对所有静态文件发 `Cache-Control: no-cache`，每次启动每个文件都要一次 `304` 重验证。现在 `/vendor/*` 发 `public, max-age=604800`：vendor 库跟着安装包版本走、不会单独热替换，7 天内启动免重验证，再省约 `10~30ms`；其余静态文件仍保持 `no-cache` 即时生效。
- 布局、配色、文案、交互入口与脚本执行顺序零改动；`public/index.html` 只移动了两行 `<script>` 标签的位置，没有新增或删除任何元素。

## v1.7.7 收缩过渡改成看得见的一段动作

- 发布版本从 `1.7.6` 提升为 `1.7.7`；已安装 `1.7.6` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **修好了 `v1.7.6` 那段「看不出变化」的收缩过渡。** 实测抓到病根：旧版在 `150ms` 就把窗口交给系统最小化，而那一刻外壳还有约 `19%` 不透明度、只缩到 `88.5%`，于是 Windows 自己那段飞向任务栏的最小化动画盖在还看得见的界面上，两段动作互相打架，最终看到的仍然是熟悉的原生最小化。
- 现在收缩必须先跑完再交接：外壳 `200ms` 内加速收拢到 `60%` 并淡到全透明，`240ms` 才调用系统最小化。窗口 `html` / `body` 本身完全透明，外壳一淡完窗口里就没有可见内容，系统那段最小化动画没有东西可动，两段动作才真正接成一次。
- 收缩幅度和曲线一起加大：`scale` 终态从 `.86` 压到 `.6`，`transform` 走 `cubic-bezier(.3,0,.8,.15)` 的加速进入，配合同步加速的淡出，读起来是「被吸进迷你播放器所在的角落」，而不是原地淡一下。
- 最大化状态下收缩时补一个 `26px` 圆角，避免直角全屏矩形缩小时露出生硬的边。
- **修掉收缩中途原点跳变。** `transform-origin` 不参与过渡，而预热 IPC 的回包常常落在动画中段，旧版会当帧改写 `--mini-collapse-x/y`，外壳缩到一半突然掉头。现在过渡进行中的原点只缓存，等收尾复位时（窗口已最小化、外壳不可见）再落地，下一次收缩就用上准确原点；悬停预热拿到的原点仍然即时生效，所以正常悬停再点的路径第一次就朝正确的角落收。
- 迷你播放器入场动画同步加大：标准外壳 `280ms` / `translateY(7px) scale(.9)`，极简外壳 `260ms` / `translateY(6px) scale(.92)`，接上主界面消失后的空档。
- 布局、配色、文案和交互入口零改动；`prefers-reduced-motion: reduce` 下收缩仍压到 `60ms`、入场淡入仍直接跳过。
- `tests/mini-player-collapse-transition.test.js` 补到 15 例，新增一条把「淡出与缩放必须在交接前至少 `30ms` 结束」和「收缩终态 `scale ≤ .7`」钉死的回归断言，并断言过渡进行中不改收缩原点、收尾时才落地。

## v1.7.6 主界面收缩到迷你播放器有了连续过渡

- 发布版本从 `1.7.5` 提升为 `1.7.6`；已安装 `1.7.5` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **最小化不再是「主界面一瞬间消失、迷你播放器过一会儿才冒出来」。** 点最小化后主界面先朝迷你播放器所在的角落收拢（`260ms`，`transform` + `opacity` + 轻微压暗），收缩到位才交给系统最小化，两段动作接成一次连续过渡。
- 收缩方向按迷你播放器实际位置算：主进程新增 `mineradio-mini-player-prepare-transition`，回传迷你窗口中心相对主窗口的归一化坐标，renderer 写成 `--mini-collapse-x/y` 作为 `transform-origin`；把迷你播放器拖到哪个角，主界面就朝哪个角收。
- **冷启动空白间隙补上了。** 迷你窗口以前是最小化那一刻才新建并加载，`renderer` 启动约 `600ms` 全都露在外面。现在悬停最小化按钮（停留 `90ms`）就开始预热，点击时再补一次，最小化落地时首帧通常已经就绪。
- 预热窗口不常驻内存：`2600ms` 内没有真的进入迷你模式就由 `discardMiniPlayerPrewarm()` 丢弃，主窗口恢复即销毁迷你窗口的原策略不变。
- 迷你播放器两套外壳（标准 / 极简）改为等窗口真正显示且首帧状态到位后再淡入，不再闪一下 `Mineradio / 等待播放` 占位文案；任何情况下 `1500ms` 兜底都会让外壳可见。
- 迷你播放器关闭、非桌面外壳、全屏或全屏过渡进行中时一律保持原生最小化，不做任何视觉改动；`prefers-reduced-motion: reduce` 下收缩压到 `60ms` 且入场淡入直接跳过。
- 布局、配色、文案和交互入口零改动，`public/index.html` 没有新增任何元素；新增 `tests/mini-player-collapse-transition.test.js`（14 例）覆盖收缩时序、悬停预热节流、预热丢弃幂等与原点归一化。

- 发布版本从 `1.7.4` 提升为 `1.7.5`；已安装 `1.7.4` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 主题新增 `--th-bg-color` / `--th-bg-tint` / `--th-bg-tint-opacity` 三个变量，可轻量调整默认底色与封面模糊背景的色调；不改玻璃模糊、粒子、电影镜头和壁纸逻辑。
- 用户自定义的纯色、图片、视频与透明度仍然优先；Wallpaper Engine 与「播放器背景板」也保持原有图层关系，主题不会覆盖用户自己选的背景。
- 午夜靛蓝、暖琥珀与石墨升到 `1.5.0` 并接入背景变量；安装包新增 **深海微光** / **暗焰余晖** / **冷杉夜雾** 三份纯背景主题，自带主题总数变为五份，默认都不启用。
- 纯背景主题只需三个 `vars`，不需要附加 CSS；它们仍是互斥的 `theme`，启用时会关掉当前主题，面板回落到默认配色。
- 测试与文档同步补齐：钉住五份内置主题与示例文件逐字段一致、三份纯背景主题的最小负载、背景变量清洗以及用户自定义背景优先级。

## v1.7.4 插件只保留主题一种，音源与歌单能力整体移除

- 发布版本从 `1.7.3` 提升为 `1.7.4`；已安装 `1.7.3` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **插件现在只有主题一种。** 音源插件与歌单插件连同它们的六个钩子（`search` / `url` / `lyric` / `cover` / `playlists` / `playlistDetail`）一起删除，`PLUGIN_KINDS` 只剩 `['theme']`，`.json` 包不写 `kind` 默认就是 `theme`。
- **插件的全部网络能力被拿掉。** 沙箱里的 `mineradio.request` / `requestJson` 删除，`@host` 白名单与 `PLUGIN_NO_HOST` / `PLUGIN_HOST_NOT_ALLOWED` / `PLUGIN_PROXY_UNAVAILABLE` / `PLUGIN_URL_EMPTY` / `PLUGIN_TOO_MANY_REQUESTS` 这些错误码一并删除，`mineradio` 现在正好只有 `version` / `manifest` / `on` / `log` 四个成员。
- 本地代理整条链路删除：根目录 `plugin-proxy.js`（366 行）、`server.js` 里的 `/api/plugin/fetch` 与 `/api/plugin/stream` 入口、主进程的 `PLUGIN_TOKEN` 与 `mineradio-plugin-proxy-info` IPC、preload 的 `getPluginProxyInfo` 桥。插件从此没有任何出网通道，宿主也不再替它代发请求。
- 界面上跟着去掉的：搜索结果里的插件音源分区与插件来源标签、插件管理弹窗里的「全部 / 主题 / 音源 / 歌单」筛选条与插件歌单面板、插件歌曲的播放路径（`playQueueAt()` 里 144 行插件取流分支）。FX 面板的插件折叠区副标题从「主题 / 音源 / 歌单」改成「主题」，两处说明文案改成「插件只换外观」。
- **`.js` 脚本主题与 Worker 沙箱保留**：需要按时间或系统状态算颜色的主题仍然可以写 `@kind theme` 的 `.js` 并注册 `theme` 钩子，返回 `{ vars, css }`。钩子名只认 `theme` 一个，注册别的会直接启动失败；返回值走与声明式负载完全相同的清洗，脚本里绕不过去。
- 旧存档自动收拾干净：`normalizePluginRecords()` 现在把 `source` / `playlist` 记录整条丢掉，`hosts` 字段不再进清单，装过旧插件的用户升级后不需要手动清理。
- GitHub 上那些 LX Music / 落雪音乐系的「音源插件」明确不支持也不打算适配：它们要的就是宿主代发请求返回播放直链，而这条通道已经没有了。它们要么没有 `@kind theme`（`PLUGIN_BAD_KIND`），要么连 `@id` 都不写（`PLUGIN_BAD_ID`），在解析阶段就被拒。
- 主题相关的一切没动：`--th-*` 变量族与 63 处覆盖、迷你播放器 `--th-mini-*` 转发链路、主题互斥、自带的午夜靛蓝 / 暖琥珀两份主题、取色器行内变量压过插件主题的优先级，全部与 `v1.7.3` 一致。
- 删除 `tests/plugin-proxy.test.js`；`tests/plugin-system.test.js` 重写成 24 例，并**正向断言**被删掉的能力不会悄悄长回来：清单模块里搜不到 `host`、运行时源码里搜不到 `fetch` / `XMLHttpRequest` / `api/plugin/*`、运行时导出面精确等于 13 个主题相关 API、沙箱探针里 `request` / `requestJson` 都是 `undefined`。测试环境也刻意不再提供 `fetch` 与 `URL`。
- 文档同步重写：`docs/PLUGIN_AUTHORING.md`、`examples/plugins/README.md`、`.context/architecture/mineradio-plugin-system.md`。
- 全量 Node 回归 `454/454`。

## v1.7.3 主题终于能改动大部分界面，迷你播放器一起换色

- 发布版本从 `1.7.2` 提升为 `1.7.3`；已安装 `1.7.2` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- **主题覆盖面从 18 处扩到 63 处（离屏 Chromium 实测 79 个探针）。** 之前「装上主题只有几块变色」的根因是 `public/app.css` 尾部大量写死的 `!important` 字面值，其中好几条特异性是 `(1,1,1)`（如 `html.control-glass-svg-ok #playlist-panel`），主题在 `css` 通道里写同名选择器只有 `(1,0,0)`，层叠上压不过去。
- 修法是就地变量化：每条赢下层叠的字面值改写成 `var(--th-x, <原来的字面值>)`。没装主题时取回落值，79 个探针 × 12 个计算属性与改动前逐字节一致，**默认外观零变化**。
- 这一版真正跟着主题换色的包括：选择歌单来源的弹层、左侧歌单面板（面板底、卡片、悬停态、展开态）、右侧音效面板与其页签条、搜索框与搜索结果、搜索模式标签、底部控制条、收藏项、插件列表项、各处分隔线与次级文字。
- 新增 `--th-*` 变量族并支持链式回落：只设 `--th-panel-*` / `--th-row-*` 这些通用组就能整体换色，`--th-side-panel-*` / `--th-popover-*` / `--th-chip-*` 用来单独微调。清单见 `examples/plugins/README.md`。
- **迷你播放器跟着主题换色。** 它是独立窗口、不加载插件运行时，所以主窗口新增了一条转发：运行时暴露 `themeVars()`，主进程按签名去重后把整表塞进现有的 `mineradio-mini-player-state` 补丁通道，迷你窗口收到后写到自己的 `documentElement` 上。标准窗口 18 个探针里 17 个换色，极简窗口 14/14。
- 转发链路上主进程会再清洗一遍（只收 `--th-` 前缀、单值 200 字符上限、最多 64 条、照样拒 `;` `{` `}` 和 `url()`）—— 迷你窗口是另一个渲染进程，不该依赖上游的清洗结果。
- 换主题时迷你窗口会擦掉这一版不再出现的变量名，否则旧主题的颜色会残留在没被覆盖的那几个变量上。启用、禁用、安装、卸载插件之后都会立刻补推一次，不用等下一次切歌。
- 三份示例主题（午夜靛蓝 / 暖琥珀 / 石墨）各补到 55 个变量并抬到 `1.4.0`：内置主题的种子只在版本号更高时才替换用户 profile 里存着的那一份，不抬版本装过旧版的人看不到改动。
- 边界一个没动：任何 `*-filter` 变量（玻璃的模糊/饱和度黄金参数）保持原样，面板渐变第一段仍是 `rgba(var(--home-accent-rgb),…)`，卡片的选中/已添加/当前播放描边仍读 `rgba(var(--fc-accent-rgb),…)` —— 取色器的地盘主题够不到，迷你播放器封面的青色律动光晕也留作字面值。
- 新增 `tests/plugin-theme-reach.test.js`：钉住 `--th-*` 变量集、三份主题都能过清洗、载荷改了必须抬版本、迷你播放器两套外壳的变量与回落值、主进程侧清洗与签名去重、以及 renderer → 主进程 → 迷你窗口整条转发链路。测试总数 454 → 461。

## v1.7.2 自带两份主题，主题改为互斥

- 发布版本从 `1.7.1` 提升为 `1.7.2`；已安装 `1.7.1` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 安装包开始自带两份声明式主题：**午夜靛蓝**（深蓝黑底 + 蓝调玻璃）和**暖琥珀**（暖黑底 + 金调玻璃）。首次启动自动出现在插件列表里，不用手动导入。
- 自带主题**默认不启用**，界面默认仍是原本的金色玻璃 —— 升级不该擅自换掉全体用户的外观，要用就自己去插件管理里打开。
- 自带主题走的是与用户导入完全相同的 `parsePluginPackage()` 通道：变量白名单、CSS 清洗、体积上限一视同仁，不享受任何豁免。
- 自带主题卸载后不会在下次启动时又冒出来（卸载记录落在 `mineradio-plugins-builtin-v1`）；想要回来就再手动装一次 `examples/plugins/` 下的同名文件，装回后卸载记忆自动清掉。
- 自带主题只在安装包里的版本号高于本地记录时才覆盖，覆盖时保留用户的启用状态和安装时间；用户自己改过的同 id 高版本不会被按回去。
- **主题插件改为互斥：同一时刻只有一个主题能启用。** 启用一个主题时其它启用中的主题会被自动关掉，安装一个新主题同理，提示里会点名是哪个被关了。
- 两份主题同时生效时，后注入的那份会盖住前一份的同名变量，最终外观取决于安装顺序，用户既看不懂也调不动，所以改成换而不是叠加。
- 升级时会顺手修好历史存档：`1.7.2` 之前同时启用了多个主题的，启动后收敛成最近启用的那一个并立即落盘。
- 三份示例主题的着色强度提高到肉眼可分辨的程度（面板/卡片/搜索框底色与内发光），`backdrop-filter` 的模糊与饱和度参数一律没动，渐变第一段仍保留 `rgba(var(--home-accent-rgb),…)`，用户自己挑的强调色继续透出来。
- 移除两份示例脚本插件（互联网档案馆音源与歌单）。它们依赖第三方公益服务，节点间歇性 500/超时会被当成播放器本身的毛病。`source` / `playlist` / `lyric` 三类脚本插件的能力和接口没有任何变化，只是不再附带示例。
- `examples/plugins/README.md` 重写：写清主题真正能改的九个变量、三类改了没反应的变量（被取色器钉在行内的、声明了没人读的、被后面 `!important` 字面值盖掉的）、面板底色为什么必须走 `css` 通道，以及自带主题与互斥规则。
- 新增 `public/plugin-builtin-themes.js`（已加入 `build.files` 白名单守卫）；测试钉住它与 `examples/plugins/theme-*.json` 逐字段一致，改了示例文件忘了改这里会红。
- 全量 Node 回归 `454/454`。

## v1.7.1 修复 v1.7.0 安装包无法启动
- 发布版本从 `1.7.0` 提升为 `1.7.1`。**`v1.7.0` 安装包无法启动，请直接升级到本版本。**
- 修复 `v1.7.0` 安装后双击没有任何反应的问题。根因是新增的 `plugin-proxy.js` 没有加入 `package.json` 的 `build.files` 打包白名单，安装包里缺这个文件，`server.js` 在 `require('./plugin-proxy.js')` 处抛 `MODULE_NOT_FOUND`，主窗口和本地服务都建不起来，进程活着但没有界面。
- `plugin-proxy.js` 同时加入 `build.asarUnpack`：`server.js` 从 `app.asar.unpacked` 运行，相对 require 是按它自己的真实路径解析的，只打进 asar 仍然找不到。
- 本地服务加载失败不再静默：`createWindow()` 里 `require(server.js)` 改为显式 try/catch，失败时打日志、弹出「Mineradio 启动失败」错误框并退出，而不是变成未处理的 Promise rejection 让用户面对一个没有窗口的进程。
- 新增 `tests/packaging-file-whitelist.test.js`：扫描 `desktop/main.js`、`desktop/preload.js`、`server.js` 里的相对 require，逐个核对目标文件是否被 `build.files` 收录，并单独钉住插件系统四个源文件与 `asarUnpack` 的一致性。回滚白名单后这条测试会失败，所以同类漏装不会再溜过去。
- 插件系统功能本身与 `v1.7.0` 一致，没有行为改动。
- 全量 Node 回归 `446/446`；已用 `electron-builder --win dir` 实际打包并启动验证：修复前复现 `Cannot find module './plugin-proxy.js'` 且无窗口，修复后本地服务正常监听、启动流程走到 `home-revealed`。

## v1.7.0 新增插件系统：主题 / 音源 / 歌单
- 发布版本从 `1.6.3` 提升为 `1.7.0`；已安装 `1.6.3` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 新增插件系统，支持三种插件：主题插件（换配色与 CSS）、音源插件（提供搜索、播放地址、歌词、封面）、歌单插件（提供歌单列表与歌单详情）。不内置任何插件，全部由用户自己导入。
- 插件格式为单文件：`.js` 用开头的 `/** @id / @name / @kind / @version / @author / @host */` 块注释当清单，正文里用 `mineradio.on(...)` 注册钩子；`.json` 用于纯声明式主题包。单文件上限 512 KB，最多安装 40 个。
- 插件跑在独立 Web Worker 沙箱里：`fetch`、`XMLHttpRequest`、`WebSocket`、`importScripts`、`indexedDB`、`caches`、`Worker`、`postMessage` 全部剥除，没有 DOM、没有 localStorage、没有主进程 IPC 桥，因此拿不到本地文件与账号；插件正文经 `new Function` 执行，看不到宿主闭包变量。
- 插件联网只能走宿主代发：渲染进程按插件清单里的 `@host` 白名单逐条放行（只放 https，命中声明域名及其子域），再交给本地代理出网。被拒的请求根本不会到达代理。
- 本地代理拦掉私网、环回、链路本地和组播地址，并把连接钉在解析出的公网 IP 上（`servername` + 显式 `Host`）以防 DNS rebinding；转发请求头只留 `referer` / `user-agent` / `cookie`，响应体上限 8 MB、请求超时 15 秒、流超时 30 秒、最多跟 5 次跳转。
- 音频与封面统一经本地流代理，代理补上 `Access-Control-Allow-Origin` 与 `Cross-Origin-Resource-Policy`，所以插件歌曲同样有频谱律动、封面取色和封面光晕。
- 主题插件的变量与 CSS 会被清洗：带 `;` `{` `}` 的变量值、指向远端的 `url()`、`@import`、`expression(`、`javascript:` 和任何 `<` 一律去掉，只保留 `data:` 内联图；注入到唯一一个 `<style id="mineradio-plugin-theme-style">` 节点，用户自己调的主色仍然优先。
- 设置面板新增「插件」折叠区显示已装数量并进入插件管理弹窗；弹窗支持安装插件文件、按类型筛选（全部 / 主题 / 音源 / 歌单）、启用停用、卸载，以及直接播放插件歌单。
- 搜索结果里插件歌曲与本地歌曲同列，来源标签显示插件名；插件歌曲不能加入「特别喜欢」和本地歌单（那两处按本地文件引用存盘），点击时给出明确提示。
- 本地播放路径一行未改：插件曲目走并列的新分支，本地曲目仍走原有路径。插件歌词复用现有本地歌词管线，LRC 解析与逐字判定不变。
- 全量 Node 回归 `443/443`；`node --check public/app.js`、`node --check public/plugin-runtime.js`、`node --check public/plugin-manifest.js`、`node --check public/plugin-sandbox.js`、`node --check plugin-proxy.js` 与 `node --check server.js` 均通过。

## v1.6.3 壁纸模式解锁并新增展示位置切换
- 发布版本从 `1.6.2` 提升为 `1.6.3`；已安装 `1.6.2` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 壁纸模式开关和壁纸透明度滑块从「开发中，暂不可用」解锁，恢复为可用设置项。
- 设置面板新增「壁纸展示位置」分段控件：`桌面壁纸层` 把壁纸挂到 Windows 桌面，显示在桌面图标和其它窗口后面；`播放器背景板` 把同一幅画面画在播放器窗口内，作为界面最底层背景板。
- 两个展示位置共用新的 `public/wallpaper-effect.js` 单一画面实现，桌面覆盖层和窗口内背景板的画面、调度节奏与关闭态资源释放完全一致。
- 展示位置永远只有一侧在跑：切到背景板会关闭桌面壁纸覆盖窗口并停止封面 IPC 传输，切回桌面壁纸层会释放背景板画面实例、封面与粒子数组。
- 背景板排在自定义背景和 Wallpaper Engine 图层之前，用户自选的背景图片、视频和 WE 壁纸仍然覆盖在上层；启用背景板时让位封面模糊底图，避免两层画面互相糊掉。
- 壁纸开关状态改用 `wallpaper-surface-v1` 存档 schema 恢复，从旧版本升级不会因为历史强制关闭的存档而误开或误关壁纸。
- 全量 Node 回归 `420/420`；`node --check public/app.js`、`node --check public/wallpaper-effect.js`、`node --check server.js` 与 `git diff --check` 均通过。

## v1.6.2 修复搜索结果面板自动弹出
- 发布版本从 `1.6.1` 提升为 `1.6.2`；已安装 `1.6.1` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 修复本地曲库恢复、导入、清空或后台资产补水时，搜索结果面板未经用户操作自动出现的问题。
- 搜索结果现在只有在用户点击搜索框并打开面板后才会显示；面板已经打开时，曲库变化仍会原地刷新结果，不改变搜索排序和结果数量。
- 移除未提交的迷你播放器实验改动，保持 `v1.6.1` 的迷你播放器行为不变。
- 全量 Node 回归 `412/412`；`node --check public/app.js`、`node --check server.js` 与 `git diff --check` 均通过。

## v1.6.1 扩展本地音乐格式支持
- 发布版本从 `1.6.0` 提升为 `1.6.1`；已安装 `1.6.0` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 新增 `MP2`、`M4B` 有声书、`AIF`、`AIFF`、`AIFC` 音频文件识别、文件夹扫描、文件选择器和本地代理 MIME 支持。
- 保留已有 MP3、FLAC、M4A、WAV、OGG/OGA、AAC、Opus、WebM/WebA、歌词与封面格式支持。
- 全量 Node 回归 `411/411`；关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.6.0 修复迷你播放器封面律动与光晕无反应
- 发布版本从 `1.5.9` 提升为 `1.6.0`，已安装 `1.5.9` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 修复标准迷你播放器在主窗口隐藏播放时，低平滑节拍分析器返回空频谱帧，把主分析器的有效音频覆盖为零，导致封面律动和封面光晕完全没有反应的问题。
- 低平滑分析器有有效低频数据时仍优先用于律动；切换后台或 `AudioContext` 恢复瞬间出现空帧时，自动回退到已接入输出链路的主分析器，不改变正常播放链路。
- 修复页面 `document.hidden` 先于桌面窗口状态 IPC 到达时，迷你播放器采样定时器没有启动的问题；`visibilitychange` 会立即同步定时器，隐藏播放仍持续低开销采样。
- 保留 `1.5.8` 已发布的封面律动/光晕独立开关与 `0.00 ~ 3.00` 强度设置；本次不启动新窗口、不接管全局鼠标键盘，不影响电脑上的其它操作。

## v1.5.9 修复本机代理线路无法取消更新
- 发布版本从 `1.5.8` 提升为 `1.5.9`，已安装 `1.5.8` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 修复 `本机代理` 线路点击“取消更新”后任务仍在后台继续下载的问题：进度条会一直往前走、`已接收` 字节继续增长，最终整个安装包被拉完，取消实际无效。`直连 GitHub`、`国内加速` 与自动测速线路不受此问题影响。
- 根因是代理线路的响应体由 `Readable.toWeb()` 包装，不识别 `fetch` 的中止信号，而代理请求在响应头到达时就已经摘掉了中止监听，导致连接无人可以打断。
- 现在安装包与补丁的下载循环逐块检查取消状态，取消与具体传输实现无关；同时代理响应会跟随取消销毁响应流和 `CONNECT` 隧道 socket，连接立即释放，不再把剩余流量白读完。
- 测速阶段同样会在取消后立刻停止读取剩余样本。取消依然是正常终态：状态收敛为 `已取消`，不写失败线路、不换线、不产生错误提示，可以立即改线路重新下载。
- 全量 Node 回归 `403/403`，新增代理响应跟随取消、预先取消立即释放与下载循环逐块检查三项断言，关键 JavaScript 语法检查与 `git diff --check` 均通过。

## v1.5.8 更新线路手动选择、取消更新与迷你封面律动光晕强度
- 发布版本从 `1.5.7` 提升为 `1.5.8`，已安装 `1.5.7` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 软件内更新面板新增线路选择，四档可选：`自动测速`（保持原有最快线路行为）、`直连 GitHub`、`国内加速`（只走配置镜像）、`本机代理`（走系统代理）。选择立即落盘到 `mineradio-update-route-v1`，重启后仍然生效。
- 线路选择会真正限制候选来源：非自动档只保留对应线路的下载候选，选中的线路没有可用候选时直接给出明确提示，不再静默回退到别的线路。
- 面板会显示当前线路可用性：国内加速显示可用镜像数量，本机代理显示检测到的系统代理地址；没有检测到代理时提示先在系统里设置代理。
- `本机代理` 使用手写的 `CONNECT` 隧道下载安装包和补丁，不引入任何第三方依赖；代理地址来自 Electron 的系统代理解析结果，也可由请求显式指定。
- 下载过程中新增“取消更新”按钮，可随时中断正在排队或下载中的任务，取消是正常终态而不是错误；已经进入安装/补丁应用阶段的任务不允许取消，避免留下混合版本。
- 迷你播放器封面律动和封面光晕改为可调强度：`DIY -> 迷你播放器 -> 封面效果` 下新增“封面律动强度”和“封面光晕强度”两个滑块，范围 `0.00 ~ 3.00`，`1.00` 是标准力度，往右调越明显，调到 `0` 或关掉对应开关即停用。
- 标准力度下的可见幅度比旧版明显加强：典型能量下封面缩放幅度约为旧版的 `2.5` 倍，外沿光晕透明度从 `0.14` 提高到约 `0.29`，解决“律动和光晕看不出来”的问题。
- 封面光晕不再依附封面律动开关：关掉律动后光晕仍会跟随音乐呼吸，反之亦然；关闭开关时保留用户已选强度，重新打开会恢复原来的档位。
- 强度换算使用饱和曲线，即使拉到最大 `3.00` 也不会把放大后的封面和光晕顶出 `360 × 84` 的透明窗口，实测最坏边距仍有 `1.7px`，不会出现光晕被裁掉半圈的错位。
- 全量 Node 回归 `400/400`，新增更新线路过滤、代理线路解析、取消终态与线路提示测试，扩展迷你播放器律动/光晕强度归一化、滑块持久化与几何上限断言，关键 JavaScript 语法检查与 `git diff --check` 均通过。

## v1.5.7 自动播放开关与迷你播放器悬浮展开错位修复
- 发布版本从 `1.5.6` 提升为 `1.5.7`，已安装 `1.5.6` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 新增“自动播放”开关，入口固定为 `DIY -> 高级 -> 自动播放`：`关闭` / `继续播放` / `随机播放` 三档，选择后立即落盘，下次启动自动生效。
- `继续播放` 在启动载入本地曲库后自动接着上次的歌曲和进度出声；`随机播放` 会切到随机模式、对队列固定洗牌一次，再从中随机挑一首从头开始。
- 自动播放使用底部控制栏选中的“播放歌单”，设置区直接显示当前歌单名称，并提供“选择”按钮打开同一个歌单选择器，可自定义自动播放的歌单范围。
- 启动恢复只触发一次自动播放，后台增量扫描不会重复起播；已经在出声、队列为空或开关关闭时都不打断当前播放，关闭态继续只恢复上次播放位置并保留恢复点。
- 修复标准迷你播放器悬浮展开/收回过程中桌面歌词 `词` 按钮飞到面板中间并被裁切的问题：该按钮是绝对定位元素，收回态 `.transport` 上的 `transform` 会把 `.transport` 变成它的包含块，现改为挂在 `.mini-shell` 直属层并单独淡出。
- 完整面板向左展开时，收回态位移动画一并镜像为 `translateX(-10px)`，面板始终朝远离封面的一侧滑出，不再出现反向挤压。
- 全量 Node 回归 `385/385`，新增自动播放归一化、启动继续/随机起播、单次触发门、播放歌单联动与设置区回归测试，以及桌面歌词按钮层级与收回态淡出回归测试，关键 JavaScript 语法检查与 `git diff --check` 均通过。

## v1.5.6 标准迷你播放器收回态穿透与桌面歌词按钮镜像修复
- 发布版本从 `1.5.5` 提升为 `1.5.6`，已安装 `1.5.5` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 修复标准迷你播放器收回后，原完整面板位置仍然吞掉鼠标的问题：收回态窗口把鼠标事件交还桌面，那块空白区域的点击、双击和拖动直接落到底层的桌面或其它软件上。
- 收回态只有歌曲封面参与命中：指针回到封面热区会立即恢复窗口交互并展开完整面板，短按展开、封面拖动移动窗口、键盘聚焦展开和自动收回都保持原有手感。
- 封面拖动期间和关闭“自动收回”后强制保持窗口交互，拖动中窗口跟随指针不会因为坐标瞬时失配而丢失鼠标。
- 修复完整面板向左展开时，桌面歌词 `词` 按钮没有跟着镜像的问题：向左展开时按钮移动到左下角，与返回按钮的镜像方式一致。
- 全量 Node 回归 `372/372`，新增收回态鼠标穿透、封面热区恢复、穿透 IPC sender 门禁与生命周期重置、桌面歌词按钮镜像测试，关键 JavaScript 语法检查与 `git diff --check` 均通过。

## v1.5.5 迷你播放器封面律动、贴边展开与拖动修复
- 发布版本从 `1.5.4` 提升为 `1.5.5`，已安装 `1.5.4` 的客户端可通过现有更新检查自动发现并安装本版本，不再属于同版本重发。
- 保留并正式发布标准迷你播放器封面律动修复：主窗口隐藏且音频分析上下文挂起时会恢复分析，优先使用低平滑的节拍频谱，并用短期能量基线突出峰谷，避免不同歌曲长期饱和为固定大小。
- 标准迷你播放器按当前显示器工作区的左右余量决定展开方向：靠近右边缘收回时完整面板向左展开，靠近左边缘时向右展开，收回态封面继续贴近对应边缘。
- 点击歌曲封面仍可展开迷你播放器；按住并移动超过约 `5px` 后改为拖动窗口，主进程只接受当前迷你窗口的受信 IPC，按工作区夹紧并持久化坐标，不接管全局鼠标或键盘，不影响其它电脑操作。
- 全量 Node 回归 `368/368`，迷你播放器方向、封面拖动、脉冲恢复和 IPC sender 门禁测试，以及版本一致性、发布工作流清单、关键 JavaScript 语法检查与 `git diff --check` 均通过。

## v1.5.4 迷你播放器与 Wallpaper Engine 生命周期修复重发
- 本次保持版本号 `1.5.4`，在保留 Wallpaper Engine 生命周期与窗口重建修复的基础上，修复标准迷你播放器封面律动、贴边展开方向和封面拖动交互。
- 修复标准迷你播放器封面律动肉眼几乎不可见的问题：主窗口隐藏且音频分析上下文挂起时会恢复分析，优先使用低平滑的节拍频谱，并用短期能量基线突出峰谷，避免不同歌曲长期饱和为固定大小。
- 标准迷你播放器按当前显示器工作区的左右余量决定展开方向：靠近右边缘收回时完整面板向左展开，靠近左边缘时向右展开，收回态封面继续贴近对应边缘。
- 点击歌曲封面仍可展开迷你播放器；按住并移动超过约 `5px` 后改为拖动窗口，主进程只接受当前迷你窗口的受信 IPC，按工作区夹紧并持久化坐标，不接管全局鼠标或键盘，不影响其它电脑操作。
- 全量 Node 回归 `366/366`，迷你播放器方向、封面拖动、脉冲恢复和 IPC sender 门禁测试，以及关键 JavaScript 语法检查与 `git diff --check` 均通过。
- 本次为同版本修复重发；版本比较不会把新版 `1.5.4` 推送给已安装旧版 `1.5.4` 的用户，需要手动下载安装器覆盖安装。
- 修复主窗口重建后 Wallpaper Engine 窗口事件未重新挂载的问题；每次主窗口创建成功都会重新接入最小化、隐藏、恢复、移动、缩放、全屏和关闭处理。
- renderer 崩溃、主 frame 导航、主 frame 加载失败和窗口关闭时，统一停止当前原生 Scene，并清理 DWM helper、指针中继、静音重申定时器和捕获授权。
- pagehide 仅对离开前持有的原生 session 发送最佳努力停止请求；正常刷新会等待旧 session 清理，避免误停新 session 或重复启动 Scene。
- 增加旧窗口迟到事件隔离、并发清理 single-flight、窗口重建 hook、renderer 崩溃、导航、刷新失败和关闭生命周期回归测试；合并迷你播放器修复后全量 Node 回归 `366/366` 通过。

## v1.5.3 桌面歌词拖动原生竞态修复
- 修复桌面歌词解锁并覆盖主窗口标题栏时，拖动主窗口会在两个位置之间反复瞬移的问题；已在实际运行的 `D:\Mineradio\Mineradio.exe` v1.5.2 上复现，原生坐标采样确认主窗口相对鼠标单帧突变最高约 `410px`。
- Windows 主窗口进入原生移动循环时立即关闭桌面歌词窗口的鼠标转发，退出移动循环后再延迟恢复；拖动期间歌词 renderer 无法重新申请指针捕获，避免置顶透明歌词层与系统窗口拖动争抢鼠标。
- `will-move`、`move` 与 `WM_ENTERSIZEMOVE / WM_EXITSIZEMOVE` 形成多层兜底；用户拖动路径和松手后均不再执行主窗口 `setBounds()`，显示器插拔与参数变化仍保留安全纠偏。
- 不改变桌面歌词视觉、位置保存、中键锁定、歌词自身拖动、播放控制、主界面布局和用户数据；新增原生移动消息、鼠标转发关闭、非法指针重捕获和拖动路径无边界重设回归覆盖，全量 Node 回归 `353/353` 通过。

## v1.5.2 本地文件授权与窗口拖动修复
- 加固主窗口导航与 IPC 信任边界：主窗口只能停留在本地 127.0.0.1 服务的根路径，will-navigate 与 will-frame-navigate 阻止外部页面和非可信 frame；所有高权限 IPC 通道（文件扫描、文件读取、设置修改、启动项、热键、更新安装、重启等）统一校验可信主 frame，非法 sender 一律拒绝。
- 本地曲库授权改由主进程统一维护，任意 renderer 路径不得自行扩大本地文件读取范围；已导入曲库重启后仍可正常恢复。
- 修复开启桌面歌词后拖动主播放器松手瞬间被工作区边界拉回的跳位问题：拖动结束只保留释放位置，贴边、跨屏、任务栏遮挡不再触发 setBounds；显示器插拔与参数变化仍执行全量纠偏。
- 延续 %APPDATA%\Mineradio-oirge 用户目录和既有曲库、播放会话、特别喜欢、自建歌单与 DIY 设置，覆盖升级不要求重新导入。
- 新增导航拦截、非法 sender、目录扩权、文件读取边界、重启恢复授权与窗口拖动宽容纠偏回归测试；全量 Node 回归 345/345、关键 JavaScript 语法检查与 git diff --check 通过。

## v1.5.1 软件内更新自动选择最快线路
- 点击安装快速补丁或完整安装包后，服务端会并行探测全部候选线路，并按当前网络下的实测吞吐量自动选择最快线路，不再固定从第一条镜像开始下载。
- 每条线路最多读取 `128 KiB`，统一测速窗口为 `4 秒`；窗口结束前收到的部分样本仍参与排序，完全无响应的线路保留在队尾作为失败切换兜底。
- 快速补丁与完整安装包复用同一选线逻辑；镜像摘要门禁、SHA-256/SHA-512 校验、正式下载空闲超时和失败换线保持不变。
- 更新面板沿用原布局，测速时显示“正在测速更新线路”，选定后继续显示线路、实时速度、已下载大小和剩余时间。
- 全量 Node 回归 `341/341`、关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.5.0 桌面歌词拖动与迷你封面动效修复
- 修复开启桌面歌词后拖动主播放器经过歌词热区时，置顶歌词窗口抢占 Windows 原生鼠标捕获并导致主窗口跳位的问题；主窗口拖动期间强制歌词层穿透，松开后延迟恢复交互，歌词自身拖动保持可用。
- 修复桌面歌词工具栏“下一首”SVG 路径越界、显示成倾斜残缺图形的问题，恢复与“上一首”对称的标准图标。
- 修复标准迷你播放器封面律动肉眼几乎不可见的问题：降低脉冲增量同步阈值，增强低频信号映射，并提高收起封面态与完整面板态的平滑缩放幅度。
- 修复迷你播放器封面光晕被封面裁切的问题；光晕现在同时作用于封面外沿与内部描边，并继续跟随音乐低频强弱变化。
- 修复“封面律动强度”设为 `0` 时仍回退到默认强度的问题；关闭律动或强度归零后封面保持静止，极简迷你播放器继续保持无封面结构。
- 延续 `%APPDATA%\Mineradio-oirge` 用户目录和既有曲库、播放会话、特别喜欢、自建歌单与 DIY 设置，覆盖升级不要求重新导入。
- 全量 Node 回归 `334/334`、关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.4.11 更新圆环与窗口拖动修复
- 同版本修复重发：彻底移除检测更新圆环的 SVG 几何旋转，改用 `stroke-dashoffset` 移动描边；进度环使用固定的 SVG 坐标旋转，避免 Electron 下圆环再次偏心或偏移。
- 修复标题栏“检测更新”圆环在桌面壳中缩放到 `18px` 后仍使用固定 `12px` 旋转轴，导致圆环偏心、转到按钮外侧的问题；检测圆环与下载进度圆环现在都围绕 SVG 正中心旋转。
- 修复拖动主播放器窗口时偶发突然跳位：连续 `move` 事件只同步窗口状态，不再调用可能执行 `setBounds()` 的边界纠偏。
- 用户松开鼠标触发 `moved` 后才单次检查当前显示器工作区；尺寸过大时按副屏工作区缩放，位置越界时夹回可见区域且不强制居中，不干扰 Windows 原生拖动过程。
- 延续 `%APPDATA%\\Mineradio-oirge` 用户目录和既有曲库、播放会话、特别喜欢、自建歌单与 DIY 设置，覆盖升级不要求重新导入。
- 全量 Node 回归 `331/331`、关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.4.10 界面布局与 3D 歌单详情修复
- DIY 控制台新增独立“迷你”页，集中管理迷你播放器总开关、标准/极简样式、封面律动、光晕、悬停展开、圆角和 `× 自动收回`，常用入口不再藏在其它设置中。
- 修复顶栏“检测更新”按钮上偏、DIY 展开后底部操作按钮重叠，以及全屏状态下 Home、DIY、退出全屏按钮中心线错位的问题。
- 修复 3D 歌单详情布局参数传递错误导致坐标和缩放出现 `NaN`、点击详情后面板拉伸错位的问题。
- 打开普通歌单详情时立即隐藏舞台歌词，避免动态镜头下巨型歌词横穿详情面板；关闭详情后按原状态恢复。
- 延续 `%APPDATA%\\Mineradio-oirge` 用户目录和既有曲库、播放会话、特别喜欢、自建歌单与 DIY 设置，覆盖升级不要求重新导入。
- 全量 Node 回归 `328/328`、关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.4.8 歌单定向收藏与全屏过渡
- 当前播放队列、主曲库、歌手详情和自建歌单详情统一支持“收藏到任意歌单”；同一首歌可以同时加入多个自建歌单，不再只能收藏到特别喜欢或单一目标。
- 收藏弹窗明确显示“收藏到「歌单名」”与“已收藏到「歌单名」”，新建歌单后可直接完成本次收藏，避免收藏目标不清楚。
- 歌单选择弹层改为独立深色玻璃面板，强化标题、歌曲数量、当前态和按钮对比度，修复粒子背景穿透导致文字看不清的问题。
- 选择播放来源或收藏目标后，左侧歌单面板恢复自动隐藏状态，不再被永久展开状态锁住。
- 全屏进入与退出增加短暂暗场、轻缩放和回亮过渡，并在窗口尺寸或全屏状态稳定后释放遮罩，改善透明无边框窗口切换时的生硬跳变。
- 支持系统“减少动态效果”偏好；保留透明窗口全屏边界保护、退出按钮、DIY 布局和现有用户数据目录，不改变音乐文件、歌单与设置的升级存储。
- 全量 Node 回归 `323/323`，关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.4.7 多歌单与播放来源选择器
- 本地歌单升级为真正独立的多歌单系统：支持自定义创建、改名、删除、添加歌曲和移除歌曲，编辑歌单不会篡改当前播放队列。
- 收藏弹窗、左侧歌单页和 3D 歌单架统一接入自建歌单；歌曲引用按本地键和路径去重保存，暂时找不到的歌曲不会被误删，重新导入原文件夹后可自动恢复。
- 主播放栏原“普通 / 喜欢”二选一按钮改为歌单选择器，可直接选择全部音乐、特别喜欢或任意自建歌单，并显示歌曲数量、待重新定位数量和当前来源。
- 切换播放来源时优先保留当前歌曲与播放进度；当前歌曲不在目标歌单时从目标歌单第一首开始，空歌单只提示而不会清空当前队列。
- 标准迷你播放器右上角 `×` 改为“关闭自动收回”：点击后关闭封面悬停展开并保持完整控制栏，不关闭迷你播放器；设置会持久化并同步回主界面 DIY 开关。
- 延续稳定的 `%APPDATA%\\Mineradio-oirge` 用户目录与现有存储键，覆盖升级不会要求重新导入原音乐文件夹、特别喜欢、播放会话或已有设置。
- 浏览器真实交互验证通过全部音乐、特别喜欢和自建歌单切换、队列重建与来源持久化；全量 Node 回归 `317/317`，关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.4.6 全屏、更新安装与迷你播放器修复重发
- 修复 Windows 透明无边框主窗口开启全屏后又被工作区边界校正缩回的问题；全屏现在覆盖完整显示器区域和任务栏。
- 主进程不再只依赖 Electron 的 `BrowserWindow.isFullScreen()`，同时使用应用维护的窗口全屏和 HTML 全屏状态，覆盖 Electron 43 在透明窗口上原生查询仍返回 `false` 的情况。
- 修复逻辑全屏状态下退出按钮和 `Esc` 无法可靠恢复普通窗口的问题；HTML 全屏的 `Esc` 继续交给 Chromium 清理 DOM 全屏状态。
- 应用内启动更新安装器后主动退出后台播放器；NSIS 覆盖安装保留注册表恢复的原安装目录，并只关闭该目录、当前 Windows 会话内的 `Mineradio.exe`，避免旧进程占用文件、迁移到默认目录或误关其它安装实例。
- 标准迷你播放器完整控制栏右上角新增 `×`，用于立即收起悬停或键盘聚焦展开的控制栏；关闭“封面悬停展开”并始终显示完整面板时隐藏该按钮，极简模式保持不变。
- 新增透明窗口全屏、HTML 全屏、普通窗口越界纠偏和退出恢复回归测试；Windows 实机验证窗口可从 `1376x774` 进入 `1920x1080` 并恢复原尺寸。
- 全量 Node 回归 `312/312`、关键 JavaScript 语法检查与 `git diff --check` 通过；标准迷你播放器在 `360×84` 实际 Electron 窗口中完成按钮布局、点击折叠和永久完整模式隐藏验证。
- 本次为同版本修复重发；已安装旧版 `1.4.6` 的用户需要手动下载新版安装器覆盖安装。

## v1.4.5 综合修复重发
- 迷你播放器改为可选功能：视觉控制台和系统托盘均可开关；关闭后最小化、关闭到托盘和后台隐藏都不会创建迷你窗口，并立即释放状态、封面和 renderer 资源。
- 标准迷你播放器默认收起为圆角封面，鼠标悬停或键盘聚焦时展开完整控制栏；“封面悬停展开”可关闭并保持完整面板，标准/极简样式、律动、光晕和圆角 DIY 设置继续保留。
- 全屏模式把 DIY 与“退出全屏”固定到 Home 房子按钮左侧，退出入口始终可见，DIY 仍按悬停显现；修复与导入歌曲、账号区域重叠的问题。
- 新增多个独立本地歌单的持久化与播放来源支持；特别喜欢、普通曲库和自建歌单可独立恢复、切换和合并，不再只依赖当前播放队列。
- 优化副屏和较小工作区适配：窗口最小尺寸按当前显示器动态收缩，移动到副屏后自动校正边界，避免窗口超出可见区域。
- 二创正式版固定使用独立的 `%APPDATA%\\Mineradio-oirge` 和稳定高位本地端口，避免与原版 Mineradio 共用后台进程、端口和 Chromium 数据目录；原版关闭后可正常退出，二创关闭到托盘语义保持独立。
- 首次启动自动合并旧共享 profile 与全部 `Mineradio-path-*` 数据，恢复音乐文件夹、播放会话、红心和独立歌单；读取前使用临时副本避免锁住正在运行的原版，旧 profile 后续更新或出现新来源时会再次迁移。
- 修复迁移用隐藏窗口在主窗口创建前触发 `window-all-closed` 导致程序秒退的问题，并补充本地 WAV 播放预加载与错误诊断。
- 全量 Node 回归 `301/301`、关键 JavaScript 语法与空白检查通过；Windows x64 NSIS 安装器已重新构建。

## v1.4.4 迷你播放器封面动效
- 标准迷你播放器默认显示当前歌曲封面圆角矩形图标，悬停或键盘聚焦时展开为完整控制面板，离开后平滑收起。
- 新增封面出现、消失、展开、收起、律动缩放和柔和光晕过渡动画；无歌曲时显示 Mineradio 占位图标。
- 新增视觉控制台 DIY：封面律动开关、律动强度、光晕开关、悬停展开开关和封面圆角调节。
- 律动采用低频音频采样和增量 IPC 同步，主窗口隐藏时仅在迷你播放器启用且播放中采样；极简模式保持无封面。
- 新增迷你播放器视觉、状态缓存和边界校正回归测试，定向测试 `14/14` 通过。

## v1.4.3 Wallpaper Engine 壁纸背景
- 新增独立 Wallpaper Engine 启用开关，默认关闭；选择项目不会自动接管背景，停用后保留上次选择，可随时恢复。
- 新增 Wallpaper Engine 壁纸背景：自动识别本机 Steam/创意工坊库，也支持手动导入项目或场景包。
- Video / 图片壁纸直接作为播放器背景播放；Scene 项目由本机 Wallpaper Engine 原生引擎实时运行，并自动静音壁纸音频。
- 视觉控制台外观页新增 Wallpaper Engine 入口，可搜索、收藏、隐藏、查看项目设置，并一键恢复原背景。
- 桌面主窗口保持透明合成，Scene 走 DWM 缩略图/玻璃采样路径，不改现有玻璃质感、3D 歌单架和桌面歌词。
- 新增 `tests/wallpaper-engine-library.test.js` 覆盖库扫描、静音属性和桌面壳接入。
- 修复部分下载器写入 FLAC 的 `kuwo` / `酷我` 等来源占位标签覆盖文件名歌曲信息；导入 D 盘多级目录时现在保留正确的文件名标题和歌手，并自动让旧错误缓存重新解析。
- 新增三级目录 FLAC 扫描回归与来源占位标签回归，定向 Node 回归 `25/25` 通过。

## v1.4.2 M4A 播放与元数据修复
- 修复本地 `.m4a` 文件未被桌面曲库、文件夹导入和本地文件代理完整识别的问题，统一使用 `audio/mp4`。
- 修复 M4A `data` atom 偏移错误：标题、艺术家、专辑、专辑艺术家、年份、音轨号和 `covr` 封面现在按标准 MP4 布局读取。
- 支持后置 `moov`、UTF-8/UTF-16 标签、JPEG/PNG 内嵌封面，并在后台轻量扫描未覆盖文件尾时保留前台重试状态。
- 提升本地标签缓存 schema，清理修复前可能保存的错误 M4A 元数据；时长、文件大小和独立封面缩略图仍可复用。
- M4A 元数据解析只读取 atom 目录和目标标签/封面范围，不读取 `mdat` 音频内容，保持低 CPU 与低运行内存目标。
- 全量 Node 回归 `266/266`；新增 M4A 标准 atom、UTF-16、PNG 封面、后置 `moov`、缓存失效和桌面扫描回归测试。

## v1.4.1 / v1.4.0 音频格式与本地曲库功能
- 保留 WAV、OGG、M4A 文件夹选择、桌面扫描和对应 MIME 映射；本地文件夹过滤器现已覆盖五种支持的音频格式。
- Electron 升级到 `43.4.0`，新增“特别喜欢”本地歌单、播放来源切换、搜索排序和空曲库恢复稳定性修复。
- 保留桌面歌词、迷你播放器、歌词翻译与本地播放视觉优化。

## v1.3.13 本地曲库空状态与播放来源恢复稳定性
- 修复本地曲库被清空或扫描结果为空时仍恢复旧歌曲、旧播放队列和旧当前歌曲的问题，避免重启后重新出现已经不存在的音乐。
- 修复特别喜欢歌单在本地曲库初始化阶段被误清理的问题，等待曲库准备完成后再校验失效引用。
- 修复普通 / 喜欢播放来源切换后队列顺序校验不严格导致来源混排的问题，并保留切换前的播放进度。
- 新增空曲库清理、来源恢复、队列顺序和喜欢引用清理回归覆盖；全量 Node 回归 `262/262` 通过。
- Windows x64 NSIS 发布只生成安装器、`.blockmap`、`latest.yml` 和 SHA256 清单，不生成或上传 Portable ZIP。

## v1.3.9 主渲染缓存回收与帧调度热路径优化
- 运行时缓存回收从每个有效渲染帧移到一秒级性能采样边界，保留活动态、后台和深后台的原有回收门槛，减少稳定播放期间重复的状态与时钟判断。
- 主循环复用当前帧已经取得的 `performance.now()` 和自适应目标 FPS，调度、帧率跳过判断和交互状态不再为同一帧重复计算。
- 保持播放器布局、视觉质感、播放、歌词、歌单交互、后台调度和更新语义不变；全量 Node 回归 `250/250` 通过。

## v1.3.8 主循环状态快照与时间戳复用
- 主循环复用同一帧时间戳到自由镜头、手势衰减、歌单架更新、Home 波形和空闲引导，减少重复高频时钟读取。
- 新增就地复用的歌单渲染帧快照，`getMode()`、`hasOpenContent()` 和 `shelfAlwaysVisible()` 在主渲染链路中各只读取一次，并复用到歌单架、壁纸压暗、Skull 相机、Skull 粒子和舞台歌词。
- 保持播放器布局、视觉质感、播放、歌词、歌单交互和更新语义不变；全量 Node 回归与语法检查通过。

## v1.3.7 主渲染帧时间戳与歌词歌单状态复用
- 主循环取得的 `performance.now()` 时间戳复用到歌单架 hover 提示、Home 空库波形和空闲引导，避免同一帧重复读取高频时钟。
- 舞台歌词每帧只读取一次歌单模式、详情打开状态和常驻显示状态，复用到歌词避让、壁纸安全布局和详情偏移判断。
- 保持播放器布局、视觉质感、播放、歌词、歌单交互和更新流程不变；新增时间戳与歌单状态快路径回归断言。

## v1.3.6 3D 歌单详情绘制快路径复用
- 详情页每次同步可见歌曲行时只读取一次歌单架外观设置，并将同一帧快照复用到面板、可见行和中心行重绘。
- 面板与详情行绘制复用当前帧强调色和背景透明度，减少滚动、加载动画和主题刷新期间重复归一化偏好与解析颜色。
- 保持详情布局、动画、播放、歌词、交互、UI 文案和视觉质感不变；新增帧快照复用回归断言，修正可见行热路径测试契约。

## v1.3.5 3D 歌单详情属性低写入优化
- 3D 歌单详情可见行的位置、缩放、旋转、可见性、渲染顺序和材质透明度只在目标值变化时写入，减少滚动、播放和稳定展示期间的 Three.js setter 与矩阵更新。
- 详情组的位置、缩放、普通欧拉姿态和面板透明度复用稳定值缓存；相机四元数姿态切回普通姿态时显式失效缓存，保留原有姿态与过渡语义。
- 新增歌单详情行、详情组属性写入回归测试；全量 Node 回归 `245/245` 通过，不改变播放器布局、视觉质感、播放、歌词、桌面覆盖层或用户设置。

## v1.3.4 3D 歌单架卡片属性低写入优化
- 3D 歌单架卡片的位置、缩放、旋转、相机姿态、可见性、渲染顺序、材质颜色和透明度只在目标值变化时写入，减少稳定播放和交互期间的 Three.js setter 与矩阵更新。
- 保留动画目标、侧栏/舞台布局、Skull 相机姿态、卡片透明度和命中顺序；从相机姿态切回普通旋转时保留原有 `rotation.z` 语义。
- 新增歌单架卡片属性缓存回归测试；不改变播放器布局、视觉质感、播放、歌词、桌面覆盖层或用户设置。

## v1.3.3 相机与交互热路径低分配优化
- 普通相机在位置、观察点和节拍滚转稳定时跳过重复的 `position.set()`、`lookAt()` 和滚转重建；自由镜头、Skull 覆盖相机切换时显式失效缓存。
- 歌单架 hover 检测复用固定指针对象，涟漪九宫格去重改用整数位掩码，减少播放和交互期间的短命对象与 GC 压力。
- 新增相机姿态和涟漪热路径回归测试；不改变播放器布局、视觉质感、播放、歌词、桌面覆盖层或用户设置。

## v1.3.2 主渲染器与壁纸覆盖层低占用优化
- 可见但未播放且没有交互时，主渲染器由 30 FPS RAF 改为按目标帧率定时唤醒，减少高刷新率屏幕上的无效 JS 回调；播放或交互开始时立即恢复 RAF。
- 壁纸覆盖层播放时保留 RAF，未播放时限制为 30 FPS，关闭时改为 1 秒低频调度。
- 壁纸关闭时立即释放封面图引用和粒子数组；状态切换、窗口可见性切换会取消旧句柄，避免 RAF 与定时器并存。
- 新增主渲染器调度和壁纸资源释放回归测试；不改变播放器布局、视觉质感、播放、歌词、桌面覆盖层或用户设置。

## v1.3.0 固定乱序播放队列
- 切换到随机播放时立即用 Fisher-Yates 将整个当前队列打乱一次，之后不再为每次“下一首”重新抽签。
- “上一首”和“下一首”统一沿这份固定乱序队列前后移动并循环，因此连续点多少次都能稳定往返。
- 洗牌时保持当前正在播放的歌曲不变，只更新它在新队列中的索引，不会突然切歌或重播。
- 随机模式下换入新歌单、恢复上次随机播放会话时，也会在首次播放前完成一次整队洗牌。
- 使用 `WeakSet` 标记已洗牌队列，不保留已被替换的旧队列；移除 v1.2.100 的播放历史数组，进一步简化内存所有权。
- 主界面、迷你播放器、桌面歌词、系统媒体键和快捷键继续共用同一套前后切歌逻辑，不改 UI、歌词或音质。

## v1.3.1 CPU 与运行内存优化
- 可见但未播放且没有交互时，主 3D 渲染降至 30 FPS；播放、交互、后台和帧压力状态保持原有调度语义。
- 3D 歌单架在启动页或完全隐藏时跳过卡片位置、旋转、透明度和详情更新，避免不可见内容继续占用主线程。
- 主实时频谱四段累加改为单次 TypedArray 扫描；独立节拍频谱按 30 FPS 刷新并复用五个频段标量，保持 kick、人声、中频、高频和 RMS 数值不变。
- 本地资产后台预载改用数字排序键，避免大曲库导入时为每首候选歌曲创建临时排序对象。
- IndexedDB 缓存清理改为串行游标扫描和逐条删除，不再同时把资产、歌词和曲库完整记录复制到多套数组。
- 歌词特效关闭、暂停镜头无残留冲击时直接跳过无效帧工作；歌词镜头边界计算移除每帧临时闭包。
- 安装器从原始 Windows 命令行读取 `/D=`，保留安装目录归一化和卸载安全门禁。
- 不改变播放器布局、视觉、播放、歌词、桌面覆盖层或用户设置；全量 Node 回归 `237/237` 通过。

## v1.2.100 随机播放上一首修复
- 修复随机播放时“上一首”不能返回刚才歌曲的问题：现在按真实播放历史后退，不再根据随机索引猜上一首。
- 从历史后退后再点“下一首”，会先沿原播放历史前进；走到历史末尾后才继续随机选曲。
- 随机下一首会排除当前歌曲，队列有多首歌曲时不会原地重复。
- 播放历史最多保留 96 条，只记录轻量 ID 和歌曲键，不强引用歌词、封面或完整歌曲对象；清空队列时同步释放。
- 主界面、迷你播放器、桌面歌词、系统媒体键和快捷键继续共用同一套上一首逻辑，不改 UI 和播放音质。

## v1.2.99 更新动画与下载轮询按状态运行
- 没有可用更新时，更新入口不再永久运行两条 GSAP 呼吸动画；窗口最小化、隐藏或进入后台时立即释放 tween，恢复前台且确有更新时才按需重建。
- 更新下载在面板打开时保持原有 320–360ms 进度刷新；关闭面板后自动降到 1.5 秒，窗口后台降到 5 秒，重新打开面板立即刷新，下载任务本身不暂停。
- 预览版进度定时器在面板关闭或窗口后台时显式取消并清空引用，避免不可见界面继续更新 DOM。
- 不改动播放器布局、视觉质感、播放、歌词、桌面覆盖层或用户设置。

## v1.2.98 空闲 Canvas 与提示音资源按需释放
- 空场背景功能关闭时，不再为隐藏的 `idle-guide-canvas` 申请全屏 2D backing store，也不再启动每 `140ms` 一次的永久空轮询。
- 视觉引导进入歌单架提示步骤时才按需激活 Canvas、resize 监听和 RAF；提示淡出后自动取消任务、清空粒子/轨迹并缩回 `1 × 1`。
- 歌单架选择提示音的独立 AudioContext 在最后一次播放后空闲 5 秒自动关闭，同时释放 6 个噪声 AudioBuffer；下一次交互按需重建。
- 不改动播放器布局、视觉质感、播放逻辑、歌词内容和用户设置。

## v1.2.97 CPU 与运行内存释放优化重发
- 启动页退出后立即释放全屏 Canvas/WebGL context、shader、buffer、粒子数组和 resize 监听，不再让隐藏启动页长期占用 GPU/内存或在窗口缩放时做无效重建。
- 启动音效结束后主动关闭独立 AudioContext，释放音频线程、节点图和临时噪声缓冲。
- 主窗口最小化、隐藏或不可见时彻底暂停主 3D RAF；恢复可见时无损续帧，播放、桌面歌词和壁纸的独立调度不受影响。
- Windows 发布继续只生成 NSIS 安装包、`.blockmap` 和 `latest.yml`，不再生成 Portable ZIP。
- 不改动播放器布局、视觉质感、播放逻辑、歌词内容和用户设置。

## v1.2.96 迷你播放器桌面歌词按钮细化
- 桌面歌词按钮改为右下角小号“词”字，移除复杂显示器图标和方框，不挤占播放控制栏空间。
- 开启时仅以青色文字和轻微文字光效提示，关闭时保持低调灰色。
- 标准和极简迷你播放器保持同一位置语义，继续复用桌面歌词开关与状态同步。

## v1.2.95 迷你播放器桌面歌词开关
- 标准迷你播放器和极简迷你播放器各增加一个桌面歌词显示/关闭按钮，不新增多余控制项。
- 按钮状态跟随桌面歌词实时同步，开启时高亮，关闭时恢复默认；悬停提示随状态切换。
- 点击后复用主界面桌面歌词开关和持久化链路，不影响播放控制、桌面歌词位置及其它设置。

## v1.2.94 桌面歌词调节控件重排
- 桌面歌词字号和光效调整改为两组独立设置胶囊，统一图标、标签、数值和加减键的视觉层级。
- 去掉散落的圆形/方块按钮，默认保持低调，悬停和按下时才显示轻量反馈。
- 保留字号、光效步进值、锁定逻辑、播放控制、`×` 关闭和位置持久化行为。

## v1.2.93 桌面歌词调节按钮视觉优化
- 重做桌面歌词字号和光效调节按钮，改为精致胶囊边框、独立加减图标和百分比徽章。
- 保留原有字号、光效步进值、锁定逻辑和控制栏布局，不改变调整功能。
- 统一悬停、按下和禁用状态的反馈，降低默认文本按钮的廉价感。

## v1.2.92 桌面歌词播放控制栏
- 桌面歌词工具栏新增上一首、播放/暂停、下一首按钮，统一转发主播放器命令，不维护重复播放状态。
- 播放按钮根据当前播放状态自动切换播放/暂停图标与无障碍标签。
- 桌面歌词关闭按钮改为 `×` 符号，保留原有关闭行为。
- 保留桌面歌词位置、锁定、字号、光效和窗口持久化修复。

## v1.2.91 桌面歌词手动位置持久化修复（重发）

- 修复桌面歌词拖到屏幕顶部之外时，真实窗口坐标（例如 `y=-171`）在保存和恢复阶段被强制夹成 `y=0`，导致重启后回到错误位置的问题。
- 对仍有可操作区域留在当前显示器内的手动位置保留完整坐标；窗口已经不可操作或显示器变化时仍会安全拉回屏幕。
- 保留调整纵向位置时清理旧 `desktopLyricsBounds` 的修复；新增真实负坐标保存、恢复和不可达位置回退测试，全量 Node 回归 `202/202` 通过。
- 不改变桌面歌词 UI、布局、字号、透明度、光效、播放时钟、鼠标穿透或交互入口。
- 本次按要求覆盖重发同版本 `1.2.91`；已经安装旧 `1.2.91` 的设备需要手动覆盖安装新版安装包。

## v1.2.90 更新介绍清洗与乱码防护

- 修复 GitHub Release 使用“修复内容 / 验证 / 下载”等 Markdown 章节时，更新卡片把章节标题误当摘要、验证或下载信息挤占更新条目的问题；摘要现在取首条真实更新内容。
- 更新介绍统一清理 Markdown 标题、链接、图片、重复项和 SHA 校验信息，并在“验证 / 下载 / 安装”等后续章节前停止提取。
- 增加 UTF-8 BOM 与常见“UTF-8 被按 Latin-1 解码”乱码恢复；无法可靠恢复的替换字符和控制字符不会再直接显示到更新面板。
- 自定义更新 manifest 的 `summary` / `notes` 与 GitHub Release 正文复用同一归一化链路；新增 4 个回归用例，当前全量 Node 回归 `200/200` 通过。
- 不改变更新面板布局、下载线路、补丁策略、安装器校验、UI、歌词、播放或视觉效果。

## v1.2.89 桌面歌词状态与位置持久化修复

- 修复桌面歌词已经开启时退出软件，主进程拆卸覆盖层却向 renderer 广播“已关闭”，导致重启后开启偏好被覆盖的问题；用户主动关闭仍会正常保存关闭状态。
- 修复桌面歌词拖动过程中遇到程序定位保护窗口时，最后一次坐标可能没有落盘的问题；拖动、窗口关闭和更新重启都会强制保存最终位置。
- 快速补丁触发立即重启前先同步保存视觉配置，主进程重启前再关闭覆盖层并落盘窗口坐标，避免直接 `app.exit()` 跳过常规卸载保存。
- 新增桌面歌词持久化回归测试；全量 Node 回归 `196/196` 通过，不改变歌词样式、播放时钟、鼠标穿透、锁定、字号、光效或 UI 布局。

## v1.2.88 Home 空库波形热路径优化

- Home 空库波形预先缓存 24 个频谱柱对应的频谱桶索引；播放分析频谱长度不变时，刷新波形不再每次重复执行幂运算和边界计算。
- 保持波形刷新间隔、频谱取样顺序、平滑参数、柱高、透明度、节拍驱动和 UI 视觉完全不变。
- 新增 Home 波形热路径回归测试；全量 Node 回归 `194/194` 通过。

## v1.2.87 桌面歌词 60 FPS 少分配优化

- 桌面歌词窗口按“原始文本 + 单/双行模式”缓存归一化结果；播放中相同歌词的高频 IPC 不再重复执行换行拆分、空行过滤和数组拼接。
- 画布光效绘字把 Unicode 字形数组与字宽一起写入缓存；同一句歌词后续描边/填充绘制不再每帧 `Array.from()` 重建字符数组，代理对字符显示语义保持不变。
- 新增桌面歌词热路径回归与全部发布页面内联脚本语法门禁；GitHub Actions 同时直接检查 `public/app.js`。
- 不改变歌词文本、字号、位置、颜色、光效、30/60 FPS、播放时钟、鼠标穿透或调整栏自动避让行为；全量 Node 回归 `193/193` 通过。

## v1.2.86 桌面歌词修复与发布链路完善

- 将 `v1.2.85` 的桌面歌词修复提升为新版本；已安装 `1.2.85` 的客户端可通过版本比较正常发现更新。
- 保留窗口被拖到物理屏幕顶部之外时的裁切量同步，歌词调整栏会自动移动到实际可见区域。
- 将当前完整源码推进 GitHub 默认 `main`，仓库首页、clone 源码与安装包版本恢复一致。
- 新增 GitHub Actions Windows CI：依赖安装、全量 Node 回归、主进程/预加载/服务端语法检查和 `git diff --check` 都会在 push、PR 时自动执行。
- 正式发布资产只保留安装器、blockmap、`latest.yml` 和 SHA256 清单；Portable ZIP 只保留本地产物，避免 Release 正文错误声称远端可下载。
- 发布前全量 Node 回归 `190/190` 通过，安装器、blockmap、`latest.yml` 和本地 Portable ZIP 均已生成并校验。
## 2026-08-09 NSIS 静默安装目录安全修复（待发布）

- 保留从原始 Windows 命令行读取 `/D=` 的修复，避免自定义安装逻辑覆盖用户显式指定的目录。
- 保留专用安装目录归一化、`.mineradio-install-root` 标记和卸载安全门禁；新增安装器命令行回归测试。

## v1.2.85 桌面歌词调整栏自动避让修复重发

- 修复歌词靠近屏幕顶部时，上方的锁定、字号和光效调整栏被屏幕上沿裁切的问题；上方空间不足会自动翻到歌词下方。
- 调整栏会跟随歌词舞台浮动和入场动画重新定位；上下空间都不足时选择屏幕上露出更多的一侧。
- 修复窗口被拖到物理屏幕顶部之外时 renderer 无法感知裁切量的问题；主进程通过轻量几何 IPC 传递上下裁切量，调整栏会稳定翻到实际可见区域内。
- 不改变歌词样式、窗口位置保存、鼠标穿透、拖动和播放时钟；全量 Node 回归 `189/189` 通过。

## v1.2.84 桌面覆盖层帧热路径优化

- 移除主渲染动画帧对桌面歌词/壁纸同步的重复调用；覆盖层继续由独立自调度计时器按歌词 FPS 和壁纸节奏同步，减少每帧无效判重、时间读取和状态分支。
- 不改桌面歌词显示时序、歌词位置、壁纸画面、UI、玻璃质感、播放控制和鼠标操作；后台/隐藏窗口降载逻辑保持不变。
- 新增桌面覆盖层调度器契约测试；全量 Node 回归 `183/183` 通过后发布。

## v1.2.83 更新状态轮询单飞优化

- 更新下载/快速补丁状态轮询由固定 `setInterval` 改为单飞请求：上一轮未返回时不再叠加下一轮请求，网络慢时减少请求堆积和无效 JSON/DOM 刷新。
- 增加轮询代际与任务 ID 校验，旧任务迟到响应不会覆盖新任务状态；完成、失败、切换任务时统一清理定时器和在途标记。
- 桌面歌词交互提示在歌词靠近窗口顶部、上方空间不足时自动移动到歌词下方；手动拖动后的窗口坐标在重启后优先恢复，关闭时补存一次。
- 修复桌面歌词外层同步调度固定 `320ms` 造成的歌词慢半拍：启用桌面歌词时改为跟随 `30/60 FPS` 推送节奏，保留后台压力和隐藏窗口降载保护，不改音频时钟、不影响正常电脑操作。
- 新增更新轮询、桌面歌词布局和同步频率回归测试；全量 Node 回归 `182/182` 通过。

## v1.2.82 静态资源流式传输优化

- 本地服务器对 HTML、CSS、JS、字体和图标的 `200` 响应改用 `fs.createReadStream()` 流式发送，避免先把整份 renderer 资源复制成 Buffer，降低启动和重复加载时的瞬时内存峰值。
- 保留 `ETag`、`Last-Modified`、`Cache-Control: no-cache` 和 `304` 条件请求语义；页面、UI、布局、玻璃质感、电影视觉、播放控制和交互语义不变。
- 新增静态资源流式响应回归测试；当前全量 Node 回归 `175/175` 通过。

## v1.2.81 本地静态资源条件缓存优化

- 本地服务器为 HTML、CSS、JS、字体和图标增加 ETag/Last-Modified 条件缓存；重复启动命中缓存时返回 `304`，不再重新传输大型 renderer 资源。
- 保持更新后的资源可及时生效，使用 `Cache-Control: no-cache` 校验而不是长时间强缓存；页面、UI、布局、玻璃质感、电影视觉、播放控制和交互语义不变。
- 新增真实 HTTP 200/304 回归测试，完整回归共 `174/174` 通过。

## v1.2.80 asar 资源归档与运行根拆分优化

- 收窄 `asarUnpack` 到可写的 `server.js` 和 `package.json`；`public`、`build` 和桌面脚本回到 `app.asar`，减少解包文件与启动 I/O。
- 主进程和本地服务器拆分“可写运行根”和“静态资源根”：补丁仍只写 `app.asar.unpacked`，静态页面、字体、图标从 `app.asar` 读取。
- 兼容 `v1.2.79` 已安装的旧解包布局；使用完整安装器升级后不要求手动迁移文件。
- 实际 Electron 隔离启动验证首页、CSS、JS、字体和图标资源均返回 `200`；UI、布局、玻璃质感、电影视觉、播放控制和交互语义不变。

## v1.2.79 全量启动与后台资源优化

- `asar` 开启为 `true`，将 `public`、`server.js`、`package.json` 和 `build` 放入 `app.asar.unpacked`；主进程、本地服务器和快速补丁统一使用可写运行根，保留原有更新链路。
- 自定义封面、歌词、歌词偏好、本地节奏图、听歌统计和视觉存档迁移到独立 IndexedDB；旧 `localStorage` 自动迁移，写入失败才回退，曲库快照/索引成功落盘后清理旧大 JSON。
- 桌面歌词与壁纸覆盖层恢复 Chromium 后台节流，隐藏时停止 RAF，重新可见后继续；移除全局禁用后台节流开关，主窗口后台保活策略不变。
- 新增 Noto Sans SC UI 中文子集并修正 Inter 为真实 WOFF2；动态歌词未命中的字符继续使用 Windows 本地字体回退，无远程字体请求。
- 新增全量优化门禁测试；UI、布局、玻璃质感、电影视觉、播放控制和交互语义保持不变。

## v1.2.78 启动资源本地化与渲染器缓存优化

- 主渲染页面改为加载可缓存的 `app.css`、`app.js`，HTML 首屏入口从约 `1.6MB` 降到约 `55KB`，减少冷启动时重复解析内联 CSS/JS 的开销。
- 移除启动时的 Google Fonts 网络依赖，新增本地 Inter 字体资源；中文字体保留 Windows 本地回退，动态歌曲名和歌词不做危险的固定字集裁剪。
- 逐字歌词进度复用播放帧游标，连续播放不再每帧从当前歌词行第一个字重新扫描；跳播会自动重置游标，歌词高亮数值和视觉表现保持不变。
- 移除未使用的 `gsap`、`mpg123-decoder` npm 依赖及锁文件记录；保留 `public/vendor/gsap.min.js`，不改变现有动画库运行路径。
- 保持 `asar:false`，因为现有快速补丁需要通过 `server.js` 的 `__dirname` 原地替换资源；UI、布局、玻璃质感、播放控制和 3D 歌单架交互语义不变。
- 新增本地 renderer 资源、字体资源和逐字歌词游标回归测试。

## v1.2.77 3D 歌单详情屏幕命中低分配优化

- 详情歌单屏幕点击兜底改为单次扫描可见行，保持 `renderOrder` 命中优先级，移除每次点击的 `filter().sort()` 临时数组。
- 详情行屏幕投影复用四个固定向量和 bounds scratch，减少连续点击、按钮命中和屏幕拾取路径的短命对象与 GC 压力。
- 保持详情列表布局、命中范围、按钮区域、播放/收藏/下一首动作、UI、玻璃质感和 3D 歌单架交互语义不变；新增屏幕命中热路径回归测试。

## v1.2.76 3D 歌单架卡片绘制低卡顿优化

- 歌单架每帧复用同一份外观快照，卡片绘制签名、DOF 变化、中心状态变化和周期重绘不再重复读取歌单架设置与强调色。
- 保持卡片颜色、透明度、布局、UI、视觉质感和交互语义不变；新增卡片绘制帧快照复用回归测试。
- 全量 Node 回归 `166/166`、59 个 JavaScript 文件语法检查、前端内联脚本解析和 `git diff --check` 均通过。

## v1.2.75 音频分析热循环低卡顿优化

- 频谱分析四个频段先累加 8 位采样值，再统一执行归一化，减少播放分析帧中的重复除法。
- 时域 RMS 使用 256 项归一化平方查找表，避免每个分析帧对约 2048 个采样重复执行减法、除法和乘法。
- 保持频段边界、RMS、节拍判断、视觉响应、UI、布局、播放控制和交互语义不变；新增旧算法数值等价回归测试。

## v1.2.74 3D 歌单详情帧快照复用低卡顿优化

- 3D 歌单架把当前帧已经计算的布局与外观设置快照传给歌单详情列表，详情页每帧不再重复归一化设置和解析颜色。
- 可见歌曲行定位、详情面板透明度和布局更新复用同一帧快照；保持歌单详情的布局、透明度、滚动、交互、UI 和视觉质感不变。
- 新增歌单详情帧快照复用回归测试；构建前执行全量 Node 回归、前端内联脚本解析、语法检查和 `git diff --check`。

## v1.2.73 3D 歌单架帧设置快照复用低卡顿优化

- 歌单架更新帧先计算一次 `shelfSettings()`，再把同一份设置快照传给 `shelfLayoutProfile()`，避免每帧重复执行偏好归一化和颜色解析。
- 保持歌单架布局、卡片位置、透明度、层级、详情页、UI、视觉质感和交互语义不变；新增帧级设置快照回归测试。

## v1.2.72 相机投影矩阵低卡顿优化

- 普通镜头和自由镜头只在 `fov`、`aspect`、`near`、`far`、`zoom` 或胶片投影参数变化时重建投影矩阵，稳定播放帧跳过重复矩阵计算。
- 保持镜头位置、节拍推拉、自由镜头、窗口尺寸变化和视觉输出语义不变；新增相机投影热路径回归测试。

## v1.2.71 实时节拍分析低卡顿优化

- 实时节拍分析复用主频谱分析已经计算出的时域 RMS，避免同一播放帧再次扫描 `2048` 个时域采样点；直接调用节拍引擎时仍保留原有回退路径。
- 缓存实时节拍频段的采样桶边界，避免连续播放期间重复执行相同的 Hz 到 FFT 桶换算；频段数值、节拍判断和视觉响应保持不变。
- 空 Home 波形先执行已有时间节流，再查询 DOM，减少未到刷新间隔时的重复节点查找；不改变 Home 波形显示和播放交互。
- 新增实时音频分析热路径回归测试；全量 Node 回归测试通过后再构建发布。

## v1.2.70 队列可见行快照复用低卡顿优化

- 队列面板和底部迷你队列为同一轮可见歌曲生成一次行快照，复用歌曲键、副标题、封面签名、封面地址和喜欢状态，减少连续元数据/封面刷新时的主线程重复计算。
- 主队列生成的可见行快照传给迷你队列；保持队列排序、当前歌曲高亮、封面、文案、按钮、懒加载和交互语义不变。
- 新增队列热路径回归测试；旧版同输入 72 行渲染中，副标题读取由 144 次降至 72 次，喜欢状态由 216 次降至 72 次。

## v1.2.69 Home 首屏快照复用低分配优化

- Home 刷新时复用同一轮已经计算的本地歌曲池和听歌统计摘要，避免首屏卡片渲染再次扫描大曲库与长期统计记录，降低导入后恢复和播放统计更新时的主线程开销。
- 保持 Home 卡片顺序、标题、封面、文案、布局和交互语义不变；新增快照复用帧热点回归测试。

## v1.2.68 3D 歌单架卡片帧状态复用优化

- 3D 歌单架卡片布局直接复用更新帧已计算的内容打开状态和常驻显示状态，避免每张可见卡片重复查询，降低持续播放、视差移动和详情切换时的主线程开销。
- 保持卡片位置、层级、透明度、封面绑定、详情页遮罩和交互语义不变；补充帧级热点契约测试。

## v1.2.67 舞台歌词与歌单架帧更新低开销优化

- 3D 舞台歌词光粒循环复用当前帧的启用状态、节拍强度、漂移系数和粒子数量，减少大批量光粒更新时的重复属性读取与分支判断；粒子坐标、旋转、透明度和可见性语义保持不变。
- 3D 歌单架每帧缓存内容打开状态和常驻显示状态，避免可见性、层级和封面绑定路径重复查询，降低持续播放与鼠标视差更新时的主线程开销。
- 新增歌词光粒与歌单架帧热点契约测试；不改变 UI、布局、文案、玻璃质感、电影视觉、播放控制或 3D 歌单架交互。

## v1.2.66 歌单详情可见行渲染低分配优化

- 3D 歌单详情页复用可见窗口时，将歌曲引用同步和条件重绘合并为单次索引循环，减少连续滚动、加载动画和曲库刷新时的回调遍历与短命闭包分配。
- 保持可见歌曲窗口、中心行判定、加载动画刷新、绘制顺序和 UI/交互语义不变；新增可见行热路径回归测试。

## v1.2.65 本地搜索预热与空查询缓存优化

- 本地搜索候选池筛选结果直接复用给索引预热任务，避免大曲库每次刷新时重复遍历同一批歌曲。
- 空搜索结果按曲库签名缓存，重复打开搜索区或恢复本地库时不再反复复制最多 80 个歌曲引用；搜索排序、数量上限和结果 DOM 保持不变。
- 新增本地搜索预热数组复用、混合队列结果保持和空查询结果身份复用回归测试。

## v1.2.64 本地资产缓存与封面流式读取优化

- IndexedDB 升级到 v3，新增独立 `lyrics` store；旧 `assets` 记录按 cursor 逐条迁移，歌词原文不再和封面、元数据一起驻留在资产记录中。
- 歌词缓存的读取、写入、释放后合并和清理全部切换到独立记录，保留 `complete`、`error`、`abort` 三路事务结算，减少大曲库缓存补水与清理时的内存峰值。
- 桌面曲库外置封面优先使用已授权的本地流 URL，缩略图和当前封面直接从 URL 解码，不再把整张图片读成 renderer base64；普通浏览器拖放文件仍保留 data URL 回退。
- 保留现有 UI、布局、玻璃视觉和播放交互；新增 5 条回归用例，全量测试 `150/150` 通过。

## v1.2.63 桌面歌词渲染热路径性能优化

- 桌面歌词光效文字改为按字形宽度缓存，播放中同一句歌词不再每帧重复调用 `measureText`；文本、字号或字体变化时自动重建，视觉输出与之前一致。
- 光效四层模糊参数提为常量，每帧不再重建层数组；固定歌词位置 + 暂停时，stage 的 transform/filter、进度百分比、扫光位移等无变化值不再重复写入样式，减少每帧无效样式计算与布局读取。
- 桌面歌词每帧只在实际布局、位移或滚动变化时重新测量歌词矩形；固定模式下暂停时零额外测量，播放与滚动热区判定逻辑不变。
- 新增/更新桌面歌词光效契约回归断言；全量 `node --test` 145/145、内联脚本语法、真实 Chromium 启用态画布渲染和 `git diff --check` 均通过。

## v1.2.62 桌面歌词光效增强与强度调节

- 提亮桌面歌词随播放进度推进的流动光效，扩大扫光范围并增强进度边缘 bloom、文字 glow 与阴影亮度，播放时更清晰易辨。
- 桌面歌词工具条新增光效强度 `− / 百分比 / +` 控制，支持 `0–85%`、每档 `5%` 调整；设置会持久化，并与 DIY 面板的「歌词溢光」滑块同步。
- 保留固定歌词位置、字号调节和歌词热区随字号变化等既有体验；本轮不改歌词布局和玻璃视觉基线。

## v1.2.61 桌面歌词固定位置与流动光效增强

- 修复桌面歌词光效绘制函数未接入每帧循环的问题，播放时现在会随歌词进度流动。
- 增加桌面歌词文字 glow、进度边缘 bloom 和 canvas 叠层，并提高光效亮度，播放时更容易看清歌词。
- 悬浮工具条新增「固定/浮动」按钮；固定状态会关闭上下漂浮并持久化，下次打开桌面歌词继续使用。

## Unreleased

- 桌面歌词新增「固定不跳动」选项（关闭切行弹入和上下漂浮，垂直位置保持固定）
- 接通主软件歌词光效流动（canvas 轻量 aura + progress 高亮 bloom + 粒子，已默认高亮跟随开启并改善边缘流动效果）
## v1.4.9 迷你播放器与更新检测
- 标准迷你播放器本体移除点击后会消失的 `×`；四角“返回主界面”按钮恢复到右上角原位，桌面歌词和播放控制保持不变。
- DIY“迷你播放器样式”区域新增永久可见的 `× 自动收回` 双向开关：开启时离开完整面板后收回封面，关闭时始终保持完整迷你播放器。
- 歌单删除确认改为播放器内暗色玻璃弹层，明确显示歌单名、歌曲数量和“不会删除本地音乐文件”，支持取消、确认、点击遮罩和 `Esc` 关闭。
- 顶栏更新入口改为常驻“检测更新”按钮，更新面板内新增独立检测按钮；提供检测中动画、重复点击单飞、最新版、新版本和网络失败反馈。
- 当前已是最新版或检测失败时禁用无效的下载入口，避免无更新状态误触发预览下载；检测新版本后仍沿用现有完整安装包与轻量补丁链路。
- 升级继续使用 `%APPDATA%\\Mineradio-oirge` 和既有存储键，不清空音乐文件夹、播放会话、特别喜欢、自建歌单或 DIY 设置。
- 全量 Node 回归 `325/325`，关键 JavaScript 语法检查与 `git diff --check` 通过。
