## v1.6.0 修复迷你播放器封面律动与光晕无反应
- 发布版本从 `1.5.9` 提升为 `1.6.0`，已安装 `1.5.9` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 修复标准迷你播放器在主窗口隐藏播放时，低平滑节拍分析器返回空频谱帧，把主分析器的有效音频覆盖为零，导致封面律动和封面光晕完全没有反应的问题。
- 低平滑分析器有有效低频数据时仍优先用于律动；切换后台或 `AudioContext` 恢复瞬间出现空帧时，自动回退到已接入输出链路的主分析器，不改变正常播放链路。
- 修复页面 `document.hidden` 先于桌面窗口状态 IPC 到达时，迷你播放器采样定时器没有启动的问题；`visibilitychange` 会立即同步定时器，隐藏播放仍持续低开销采样。
- 保留 `1.5.8` 已发布的封面律动/光晕独立开关与 `0.00 ~ 3.00` 强度设置；本次不启动新窗口、不接管全局鼠标键盘，不影响电脑上的其它操作。

## v1.5.9 修复本机代理线路无法取消更新
- 发布版本从 `1.5.8` 提升为 `1.5.9`，已安装 `1.5.8` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 修复 `本机代理` 线路点击“取消更新”后任务仍在后台继续下载的问题：进度条会一直往前走、`已接收` 字节继续增长，最终整个安装包被拉完，取消实际无效。`直连 GitHub`、`国内加速` 与自动测速线路不受此问题影响。
- 根因是代理线路的响应体由 `Readable.toWeb()` 包装，不识别 `fetch` 的中止信号，而代理请求在响应头到达时就已经摘掉了中止监听，导致连接无人可以打断。
- 现在安装包与补丁的下载循环逐块检查取消状态，取消与具体传输实现无关；同时代理响应会跟随取消销毁响应流和 `CONNECT` 隧道 socket，连接立即释放，不再把剩余流量白读完。
- 测速阶段同样会在取消后立刻停止读取剩余样本。取消依然是正常终态：状态收敛为 `已取消`，不写失败线路、不换线、不产生错误提示，可以立即改线路重新下载。
- 全量 Node 回归 `403/403`，新增代理响应跟随取消、预先取消立即释放与下载循环逐块检查三项断言，关键 JavaScript 语法检查与 `git diff --check` 均通过。

## v1.5.8 更新线路手动选择、取消更新与迷你封面律动光晕强度
- 发布版本从 `1.5.7` 提升为 `1.5.8`，已安装 `1.5.7` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 软件内更新面板新增线路选择，四档可选：`自动测速`（保持原有最快线路行为）、`直连 GitHub`、`国内加速`（只走配置镜像）、`本机代理`（走系统代理）。选择立即落盘到 `mineradio-update-route-v1`，重启后仍然生效。
- 线路选择会真正限制候选来源：非自动档只保留对应线路的下载候选，选中的线路没有可用候选时直接给出明确提示，不再静默回退到别的线路。
- 面板会显示当前线路可用性：国内加速显示可用镜像数量，本机代理显示检测到的系统代理地址；没有检测到代理时提示先在系统里设置代理。
- `本机代理` 使用手写的 `CONNECT` 隧道下载安装包和补丁，不引入任何第三方依赖；代理地址来自 Electron 的系统代理解析结果，也可由请求显式指定。
- 下载过程中新增“取消更新”按钮，可随时中断正在排队或下载中的任务，取消是正常终态而不是错误；已经进入安装/补丁应用阶段的任务不允许取消，避免留下混合版本。
- 迷你播放器封面律动和封面光晕改为可调强度：`DIY -> 迷你播放器 -> 封面效果` 下新增“封面律动强度”和“封面光晕强度”两个滑块，范围 `0.00 ~ 3.00`，`1.00` 是标准力度，往右调越明显，调到 `0` 或关掉对应开关即停用。
- 标准力度下的可见幅度比旧版明显加强：典型能量下封面缩放幅度约为旧版的 `2.5` 倍，外沿光晕透明度从 `0.14` 提高到约 `0.29`，解决“律动和光晕看不出来”的问题。
- 封面光晕不再依附封面律动开关：关掉律动后光晕仍会跟随音乐呼吸，反之亦然；关闭开关时保留用户已选强度，重新打开会恢复原来的档位。
- 强度换算使用饱和曲线，即使拉到最大 `3.00` 也不会把放大后的封面和光晕顶出 `360 × 84` 的透明窗口，实测最坏边距仍有 `1.7px`，不会出现光晕被裁掉半圈的错位。
- 全量 Node 回归 `400/400`，新增更新线路过滤、代理线路解析、取消终态与线路提示测试，扩展迷你播放器律动/光晕强度归一化、滑块持久化与几何上限断言，关键 JavaScript 语法检查与 `git diff --check` 均通过。

## v1.5.7 自动播放开关与迷你播放器悬浮展开错位修复
- 发布版本从 `1.5.6` 提升为 `1.5.7`，已安装 `1.5.6` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 新增“自动播放”开关，入口固定为 `DIY -> 高级 -> 自动播放`：`关闭` / `继续播放` / `随机播放` 三档，选择后立即落盘，下次启动自动生效。
- `继续播放` 在启动载入本地曲库后自动接着上次的歌曲和进度出声；`随机播放` 会切到随机模式、对队列固定洗牌一次，再从中随机挑一首从头开始。
- 自动播放使用底部控制栏选中的“播放歌单”，设置区直接显示当前歌单名称，并提供“选择”按钮打开同一个歌单选择器，可自定义自动播放的歌单范围。
- 启动恢复只触发一次自动播放，后台增量扫描不会重复起播；已经在出声、队列为空或开关关闭时都不打断当前播放，关闭态继续只恢复上次播放位置并保留恢复点。
- 修复标准迷你播放器悬浮展开/收回过程中桌面歌词 `词` 按钮飞到面板中间并被裁切的问题：该按钮是绝对定位元素，收回态 `.transport` 上的 `transform` 会把 `.transport` 变成它的包含块，现改为挂在 `.mini-shell` 直属层并单独淡出。
- 完整面板向左展开时，收回态位移动画一并镜像为 `translateX(-10px)`，面板始终朝远离封面的一侧滑出，不再出现反向挤压。
- 全量 Node 回归 `385/385`，新增自动播放归一化、启动继续/随机起播、单次触发门、播放歌单联动与设置区回归测试，以及桌面歌词按钮层级与收回态淡出回归测试，关键 JavaScript 语法检查与 `git diff --check` 均通过。

## v1.5.6 标准迷你播放器收回态穿透与桌面歌词按钮镜像修复
- 发布版本从 `1.5.5` 提升为 `1.5.6`，已安装 `1.5.5` 及更早版本的客户端可通过现有更新检查自动发现并安装本版本。
- 修复标准迷你播放器收回后，原完整面板位置仍然吞掉鼠标的问题：收回态窗口把鼠标事件交还桌面，那块空白区域的点击、双击和拖动直接落到底层的桌面或其它软件上。
- 收回态只有歌曲封面参与命中：指针回到封面热区会立即恢复窗口交互并展开完整面板，短按展开、封面拖动移动窗口、键盘聚焦展开和自动收回都保持原有手感。
- 封面拖动期间和关闭“自动收回”后强制保持窗口交互，拖动中窗口跟随指针不会因为坐标瞬时失配而丢失鼠标。
- 修复完整面板向左展开时，桌面歌词 `词` 按钮没有跟着镜像的问题：向左展开时按钮移动到左下角，与返回按钮的镜像方式一致。
- 全量 Node 回归 `372/372`，新增收回态鼠标穿透、封面热区恢复、穿透 IPC sender 门禁与生命周期重置、桌面歌词按钮镜像测试，关键 JavaScript 语法检查与 `git diff --check` 均通过。

## v1.5.5 迷你播放器封面律动、贴边展开与拖动修复
- 发布版本从 `1.5.4` 提升为 `1.5.5`，已安装 `1.5.4` 的客户端可通过现有更新检查自动发现并安装本版本，不再属于同版本重发。
- 保留并正式发布标准迷你播放器封面律动修复：主窗口隐藏且音频分析上下文挂起时会恢复分析，优先使用低平滑的节拍频谱，并用短期能量基线突出峰谷，避免不同歌曲长期饱和为固定大小。
- 标准迷你播放器按当前显示器工作区的左右余量决定展开方向：靠近右边缘收回时完整面板向左展开，靠近左边缘时向右展开，收回态封面继续贴近对应边缘。
- 点击歌曲封面仍可展开迷你播放器；按住并移动超过约 `5px` 后改为拖动窗口，主进程只接受当前迷你窗口的受信 IPC，按工作区夹紧并持久化坐标，不接管全局鼠标或键盘，不影响其它电脑操作。
- 全量 Node 回归 `368/368`，迷你播放器方向、封面拖动、脉冲恢复和 IPC sender 门禁测试，以及版本一致性、发布工作流清单、关键 JavaScript 语法检查与 `git diff --check` 均通过。

## v1.5.4 迷你播放器与 Wallpaper Engine 生命周期修复重发
- 本次保持版本号 `1.5.4`，在保留 Wallpaper Engine 生命周期与窗口重建修复的基础上，修复标准迷你播放器封面律动、贴边展开方向和封面拖动交互。
- 修复标准迷你播放器封面律动肉眼几乎不可见的问题：主窗口隐藏且音频分析上下文挂起时会恢复分析，优先使用低平滑的节拍频谱，并用短期能量基线突出峰谷，避免不同歌曲长期饱和为固定大小。
- 标准迷你播放器按当前显示器工作区的左右余量决定展开方向：靠近右边缘收回时完整面板向左展开，靠近左边缘时向右展开，收回态封面继续贴近对应边缘。
- 点击歌曲封面仍可展开迷你播放器；按住并移动超过约 `5px` 后改为拖动窗口，主进程只接受当前迷你窗口的受信 IPC，按工作区夹紧并持久化坐标，不接管全局鼠标或键盘，不影响其它电脑操作。
- 全量 Node 回归 `366/366`，迷你播放器方向、封面拖动、脉冲恢复和 IPC sender 门禁测试，以及关键 JavaScript 语法检查与 `git diff --check` 均通过。
- 本次为同版本修复重发；版本比较不会把新版 `1.5.4` 推送给已安装旧版 `1.5.4` 的用户，需要手动下载安装器覆盖安装。
- 修复主窗口重建后 Wallpaper Engine 窗口事件未重新挂载的问题；每次主窗口创建成功都会重新接入最小化、隐藏、恢复、移动、缩放、全屏和关闭处理。
- renderer 崩溃、主 frame 导航、主 frame 加载失败和窗口关闭时，统一停止当前原生 Scene，并清理 DWM helper、指针中继、静音重申定时器和捕获授权。
- pagehide 仅对离开前持有的原生 session 发送最佳努力停止请求；正常刷新会等待旧 session 清理，避免误停新 session 或重复启动 Scene。
- 增加旧窗口迟到事件隔离、并发清理 single-flight、窗口重建 hook、renderer 崩溃、导航、刷新失败和关闭生命周期回归测试；合并迷你播放器修复后全量 Node 回归 `366/366` 通过。

## v1.5.3 桌面歌词拖动原生竞态修复
- 修复桌面歌词解锁并覆盖主窗口标题栏时，拖动主窗口会在两个位置之间反复瞬移的问题；已在实际运行的 `D:\Mineradio\Mineradio.exe` v1.5.2 上复现，原生坐标采样确认主窗口相对鼠标单帧突变最高约 `410px`。
- Windows 主窗口进入原生移动循环时立即关闭桌面歌词窗口的鼠标转发，退出移动循环后再延迟恢复；拖动期间歌词 renderer 无法重新申请指针捕获，避免置顶透明歌词层与系统窗口拖动争抢鼠标。
- `will-move`、`move` 与 `WM_ENTERSIZEMOVE / WM_EXITSIZEMOVE` 形成多层兜底；用户拖动路径和松手后均不再执行主窗口 `setBounds()`，显示器插拔与参数变化仍保留安全纠偏。
- 不改变桌面歌词视觉、位置保存、中键锁定、歌词自身拖动、播放控制、主界面布局和用户数据；新增原生移动消息、鼠标转发关闭、非法指针重捕获和拖动路径无边界重设回归覆盖，全量 Node 回归 `353/353` 通过。

## v1.5.2 本地文件授权与窗口拖动修复
- 加固主窗口导航与 IPC 信任边界：主窗口只能停留在本地 127.0.0.1 服务的根路径，will-navigate 与 will-frame-navigate 阻止外部页面和非可信 frame；所有高权限 IPC 通道（文件扫描、文件读取、设置修改、启动项、热键、更新安装、重启等）统一校验可信主 frame，非法 sender 一律拒绝。
- 本地曲库授权改由主进程统一维护，任意 renderer 路径不得自行扩大本地文件读取范围；已导入曲库重启后仍可正常恢复。
- 修复开启桌面歌词后拖动主播放器松手瞬间被工作区边界拉回的跳位问题：拖动结束只保留释放位置，贴边、跨屏、任务栏遮挡不再触发 setBounds；显示器插拔与参数变化仍执行全量纠偏。
- 延续 %APPDATA%\Mineradio-oirge 用户目录和既有曲库、播放会话、特别喜欢、自建歌单与 DIY 设置，覆盖升级不要求重新导入。
- 新增导航拦截、非法 sender、目录扩权、文件读取边界、重启恢复授权与窗口拖动宽容纠偏回归测试；全量 Node 回归 345/345、关键 JavaScript 语法检查与 git diff --check 通过。

## v1.5.1 软件内更新自动选择最快线路
- 点击安装快速补丁或完整安装包后，服务端会并行探测全部候选线路，并按当前网络下的实测吞吐量自动选择最快线路，不再固定从第一条镜像开始下载。
- 每条线路最多读取 `128 KiB`，统一测速窗口为 `4 秒`；窗口结束前收到的部分样本仍参与排序，完全无响应的线路保留在队尾作为失败切换兜底。
- 快速补丁与完整安装包复用同一选线逻辑；镜像摘要门禁、SHA-256/SHA-512 校验、正式下载空闲超时和失败换线保持不变。
- 更新面板沿用原布局，测速时显示“正在测速更新线路”，选定后继续显示线路、实时速度、已下载大小和剩余时间。
- 全量 Node 回归 `341/341`、关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.5.0 桌面歌词拖动与迷你封面动效修复
- 修复开启桌面歌词后拖动主播放器经过歌词热区时，置顶歌词窗口抢占 Windows 原生鼠标捕获并导致主窗口跳位的问题；主窗口拖动期间强制歌词层穿透，松开后延迟恢复交互，歌词自身拖动保持可用。
- 修复桌面歌词工具栏“下一首”SVG 路径越界、显示成倾斜残缺图形的问题，恢复与“上一首”对称的标准图标。
- 修复标准迷你播放器封面律动肉眼几乎不可见的问题：降低脉冲增量同步阈值，增强低频信号映射，并提高收起封面态与完整面板态的平滑缩放幅度。
- 修复迷你播放器封面光晕被封面裁切的问题；光晕现在同时作用于封面外沿与内部描边，并继续跟随音乐低频强弱变化。
- 修复“封面律动强度”设为 `0` 时仍回退到默认强度的问题；关闭律动或强度归零后封面保持静止，极简迷你播放器继续保持无封面结构。
- 延续 `%APPDATA%\Mineradio-oirge` 用户目录和既有曲库、播放会话、特别喜欢、自建歌单与 DIY 设置，覆盖升级不要求重新导入。
- 全量 Node 回归 `334/334`、关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.4.11 更新圆环与窗口拖动修复
- 同版本修复重发：彻底移除检测更新圆环的 SVG 几何旋转，改用 `stroke-dashoffset` 移动描边；进度环使用固定的 SVG 坐标旋转，避免 Electron 下圆环再次偏心或偏移。
- 修复标题栏“检测更新”圆环在桌面壳中缩放到 `18px` 后仍使用固定 `12px` 旋转轴，导致圆环偏心、转到按钮外侧的问题；检测圆环与下载进度圆环现在都围绕 SVG 正中心旋转。
- 修复拖动主播放器窗口时偶发突然跳位：连续 `move` 事件只同步窗口状态，不再调用可能执行 `setBounds()` 的边界纠偏。
- 用户松开鼠标触发 `moved` 后才单次检查当前显示器工作区；尺寸过大时按副屏工作区缩放，位置越界时夹回可见区域且不强制居中，不干扰 Windows 原生拖动过程。
- 延续 `%APPDATA%\\Mineradio-oirge` 用户目录和既有曲库、播放会话、特别喜欢、自建歌单与 DIY 设置，覆盖升级不要求重新导入。
- 全量 Node 回归 `331/331`、关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.4.10 界面布局与 3D 歌单详情修复
- DIY 控制台新增独立“迷你”页，集中管理迷你播放器总开关、标准/极简样式、封面律动、光晕、悬停展开、圆角和 `× 自动收回`，常用入口不再藏在其它设置中。
- 修复顶栏“检测更新”按钮上偏、DIY 展开后底部操作按钮重叠，以及全屏状态下 Home、DIY、退出全屏按钮中心线错位的问题。
- 修复 3D 歌单详情布局参数传递错误导致坐标和缩放出现 `NaN`、点击详情后面板拉伸错位的问题。
- 打开普通歌单详情时立即隐藏舞台歌词，避免动态镜头下巨型歌词横穿详情面板；关闭详情后按原状态恢复。
- 延续 `%APPDATA%\\Mineradio-oirge` 用户目录和既有曲库、播放会话、特别喜欢、自建歌单与 DIY 设置，覆盖升级不要求重新导入。
- 全量 Node 回归 `328/328`、关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.4.8 歌单定向收藏与全屏过渡
- 当前播放队列、主曲库、歌手详情和自建歌单详情统一支持“收藏到任意歌单”；同一首歌可以同时加入多个自建歌单，不再只能收藏到特别喜欢或单一目标。
- 收藏弹窗明确显示“收藏到「歌单名」”与“已收藏到「歌单名」”，新建歌单后可直接完成本次收藏，避免收藏目标不清楚。
- 歌单选择弹层改为独立深色玻璃面板，强化标题、歌曲数量、当前态和按钮对比度，修复粒子背景穿透导致文字看不清的问题。
- 选择播放来源或收藏目标后，左侧歌单面板恢复自动隐藏状态，不再被永久展开状态锁住。
- 全屏进入与退出增加短暂暗场、轻缩放和回亮过渡，并在窗口尺寸或全屏状态稳定后释放遮罩，改善透明无边框窗口切换时的生硬跳变。
- 支持系统“减少动态效果”偏好；保留透明窗口全屏边界保护、退出按钮、DIY 布局和现有用户数据目录，不改变音乐文件、歌单与设置的升级存储。
- 全量 Node 回归 `323/323`，关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.4.7 多歌单与播放来源选择器
- 本地歌单升级为真正独立的多歌单系统：支持自定义创建、改名、删除、添加歌曲和移除歌曲，编辑歌单不会篡改当前播放队列。
- 收藏弹窗、左侧歌单页和 3D 歌单架统一接入自建歌单；歌曲引用按本地键和路径去重保存，暂时找不到的歌曲不会被误删，重新导入原文件夹后可自动恢复。
- 主播放栏原“普通 / 喜欢”二选一按钮改为歌单选择器，可直接选择全部音乐、特别喜欢或任意自建歌单，并显示歌曲数量、待重新定位数量和当前来源。
- 切换播放来源时优先保留当前歌曲与播放进度；当前歌曲不在目标歌单时从目标歌单第一首开始，空歌单只提示而不会清空当前队列。
- 标准迷你播放器右上角 `×` 改为“关闭自动收回”：点击后关闭封面悬停展开并保持完整控制栏，不关闭迷你播放器；设置会持久化并同步回主界面 DIY 开关。
- 延续稳定的 `%APPDATA%\\Mineradio-oirge` 用户目录与现有存储键，覆盖升级不会要求重新导入原音乐文件夹、特别喜欢、播放会话或已有设置。
- 浏览器真实交互验证通过全部音乐、特别喜欢和自建歌单切换、队列重建与来源持久化；全量 Node 回归 `317/317`，关键 JavaScript 语法检查与 `git diff --check` 通过。

## v1.4.6 全屏、更新安装与迷你播放器修复重发
- 修复 Windows 透明无边框主窗口开启全屏后又被工作区边界校正缩回的问题；全屏现在覆盖完整显示器区域和任务栏。
- 主进程不再只依赖 Electron 的 `BrowserWindow.isFullScreen()`，同时使用应用维护的窗口全屏和 HTML 全屏状态，覆盖 Electron 43 在透明窗口上原生查询仍返回 `false` 的情况。
- 修复逻辑全屏状态下退出按钮和 `Esc` 无法可靠恢复普通窗口的问题；HTML 全屏的 `Esc` 继续交给 Chromium 清理 DOM 全屏状态。
- 应用内启动更新安装器后主动退出后台播放器；NSIS 覆盖安装保留注册表恢复的原安装目录，并只关闭该目录、当前 Windows 会话内的 `Mineradio.exe`，避免旧进程占用文件、迁移到默认目录或误关其它安装实例。
- 标准迷你播放器完整控制栏右上角新增 `×`，用于立即收起悬停或键盘聚焦展开的控制栏；关闭“封面悬停展开”并始终显示完整面板时隐藏该按钮，极简模式保持不变。
- 新增透明窗口全屏、HTML 全屏、普通窗口越界纠偏和退出恢复回归测试；Windows 实机验证窗口可从 `1376x774` 进入 `1920x1080` 并恢复原尺寸。
- 全量 Node 回归 `312/312`、关键 JavaScript 语法检查与 `git diff --check` 通过；标准迷你播放器在 `360×84` 实际 Electron 窗口中完成按钮布局、点击折叠和永久完整模式隐藏验证。
- 本次为同版本修复重发；已安装旧版 `1.4.6` 的用户需要手动下载新版安装器覆盖安装。

## v1.4.5 综合修复重发
- 迷你播放器改为可选功能：视觉控制台和系统托盘均可开关；关闭后最小化、关闭到托盘和后台隐藏都不会创建迷你窗口，并立即释放状态、封面和 renderer 资源。
- 标准迷你播放器默认收起为圆角封面，鼠标悬停或键盘聚焦时展开完整控制栏；“封面悬停展开”可关闭并保持完整面板，标准/极简样式、律动、光晕和圆角 DIY 设置继续保留。
- 全屏模式把 DIY 与“退出全屏”固定到 Home 房子按钮左侧，退出入口始终可见，DIY 仍按悬停显现；修复与导入歌曲、账号区域重叠的问题。
- 新增多个独立本地歌单的持久化与播放来源支持；特别喜欢、普通曲库和自建歌单可独立恢复、切换和合并，不再只依赖当前播放队列。
- 优化副屏和较小工作区适配：窗口最小尺寸按当前显示器动态收缩，移动到副屏后自动校正边界，避免窗口超出可见区域。
- 二创正式版固定使用独立的 `%APPDATA%\\Mineradio-oirge` 和稳定高位本地端口，避免与原版 Mineradio 共用后台进程、端口和 Chromium 数据目录；原版关闭后可正常退出，二创关闭到托盘语义保持独立。
- 首次启动自动合并旧共享 profile 与全部 `Mineradio-path-*` 数据，恢复音乐文件夹、播放会话、红心和独立歌单；读取前使用临时副本避免锁住正在运行的原版，旧 profile 后续更新或出现新来源时会再次迁移。
- 修复迁移用隐藏窗口在主窗口创建前触发 `window-all-closed` 导致程序秒退的问题，并补充本地 WAV 播放预加载与错误诊断。
- 全量 Node 回归 `301/301`、关键 JavaScript 语法与空白检查通过；Windows x64 NSIS 安装器已重新构建。

## v1.4.4 迷你播放器封面动效
- 标准迷你播放器默认显示当前歌曲封面圆角矩形图标，悬停或键盘聚焦时展开为完整控制面板，离开后平滑收起。
- 新增封面出现、消失、展开、收起、律动缩放和柔和光晕过渡动画；无歌曲时显示 Mineradio 占位图标。
- 新增视觉控制台 DIY：封面律动开关、律动强度、光晕开关、悬停展开开关和封面圆角调节。
- 律动采用低频音频采样和增量 IPC 同步，主窗口隐藏时仅在迷你播放器启用且播放中采样；极简模式保持无封面。
- 新增迷你播放器视觉、状态缓存和边界校正回归测试，定向测试 `14/14` 通过。

## v1.4.3 Wallpaper Engine 壁纸背景
- 新增独立 Wallpaper Engine 启用开关，默认关闭；选择项目不会自动接管背景，停用后保留上次选择，可随时恢复。
- 新增 Wallpaper Engine 壁纸背景：自动识别本机 Steam/创意工坊库，也支持手动导入项目或场景包。
- Video / 图片壁纸直接作为播放器背景播放；Scene 项目由本机 Wallpaper Engine 原生引擎实时运行，并自动静音壁纸音频。
- 视觉控制台外观页新增 Wallpaper Engine 入口，可搜索、收藏、隐藏、查看项目设置，并一键恢复原背景。
- 桌面主窗口保持透明合成，Scene 走 DWM 缩略图/玻璃采样路径，不改现有玻璃质感、3D 歌单架和桌面歌词。
- 新增 `tests/wallpaper-engine-library.test.js` 覆盖库扫描、静音属性和桌面壳接入。
- 修复部分下载器写入 FLAC 的 `kuwo` / `酷我` 等来源占位标签覆盖文件名歌曲信息；导入 D 盘多级目录时现在保留正确的文件名标题和歌手，并自动让旧错误缓存重新解析。
- 新增三级目录 FLAC 扫描回归与来源占位标签回归，定向 Node 回归 `25/25` 通过。

## v1.4.2 M4A 播放与元数据修复
- 修复本地 `.m4a` 文件未被桌面曲库、文件夹导入和本地文件代理完整识别的问题，统一使用 `audio/mp4`。
- 修复 M4A `data` atom 偏移错误：标题、艺术家、专辑、专辑艺术家、年份、音轨号和 `covr` 封面现在按标准 MP4 布局读取。
- 支持后置 `moov`、UTF-8/UTF-16 标签、JPEG/PNG 内嵌封面，并在后台轻量扫描未覆盖文件尾时保留前台重试状态。
- 提升本地标签缓存 schema，清理修复前可能保存的错误 M4A 元数据；时长、文件大小和独立封面缩略图仍可复用。
- M4A 元数据解析只读取 atom 目录和目标标签/封面范围，不读取 `mdat` 音频内容，保持低 CPU 与低运行内存目标。
- 全量 Node 回归 `266/266`；新增 M4A 标准 atom、UTF-16、PNG 封面、后置 `moov`、缓存失效和桌面扫描回归测试。

## v1.4.1 / v1.4.0 音频格式与本地曲库功能
- 保留 WAV、OGG、M4A 文件夹选择、桌面扫描和对应 MIME 映射；本地文件夹过滤器现已覆盖五种支持的音频格式。
- Electron 升级到 `43.4.0`，新增“特别喜欢”本地歌单、播放来源切换、搜索排序和空曲库恢复稳定性修复。
- 保留桌面歌词、迷你播放器、歌词翻译与本地播放视觉优化。

## v1.3.13 本地曲库空状态与播放来源恢复稳定性
- 修复本地曲库被清空或扫描结果为空时仍恢复旧歌曲、旧播放队列和旧当前歌曲的问题，避免重启后重新出现已经不存在的音乐。
- 修复特别喜欢歌单在本地曲库初始化阶段被误清理的问题，等待曲库准备完成后再校验失效引用。
- 修复普通 / 喜欢播放来源切换后队列顺序校验不严格导致来源混排的问题，并保留切换前的播放进度。
- 新增空曲库清理、来源恢复、队列顺序和喜欢引用清理回归覆盖；全量 Node 回归 `262/262` 通过。
- Windows x64 NSIS 发布只生成安装器、`.blockmap`、`latest.yml` 和 SHA256 清单，不生成或上传 Portable ZIP。

## v1.3.9 主渲染缓存回收与帧调度热路径优化
- 运行时缓存回收从每个有效渲染帧移到一秒级性能采样边界，保留活动态、后台和深后台的原有回收门槛，减少稳定播放期间重复的状态与时钟判断。
- 主循环复用当前帧已经取得的 `performance.now()` 和自适应目标 FPS，调度、帧率跳过判断和交互状态不再为同一帧重复计算。
- 保持播放器布局、视觉质感、播放、歌词、歌单交互、后台调度和更新语义不变；全量 Node 回归 `250/250` 通过。

## v1.3.8 主循环状态快照与时间戳复用
- 主循环复用同一帧时间戳到自由镜头、手势衰减、歌单架更新、Home 波形和空闲引导，减少重复高频时钟读取。
- 新增就地复用的歌单渲染帧快照，`getMode()`、`hasOpenContent()` 和 `shelfAlwaysVisible()` 在主渲染链路中各只读取一次，并复用到歌单架、壁纸压暗、Skull 相机、Skull 粒子和舞台歌词。
- 保持播放器布局、视觉质感、播放、歌词、歌单交互和更新语义不变；全量 Node 回归与语法检查通过。

## v1.3.7 主渲染帧时间戳与歌词歌单状态复用
- 主循环取得的 `performance.now()` 时间戳复用到歌单架 hover 提示、Home 空库波形和空闲引导，避免同一帧重复读取高频时钟。
- 舞台歌词每帧只读取一次歌单模式、详情打开状态和常驻显示状态，复用到歌词避让、壁纸安全布局和详情偏移判断。
- 保持播放器布局、视觉质感、播放、歌词、歌单交互和更新流程不变；新增时间戳与歌单状态快路径回归断言。

## v1.3.6 3D 歌单详情绘制快路径复用
- 详情页每次同步可见歌曲行时只读取一次歌单架外观设置，并将同一帧快照复用到面板、可见行和中心行重绘。
- 面板与详情行绘制复用当前帧强调色和背景透明度，减少滚动、加载动画和主题刷新期间重复归一化偏好与解析颜色。
- 保持详情布局、动画、播放、歌词、交互、UI 文案和视觉质感不变；新增帧快照复用回归断言，修正可见行热路径测试契约。

## v1.3.5 3D 歌单详情属性低写入优化
- 3D 歌单详情可见行的位置、缩放、旋转、可见性、渲染顺序和材质透明度只在目标值变化时写入，减少滚动、播放和稳定展示期间的 Three.js setter 与矩阵更新。
- 详情组的位置、缩放、普通欧拉姿态和面板透明度复用稳定值缓存；相机四元数姿态切回普通姿态时显式失效缓存，保留原有姿态与过渡语义。
- 新增歌单详情行、详情组属性写入回归测试；全量 Node 回归 `245/245` 通过，不改变播放器布局、视觉质感、播放、歌词、桌面覆盖层或用户设置。

## v1.3.4 3D 歌单架卡片属性低写入优化
- 3D 歌单架卡片的位置、缩放、旋转、相机姿态、可见性、渲染顺序、材质颜色和透明度只在目标值变化时写入，减少稳定播放和交互期间的 Three.js setter 与矩阵更新。
- 保留动画目标、侧栏/舞台布局、Skull 相机姿态、卡片透明度和命中顺序；从相机姿态切回普通旋转时保留原有 `rotation.z` 语义。
- 新增歌单架卡片属性缓存回归测试；不改变播放器布局、视觉质感、播放、歌词、桌面覆盖层或用户设置。

## v1.3.3 相机与交互热路径低分配优化
- 普通相机在位置、观察点和节拍滚转稳定时跳过重复的 `position.set()`、`lookAt()` 和滚转重建；自由镜头、Skull 覆盖相机切换时显式失效缓存。
- 歌单架 hover 检测复用固定指针对象，涟漪九宫格去重改用整数位掩码，减少播放和交互期间的短命对象与 GC 压力。
- 新增相机姿态和涟漪热路径回归测试；不改变播放器布局、视觉质感、播放、歌词、桌面覆盖层或用户设置。

## v1.3.2 主渲染器与壁纸覆盖层低占用优化
- 可见但未播放且没有交互时，主渲染器由 30 FPS RAF 改为按目标帧率定时唤醒，减少高刷新率屏幕上的无效 JS 回调；播放或交互开始时立即恢复 RAF。
- 壁纸覆盖层播放时保留 RAF，未播放时限制为 30 FPS，关闭时改为 1 秒低频调度。
- 壁纸关闭时立即释放封面图引用和粒子数组；状态切换、窗口可见性切换会取消旧句柄，避免 RAF 与定时器并存。
- 新增主渲染器调度和壁纸资源释放回归测试；不改变播放器布局、视觉质感、播放、歌词、桌面覆盖层或用户设置。

## v1.3.0 固定乱序播放队列
- 切换到随机播放时立即用 Fisher-Yates 将整个当前队列打乱一次，之后不再为每次“下一首”重新抽签。
- “上一首”和“下一首”统一沿这份固定乱序队列前后移动并循环，因此连续点多少次都能稳定往返。
- 洗牌时保持当前正在播放的歌曲不变，只更新它在新队列中的索引，不会突然切歌或重播。
- 随机模式下换入新歌单、恢复上次随机播放会话时，也会在首次播放前完成一次整队洗牌。
- 使用 `WeakSet` 标记已洗牌队列，不保留已被替换的旧队列；移除 v1.2.100 的播放历史数组，进一步简化内存所有权。
- 主界面、迷你播放器、桌面歌词、系统媒体键和快捷键继续共用同一套前后切歌逻辑，不改 UI、歌词或音质。

## v1.3.1 CPU 与运行内存优化
- 可见但未播放且没有交互时，主 3D 渲染降至 30 FPS；播放、交互、后台和帧压力状态保持原有调度语义。
- 3D 歌单架在启动页或完全隐藏时跳过卡片位置、旋转、透明度和详情更新，避免不可见内容继续占用主线程。
- 主实时频谱四段累加改为单次 TypedArray 扫描；独立节拍频谱按 30 FPS 刷新并复用五个频段标量，保持 kick、人声、中频、高频和 RMS 数值不变。
- 本地资产后台预载改用数字排序键，避免大曲库导入时为每首候选歌曲创建临时排序对象。
- IndexedDB 缓存清理改为串行游标扫描和逐条删除，不再同时把资产、歌词和曲库完整记录复制到多套数组。
- 歌词特效关闭、暂停镜头无残留冲击时直接跳过无效帧工作；歌词镜头边界计算移除每帧临时闭包。
- 安装器从原始 Windows 命令行读取 `/D=`，保留安装目录归一化和卸载安全门禁。
- 不改变播放器布局、视觉、播放、歌词、桌面覆盖层或用户设置；全量 Node 回归 `237/237` 通过。

## v1.2.100 随机播放上一首修复
- 修复随机播放时“上一首”不能返回刚才歌曲的问题：现在按真实播放历史后退，不再根据随机索引猜上一首。
- 从历史后退后再点“下一首”，会先沿原播放历史前进；走到历史末尾后才继续随机选曲。
- 随机下一首会排除当前歌曲，队列有多首歌曲时不会原地重复。
- 播放历史最多保留 96 条，只记录轻量 ID 和歌曲键，不强引用歌词、封面或完整歌曲对象；清空队列时同步释放。
- 主界面、迷你播放器、桌面歌词、系统媒体键和快捷键继续共用同一套上一首逻辑，不改 UI 和播放音质。

## v1.2.99 更新动画与下载轮询按状态运行
- 没有可用更新时，更新入口不再永久运行两条 GSAP 呼吸动画；窗口最小化、隐藏或进入后台时立即释放 tween，恢复前台且确有更新时才按需重建。
- 更新下载在面板打开时保持原有 320–360ms 进度刷新；关闭面板后自动降到 1.5 秒，窗口后台降到 5 秒，重新打开面板立即刷新，下载任务本身不暂停。
- 预览版进度定时器在面板关闭或窗口后台时显式取消并清空引用，避免不可见界面继续更新 DOM。
- 不改动播放器布局、视觉质感、播放、歌词、桌面覆盖层或用户设置。

## v1.2.98 空闲 Canvas 与提示音资源按需释放
- 空场背景功能关闭时，不再为隐藏的 `idle-guide-canvas` 申请全屏 2D backing store，也不再启动每 `140ms` 一次的永久空轮询。
- 视觉引导进入歌单架提示步骤时才按需激活 Canvas、resize 监听和 RAF；提示淡出后自动取消任务、清空粒子/轨迹并缩回 `1 × 1`。
- 歌单架选择提示音的独立 AudioContext 在最后一次播放后空闲 5 秒自动关闭，同时释放 6 个噪声 AudioBuffer；下一次交互按需重建。
- 不改动播放器布局、视觉质感、播放逻辑、歌词内容和用户设置。

## v1.2.97 CPU 与运行内存释放优化重发
- 启动页退出后立即释放全屏 Canvas/WebGL context、shader、buffer、粒子数组和 resize 监听，不再让隐藏启动页长期占用 GPU/内存或在窗口缩放时做无效重建。
- 启动音效结束后主动关闭独立 AudioContext，释放音频线程、节点图和临时噪声缓冲。
- 主窗口最小化、隐藏或不可见时彻底暂停主 3D RAF；恢复可见时无损续帧，播放、桌面歌词和壁纸的独立调度不受影响。
- Windows 发布继续只生成 NSIS 安装包、`.blockmap` 和 `latest.yml`，不再生成 Portable ZIP。
- 不改动播放器布局、视觉质感、播放逻辑、歌词内容和用户设置。

## v1.2.96 迷你播放器桌面歌词按钮细化
- 桌面歌词按钮改为右下角小号“词”字，移除复杂显示器图标和方框，不挤占播放控制栏空间。
- 开启时仅以青色文字和轻微文字光效提示，关闭时保持低调灰色。
- 标准和极简迷你播放器保持同一位置语义，继续复用桌面歌词开关与状态同步。

## v1.2.95 迷你播放器桌面歌词开关
- 标准迷你播放器和极简迷你播放器各增加一个桌面歌词显示/关闭按钮，不新增多余控制项。
- 按钮状态跟随桌面歌词实时同步，开启时高亮，关闭时恢复默认；悬停提示随状态切换。
- 点击后复用主界面桌面歌词开关和持久化链路，不影响播放控制、桌面歌词位置及其它设置。

## v1.2.94 桌面歌词调节控件重排
- 桌面歌词字号和光效调整改为两组独立设置胶囊，统一图标、标签、数值和加减键的视觉层级。
- 去掉散落的圆形/方块按钮，默认保持低调，悬停和按下时才显示轻量反馈。
- 保留字号、光效步进值、锁定逻辑、播放控制、`×` 关闭和位置持久化行为。

## v1.2.93 桌面歌词调节按钮视觉优化
- 重做桌面歌词字号和光效调节按钮，改为精致胶囊边框、独立加减图标和百分比徽章。
- 保留原有字号、光效步进值、锁定逻辑和控制栏布局，不改变调整功能。
- 统一悬停、按下和禁用状态的反馈，降低默认文本按钮的廉价感。

## v1.2.92 桌面歌词播放控制栏
- 桌面歌词工具栏新增上一首、播放/暂停、下一首按钮，统一转发主播放器命令，不维护重复播放状态。
- 播放按钮根据当前播放状态自动切换播放/暂停图标与无障碍标签。
- 桌面歌词关闭按钮改为 `×` 符号，保留原有关闭行为。
- 保留桌面歌词位置、锁定、字号、光效和窗口持久化修复。

## v1.2.91 桌面歌词手动位置持久化修复（重发）

- 修复桌面歌词拖到屏幕顶部之外时，真实窗口坐标（例如 `y=-171`）在保存和恢复阶段被强制夹成 `y=0`，导致重启后回到错误位置的问题。
- 对仍有可操作区域留在当前显示器内的手动位置保留完整坐标；窗口已经不可操作或显示器变化时仍会安全拉回屏幕。
- 保留调整纵向位置时清理旧 `desktopLyricsBounds` 的修复；新增真实负坐标保存、恢复和不可达位置回退测试，全量 Node 回归 `202/202` 通过。
- 不改变桌面歌词 UI、布局、字号、透明度、光效、播放时钟、鼠标穿透或交互入口。
- 本次按要求覆盖重发同版本 `1.2.91`；已经安装旧 `1.2.91` 的设备需要手动覆盖安装新版安装包。

## v1.2.90 更新介绍清洗与乱码防护

- 修复 GitHub Release 使用“修复内容 / 验证 / 下载”等 Markdown 章节时，更新卡片把章节标题误当摘要、验证或下载信息挤占更新条目的问题；摘要现在取首条真实更新内容。
- 更新介绍统一清理 Markdown 标题、链接、图片、重复项和 SHA 校验信息，并在“验证 / 下载 / 安装”等后续章节前停止提取。
- 增加 UTF-8 BOM 与常见“UTF-8 被按 Latin-1 解码”乱码恢复；无法可靠恢复的替换字符和控制字符不会再直接显示到更新面板。
- 自定义更新 manifest 的 `summary` / `notes` 与 GitHub Release 正文复用同一归一化链路；新增 4 个回归用例，当前全量 Node 回归 `200/200` 通过。
- 不改变更新面板布局、下载线路、补丁策略、安装器校验、UI、歌词、播放或视觉效果。

## v1.2.89 桌面歌词状态与位置持久化修复

- 修复桌面歌词已经开启时退出软件，主进程拆卸覆盖层却向 renderer 广播“已关闭”，导致重启后开启偏好被覆盖的问题；用户主动关闭仍会正常保存关闭状态。
- 修复桌面歌词拖动过程中遇到程序定位保护窗口时，最后一次坐标可能没有落盘的问题；拖动、窗口关闭和更新重启都会强制保存最终位置。
- 快速补丁触发立即重启前先同步保存视觉配置，主进程重启前再关闭覆盖层并落盘窗口坐标，避免直接 `app.exit()` 跳过常规卸载保存。
- 新增桌面歌词持久化回归测试；全量 Node 回归 `196/196` 通过，不改变歌词样式、播放时钟、鼠标穿透、锁定、字号、光效或 UI 布局。

## v1.2.88 Home 空库波形热路径优化

- Home 空库波形预先缓存 24 个频谱柱对应的频谱桶索引；播放分析频谱长度不变时，刷新波形不再每次重复执行幂运算和边界计算。
- 保持波形刷新间隔、频谱取样顺序、平滑参数、柱高、透明度、节拍驱动和 UI 视觉完全不变。
- 新增 Home 波形热路径回归测试；全量 Node 回归 `194/194` 通过。

## v1.2.87 桌面歌词 60 FPS 少分配优化

- 桌面歌词窗口按“原始文本 + 单/双行模式”缓存归一化结果；播放中相同歌词的高频 IPC 不再重复执行换行拆分、空行过滤和数组拼接。
- 画布光效绘字把 Unicode 字形数组与字宽一起写入缓存；同一句歌词后续描边/填充绘制不再每帧 `Array.from()` 重建字符数组，代理对字符显示语义保持不变。
- 新增桌面歌词热路径回归与全部发布页面内联脚本语法门禁；GitHub Actions 同时直接检查 `public/app.js`。
- 不改变歌词文本、字号、位置、颜色、光效、30/60 FPS、播放时钟、鼠标穿透或调整栏自动避让行为；全量 Node 回归 `193/193` 通过。

## v1.2.86 桌面歌词修复与发布链路完善

- 将 `v1.2.85` 的桌面歌词修复提升为新版本；已安装 `1.2.85` 的客户端可通过版本比较正常发现更新。
- 保留窗口被拖到物理屏幕顶部之外时的裁切量同步，歌词调整栏会自动移动到实际可见区域。
- 将当前完整源码推进 GitHub 默认 `main`，仓库首页、clone 源码与安装包版本恢复一致。
- 新增 GitHub Actions Windows CI：依赖安装、全量 Node 回归、主进程/预加载/服务端语法检查和 `git diff --check` 都会在 push、PR 时自动执行。
- 正式发布资产只保留安装器、blockmap、`latest.yml` 和 SHA256 清单；Portable ZIP 只保留本地产物，避免 Release 正文错误声称远端可下载。
- 发布前全量 Node 回归 `190/190` 通过，安装器、blockmap、`latest.yml` 和本地 Portable ZIP 均已生成并校验。
## 2026-08-09 NSIS 静默安装目录安全修复（待发布）

- 保留从原始 Windows 命令行读取 `/D=` 的修复，避免自定义安装逻辑覆盖用户显式指定的目录。
- 保留专用安装目录归一化、`.mineradio-install-root` 标记和卸载安全门禁；新增安装器命令行回归测试。

## v1.2.85 桌面歌词调整栏自动避让修复重发

- 修复歌词靠近屏幕顶部时，上方的锁定、字号和光效调整栏被屏幕上沿裁切的问题；上方空间不足会自动翻到歌词下方。
- 调整栏会跟随歌词舞台浮动和入场动画重新定位；上下空间都不足时选择屏幕上露出更多的一侧。
- 修复窗口被拖到物理屏幕顶部之外时 renderer 无法感知裁切量的问题；主进程通过轻量几何 IPC 传递上下裁切量，调整栏会稳定翻到实际可见区域内。
- 不改变歌词样式、窗口位置保存、鼠标穿透、拖动和播放时钟；全量 Node 回归 `189/189` 通过。

## v1.2.84 桌面覆盖层帧热路径优化

- 移除主渲染动画帧对桌面歌词/壁纸同步的重复调用；覆盖层继续由独立自调度计时器按歌词 FPS 和壁纸节奏同步，减少每帧无效判重、时间读取和状态分支。
- 不改桌面歌词显示时序、歌词位置、壁纸画面、UI、玻璃质感、播放控制和鼠标操作；后台/隐藏窗口降载逻辑保持不变。
- 新增桌面覆盖层调度器契约测试；全量 Node 回归 `183/183` 通过后发布。

## v1.2.83 更新状态轮询单飞优化

- 更新下载/快速补丁状态轮询由固定 `setInterval` 改为单飞请求：上一轮未返回时不再叠加下一轮请求，网络慢时减少请求堆积和无效 JSON/DOM 刷新。
- 增加轮询代际与任务 ID 校验，旧任务迟到响应不会覆盖新任务状态；完成、失败、切换任务时统一清理定时器和在途标记。
- 桌面歌词交互提示在歌词靠近窗口顶部、上方空间不足时自动移动到歌词下方；手动拖动后的窗口坐标在重启后优先恢复，关闭时补存一次。
- 修复桌面歌词外层同步调度固定 `320ms` 造成的歌词慢半拍：启用桌面歌词时改为跟随 `30/60 FPS` 推送节奏，保留后台压力和隐藏窗口降载保护，不改音频时钟、不影响正常电脑操作。
- 新增更新轮询、桌面歌词布局和同步频率回归测试；全量 Node 回归 `182/182` 通过。

## v1.2.82 静态资源流式传输优化

- 本地服务器对 HTML、CSS、JS、字体和图标的 `200` 响应改用 `fs.createReadStream()` 流式发送，避免先把整份 renderer 资源复制成 Buffer，降低启动和重复加载时的瞬时内存峰值。
- 保留 `ETag`、`Last-Modified`、`Cache-Control: no-cache` 和 `304` 条件请求语义；页面、UI、布局、玻璃质感、电影视觉、播放控制和交互语义不变。
- 新增静态资源流式响应回归测试；当前全量 Node 回归 `175/175` 通过。

## v1.2.81 本地静态资源条件缓存优化

- 本地服务器为 HTML、CSS、JS、字体和图标增加 ETag/Last-Modified 条件缓存；重复启动命中缓存时返回 `304`，不再重新传输大型 renderer 资源。
- 保持更新后的资源可及时生效，使用 `Cache-Control: no-cache` 校验而不是长时间强缓存；页面、UI、布局、玻璃质感、电影视觉、播放控制和交互语义不变。
- 新增真实 HTTP 200/304 回归测试，完整回归共 `174/174` 通过。

## v1.2.80 asar 资源归档与运行根拆分优化

- 收窄 `asarUnpack` 到可写的 `server.js` 和 `package.json`；`public`、`build` 和桌面脚本回到 `app.asar`，减少解包文件与启动 I/O。
- 主进程和本地服务器拆分“可写运行根”和“静态资源根”：补丁仍只写 `app.asar.unpacked`，静态页面、字体、图标从 `app.asar` 读取。
- 兼容 `v1.2.79` 已安装的旧解包布局；使用完整安装器升级后不要求手动迁移文件。
- 实际 Electron 隔离启动验证首页、CSS、JS、字体和图标资源均返回 `200`；UI、布局、玻璃质感、电影视觉、播放控制和交互语义不变。

## v1.2.79 全量启动与后台资源优化

- `asar` 开启为 `true`，将 `public`、`server.js`、`package.json` 和 `build` 放入 `app.asar.unpacked`；主进程、本地服务器和快速补丁统一使用可写运行根，保留原有更新链路。
- 自定义封面、歌词、歌词偏好、本地节奏图、听歌统计和视觉存档迁移到独立 IndexedDB；旧 `localStorage` 自动迁移，写入失败才回退，曲库快照/索引成功落盘后清理旧大 JSON。
- 桌面歌词与壁纸覆盖层恢复 Chromium 后台节流，隐藏时停止 RAF，重新可见后继续；移除全局禁用后台节流开关，主窗口后台保活策略不变。
- 新增 Noto Sans SC UI 中文子集并修正 Inter 为真实 WOFF2；动态歌词未命中的字符继续使用 Windows 本地字体回退，无远程字体请求。
- 新增全量优化门禁测试；UI、布局、玻璃质感、电影视觉、播放控制和交互语义保持不变。

## v1.2.78 启动资源本地化与渲染器缓存优化

- 主渲染页面改为加载可缓存的 `app.css`、`app.js`，HTML 首屏入口从约 `1.6MB` 降到约 `55KB`，减少冷启动时重复解析内联 CSS/JS 的开销。
- 移除启动时的 Google Fonts 网络依赖，新增本地 Inter 字体资源；中文字体保留 Windows 本地回退，动态歌曲名和歌词不做危险的固定字集裁剪。
- 逐字歌词进度复用播放帧游标，连续播放不再每帧从当前歌词行第一个字重新扫描；跳播会自动重置游标，歌词高亮数值和视觉表现保持不变。
- 移除未使用的 `gsap`、`mpg123-decoder` npm 依赖及锁文件记录；保留 `public/vendor/gsap.min.js`，不改变现有动画库运行路径。
- 保持 `asar:false`，因为现有快速补丁需要通过 `server.js` 的 `__dirname` 原地替换资源；UI、布局、玻璃质感、播放控制和 3D 歌单架交互语义不变。
- 新增本地 renderer 资源、字体资源和逐字歌词游标回归测试。

## v1.2.77 3D 歌单详情屏幕命中低分配优化

- 详情歌单屏幕点击兜底改为单次扫描可见行，保持 `renderOrder` 命中优先级，移除每次点击的 `filter().sort()` 临时数组。
- 详情行屏幕投影复用四个固定向量和 bounds scratch，减少连续点击、按钮命中和屏幕拾取路径的短命对象与 GC 压力。
- 保持详情列表布局、命中范围、按钮区域、播放/收藏/下一首动作、UI、玻璃质感和 3D 歌单架交互语义不变；新增屏幕命中热路径回归测试。

## v1.2.76 3D 歌单架卡片绘制低卡顿优化

- 歌单架每帧复用同一份外观快照，卡片绘制签名、DOF 变化、中心状态变化和周期重绘不再重复读取歌单架设置与强调色。
- 保持卡片颜色、透明度、布局、UI、视觉质感和交互语义不变；新增卡片绘制帧快照复用回归测试。
- 全量 Node 回归 `166/166`、59 个 JavaScript 文件语法检查、前端内联脚本解析和 `git diff --check` 均通过。

## v1.2.75 音频分析热循环低卡顿优化

- 频谱分析四个频段先累加 8 位采样值，再统一执行归一化，减少播放分析帧中的重复除法。
- 时域 RMS 使用 256 项归一化平方查找表，避免每个分析帧对约 2048 个采样重复执行减法、除法和乘法。
- 保持频段边界、RMS、节拍判断、视觉响应、UI、布局、播放控制和交互语义不变；新增旧算法数值等价回归测试。

## v1.2.74 3D 歌单详情帧快照复用低卡顿优化

- 3D 歌单架把当前帧已经计算的布局与外观设置快照传给歌单详情列表，详情页每帧不再重复归一化设置和解析颜色。
- 可见歌曲行定位、详情面板透明度和布局更新复用同一帧快照；保持歌单详情的布局、透明度、滚动、交互、UI 和视觉质感不变。
- 新增歌单详情帧快照复用回归测试；构建前执行全量 Node 回归、前端内联脚本解析、语法检查和 `git diff --check`。

## v1.2.73 3D 歌单架帧设置快照复用低卡顿优化

- 歌单架更新帧先计算一次 `shelfSettings()`，再把同一份设置快照传给 `shelfLayoutProfile()`，避免每帧重复执行偏好归一化和颜色解析。
- 保持歌单架布局、卡片位置、透明度、层级、详情页、UI、视觉质感和交互语义不变；新增帧级设置快照回归测试。

## v1.2.72 相机投影矩阵低卡顿优化

- 普通镜头和自由镜头只在 `fov`、`aspect`、`near`、`far`、`zoom` 或胶片投影参数变化时重建投影矩阵，稳定播放帧跳过重复矩阵计算。
- 保持镜头位置、节拍推拉、自由镜头、窗口尺寸变化和视觉输出语义不变；新增相机投影热路径回归测试。

## v1.2.71 实时节拍分析低卡顿优化

- 实时节拍分析复用主频谱分析已经计算出的时域 RMS，避免同一播放帧再次扫描 `2048` 个时域采样点；直接调用节拍引擎时仍保留原有回退路径。
- 缓存实时节拍频段的采样桶边界，避免连续播放期间重复执行相同的 Hz 到 FFT 桶换算；频段数值、节拍判断和视觉响应保持不变。
- 空 Home 波形先执行已有时间节流，再查询 DOM，减少未到刷新间隔时的重复节点查找；不改变 Home 波形显示和播放交互。
- 新增实时音频分析热路径回归测试；全量 Node 回归测试通过后再构建发布。

## v1.2.70 队列可见行快照复用低卡顿优化

- 队列面板和底部迷你队列为同一轮可见歌曲生成一次行快照，复用歌曲键、副标题、封面签名、封面地址和喜欢状态，减少连续元数据/封面刷新时的主线程重复计算。
- 主队列生成的可见行快照传给迷你队列；保持队列排序、当前歌曲高亮、封面、文案、按钮、懒加载和交互语义不变。
- 新增队列热路径回归测试；旧版同输入 72 行渲染中，副标题读取由 144 次降至 72 次，喜欢状态由 216 次降至 72 次。

## v1.2.69 Home 首屏快照复用低分配优化

- Home 刷新时复用同一轮已经计算的本地歌曲池和听歌统计摘要，避免首屏卡片渲染再次扫描大曲库与长期统计记录，降低导入后恢复和播放统计更新时的主线程开销。
- 保持 Home 卡片顺序、标题、封面、文案、布局和交互语义不变；新增快照复用帧热点回归测试。

## v1.2.68 3D 歌单架卡片帧状态复用优化

- 3D 歌单架卡片布局直接复用更新帧已计算的内容打开状态和常驻显示状态，避免每张可见卡片重复查询，降低持续播放、视差移动和详情切换时的主线程开销。
- 保持卡片位置、层级、透明度、封面绑定、详情页遮罩和交互语义不变；补充帧级热点契约测试。

## v1.2.67 舞台歌词与歌单架帧更新低开销优化

- 3D 舞台歌词光粒循环复用当前帧的启用状态、节拍强度、漂移系数和粒子数量，减少大批量光粒更新时的重复属性读取与分支判断；粒子坐标、旋转、透明度和可见性语义保持不变。
- 3D 歌单架每帧缓存内容打开状态和常驻显示状态，避免可见性、层级和封面绑定路径重复查询，降低持续播放与鼠标视差更新时的主线程开销。
- 新增歌词光粒与歌单架帧热点契约测试；不改变 UI、布局、文案、玻璃质感、电影视觉、播放控制或 3D 歌单架交互。

## v1.2.66 歌单详情可见行渲染低分配优化

- 3D 歌单详情页复用可见窗口时，将歌曲引用同步和条件重绘合并为单次索引循环，减少连续滚动、加载动画和曲库刷新时的回调遍历与短命闭包分配。
- 保持可见歌曲窗口、中心行判定、加载动画刷新、绘制顺序和 UI/交互语义不变；新增可见行热路径回归测试。

## v1.2.65 本地搜索预热与空查询缓存优化

- 本地搜索候选池筛选结果直接复用给索引预热任务，避免大曲库每次刷新时重复遍历同一批歌曲。
- 空搜索结果按曲库签名缓存，重复打开搜索区或恢复本地库时不再反复复制最多 80 个歌曲引用；搜索排序、数量上限和结果 DOM 保持不变。
- 新增本地搜索预热数组复用、混合队列结果保持和空查询结果身份复用回归测试。

## v1.2.64 本地资产缓存与封面流式读取优化

- IndexedDB 升级到 v3，新增独立 `lyrics` store；旧 `assets` 记录按 cursor 逐条迁移，歌词原文不再和封面、元数据一起驻留在资产记录中。
- 歌词缓存的读取、写入、释放后合并和清理全部切换到独立记录，保留 `complete`、`error`、`abort` 三路事务结算，减少大曲库缓存补水与清理时的内存峰值。
- 桌面曲库外置封面优先使用已授权的本地流 URL，缩略图和当前封面直接从 URL 解码，不再把整张图片读成 renderer base64；普通浏览器拖放文件仍保留 data URL 回退。
- 保留现有 UI、布局、玻璃视觉和播放交互；新增 5 条回归用例，全量测试 `150/150` 通过。

## v1.2.63 桌面歌词渲染热路径性能优化

- 桌面歌词光效文字改为按字形宽度缓存，播放中同一句歌词不再每帧重复调用 `measureText`；文本、字号或字体变化时自动重建，视觉输出与之前一致。
- 光效四层模糊参数提为常量，每帧不再重建层数组；固定歌词位置 + 暂停时，stage 的 transform/filter、进度百分比、扫光位移等无变化值不再重复写入样式，减少每帧无效样式计算与布局读取。
- 桌面歌词每帧只在实际布局、位移或滚动变化时重新测量歌词矩形；固定模式下暂停时零额外测量，播放与滚动热区判定逻辑不变。
- 新增/更新桌面歌词光效契约回归断言；全量 `node --test` 145/145、内联脚本语法、真实 Chromium 启用态画布渲染和 `git diff --check` 均通过。

## v1.2.62 桌面歌词光效增强与强度调节

- 提亮桌面歌词随播放进度推进的流动光效，扩大扫光范围并增强进度边缘 bloom、文字 glow 与阴影亮度，播放时更清晰易辨。
- 桌面歌词工具条新增光效强度 `− / 百分比 / +` 控制，支持 `0–85%`、每档 `5%` 调整；设置会持久化，并与 DIY 面板的「歌词溢光」滑块同步。
- 保留固定歌词位置、字号调节和歌词热区随字号变化等既有体验；本轮不改歌词布局和玻璃视觉基线。

## v1.2.61 桌面歌词固定位置与流动光效增强

- 修复桌面歌词光效绘制函数未接入每帧循环的问题，播放时现在会随歌词进度流动。
- 增加桌面歌词文字 glow、进度边缘 bloom 和 canvas 叠层，并提高光效亮度，播放时更容易看清歌词。
- 悬浮工具条新增「固定/浮动」按钮；固定状态会关闭上下漂浮并持久化，下次打开桌面歌词继续使用。

## Unreleased

- 桌面歌词新增「固定不跳动」选项（关闭切行弹入和上下漂浮，垂直位置保持固定）
- 接通主软件歌词光效流动（canvas 轻量 aura + progress 高亮 bloom + 粒子，已默认高亮跟随开启并改善边缘流动效果）
## v1.4.9 迷你播放器与更新检测
- 标准迷你播放器本体移除点击后会消失的 `×`；四角“返回主界面”按钮恢复到右上角原位，桌面歌词和播放控制保持不变。
- DIY“迷你播放器样式”区域新增永久可见的 `× 自动收回` 双向开关：开启时离开完整面板后收回封面，关闭时始终保持完整迷你播放器。
- 歌单删除确认改为播放器内暗色玻璃弹层，明确显示歌单名、歌曲数量和“不会删除本地音乐文件”，支持取消、确认、点击遮罩和 `Esc` 关闭。
- 顶栏更新入口改为常驻“检测更新”按钮，更新面板内新增独立检测按钮；提供检测中动画、重复点击单飞、最新版、新版本和网络失败反馈。
- 当前已是最新版或检测失败时禁用无效的下载入口，避免无更新状态误触发预览下载；检测新版本后仍沿用现有完整安装包与轻量补丁链路。
- 升级继续使用 `%APPDATA%\\Mineradio-oirge` 和既有存储键，不清空音乐文件夹、播放会话、特别喜欢、自建歌单或 DIY 设置。
- 全量 Node 回归 `325/325`，关键 JavaScript 语法检查与 `git diff --check` 通过。
