# Mineradio Project Rules

## Project Identity

Mineradio 是 Windows Electron 桌面音乐播放器，核心体验包括搜索、播放、歌单、歌词、3D 歌单架、粒子视觉预设、DIY 视觉控制台和 GitHub 自动更新。

- 当前可写代码/Git 仓库：`C:\Users\Administrator\Desktop\Mineradio-main`
- 当前环境未找到旧运行目录：`E:\桌面\播放器软件\Mineradio\resources\app`
- GitHub 仓库：`https://github.com/oirge/Mineradio.git`
- 当前源码续版：`v1.8.1`（整机备份：设置面板「高级」页新增「备份」折叠区，导出 `mineradio.backup`、在新电脑导入。落盘格式就是用户给的四段 `{version:2, database:{songs,playlists,favorites,history}, config:{theme,eq,player}, paths:{musicFolders}}`，外加一段 `meta`（`app`/`appVersion`/`exportedAt`/`counts`）；顶层与每段的键序被 `tests/mineradio-backup-export-import.test.js` 用 `deepEqual(Object.keys(...))` 钉死，往里加字段会红。**歌曲身份一律存 `{folder, rel, size, mtime}`，绝不存绝对路径** —— 本机 `localKey` = `绝对路径:size:mtime`，歌单/收藏引用键是 `local-key:` + 它、SQLite `song_key` 也是它，直接存下来换机盘符一变全体扑空；导入时按新根重新拼路径并重算这四样。`mineradioBackupRelPath` 必须保持原始大小写（用 `normalizeLocalLibraryPathKey` 的结果只算长度、从原始串尾部切，那函数会 `toLowerCase()`，拿它的输出当 `rel` 会让同机导入也复不出原 `localKey`）；拼回去的分隔符由 `mineradioBackupPathSeparator` 看根路径决定（含 `\` 或 `X:` 形态判 Windows），错一个字符就等于换了一首歌。**判断备份里的音乐文件夹在不在本机只能用 `api.refreshLocalMusicFiles(folder, [])`（传空数组＝只验目录+登记授权根，不走盘），绝不能用 `readLocalFileRange` 去探**：`resolveAuthorizedLocalFile` 要求路径在已扫描登记的授权根下，全新会话授权表是空的、必然 `LOCAL_FILE_NOT_AUTHORIZED`，用它判存在会把同机导入也误判成换机、每次弹选择框。换机三态：探到用探回的 `folderPath`、探不到用用户重选的目录、**用户取消一律 `return null` 整个导入放弃（返回 `[]` 会被当成「没有音乐文件夹」继续覆盖歌单）**。`config.player` 是 `MINERADIO_BACKUP_PLAYER_KEYS` 十六键白名单（与 `app.js` 里那批常量逐一对齐、有测试钉住），导出导入两侧都只认这些键、`null` 跳过——否则手改过的备份能往 localStorage 塞任意键值（测试用手写 JSON 塞 `"__proto__"` 验过这道门，`JSON.parse` 才会把它变成真自有属性）。临时键抹除固定五个：`mineradio-local-library-snapshot-v1` / `mineradio-local-library-index-v1` 走 `localStorage.removeItem`（本就不在持久镜像白名单里），`mineradio-queue-snapshots-v1` / `mineradio-playback-session-v1` / `mineradio-song-resume-v1` 走 `removePersistentLocalStorageItem`（**必须连 `desktop-ui-state.json` 里的镜像一起清，否则重启后旧值被镜像顶回来**）。SQLite 侧不加新方法，复用 `bumpPlayStat`（累加语义，空表上等于赋值）+ `setFavorite`；`lastPlayedAt` 统一垫 `1` 而不是 `0`，因为主进程是 `toInt(input.lastPlayedAt) || Date.now()`、传 0 会让所有老歌假装刚刚听过。**同机重复导入会把播放次数翻倍**，这正是导入要两步确认的原因：第一次点只 toast + 记时间戳，`MINERADIO_BACKUP_IMPORT_ARM_MS = 12000` 内再点才真走，在途用 `mineradioBackupImportPending` 挡重复点击并留 `120000ms` 过期逃生门（浏览器文件对话框被直接关窗时不回调，没这道门按钮永久卡死）；导入尾巴是 `setTimeout(restart, 900)` 整体重启，不做热重载（四层存储逐个热应用等于重写启动流程）。历史记录 `cover` 一律清成 `''`（base64 属用户点名不要的「大封面缓存」），条数上限 `MINERADIO_BACKUP_HISTORY_LIMIT = 180` 取自本机 `normalizeListenStatsState` 里的 `compactListenStatsHistory(null, data.history, 180)`。音效档案真身在 IndexedDB 用户态 `user-fx-archives`，导出优先读它、读不到才回落 `readUserFxArchives()`，导入写用户态并删掉 localStorage 旧键（听歌统计同理），不然启动时旧档案会把刚导入的顶回去。主进程两个 handler（`mineradio-export-backup-file` 保存对话框 `extensions:['backup']`、`mineradio-import-backup-file` 先 `statSync` 卡 `64 MiB`）都套 `trustedMainFrameHandler`，**位置必须留在 `ipcMain.handle('mineradio-export-json-file'` 之前**：`tests/import-json-file-size-limit.test.js` 按 `ipcMain.handle('mineradio-import-json-file'` → `ipcMain.on('mineradio-ui-state-read-sync'` 切片，插进那段会污染它。备份块整段夹在 `var MINERADIO_BACKUP_VERSION = 2;` 与 `function openPluginManager() {` 之间，`tests/mineradio-backup-export-import.test.js` 就按这对锚点切进 `node:vm`，所以**这段里新增任何裸标识符都会在 vm 里 `ReferenceError`**，跨切片调用一律 `typeof x === 'function'` 守卫。界面只在 `public/index.html` 的 `fx-plugin-fold` 与 `fx-advanced` 之间加同构折叠区 `fx-backup-fold`（复用 `fx-fold` / `lyric-color-row` / `mini-player-collapse-hint`，`public/app.css` 一行未动，那一族规则全是类选择器、没有按 id 或 `nth-child` 写死的）；**注意 `fx-backup-fold` 故意没进 `fxPanelTargetForNode`，它落到「高级」页靠的是 `organizeFxPanel` 里 `current` 的继承（前面 `fx-eq-fold` / `fx-plugin-fold` 都归 advanced），一旦被挪到某个非 advanced 折叠区上面就会静默跑到别的页去**；`organizeFxPanel` 的强制展开清单也没加（`.fx-fold-head{display:none}` 与 `.fx-fold-body{display:block}` 是无条件规则，`.open` 对新区块没有视觉意义，与 `fx-eq-fold` 同处理）。默认不备份三类：音频文件、封面缓存（**含每首歌的自定义封面、自定义节拍图、自定义歌词**，本轮按要求不进备份）、临时文件（队列快照、续播位置、曲库快照与索引）。新增 `tests/mineradio-backup-export-import.test.js` 18 例，全量回归 `864/864`；发布前补跑过一次本机 Electron 启动冒烟（干净开到 `home-revealed`，`app.js` 零报错），但**备份折叠区在真实窗口里的落位、真实文件对话框与导入后重启这条链仍没有肉眼验证过**，那部分结论全部来自 vm 跑真实源码 + 源码断言。）
- 上一续版：`v1.7.29`（同步指示器「已同步 xx 首歌曲」移到搜索框下面并接上主题插件令牌，另修掉曲库清理的扫描代号缺陷（`desktop/local-library-store.js` 的 `nextSyncStamp`：清理用 `seen_at<>?` 判定，所以那个标记必须每根严格递增，**不能直接用 `Date.now()`** —— 同一时钟刻度里的两次扫描会让删掉的文件躲过清理）；`#local-sync-badge` 的当前形态、按住搜索区的四条边界与三级令牌回落见本文件下面那条指示器专项约束，发布记录见 `RELEASE.md` 顶部小节，全量回归 `846/846`。**上一版 `v1.7.28` 的无缝播放与交叉淡入淡出约束整段继续有效：**无缝播放（Gapless）与 `0~10` 秒交叉淡入淡出（Crossfade）。**`v1.7.25` / `v1.7.26` / `v1.7.27` 没有在本链条留摘要**，那三版的长期约束按日期归档在 `docs/PROJECT_MEMORY.md` 末尾的 `### 2026-09-0x - v1.7.2x` 小节里，读版本史时两处都要看。架构是「双 deck 音频池 + 会移动的全局 `audio` 指针」：`MediaElementSource` 一旦建好就永远绑死那一个 `<audio>`、无法改指向，所以交叉必须要第二个元素配第二个 source。`audioDeckList` 里常驻两个 `<audio>`，每个 deck 各自永久接好 `deck.source.connect(deck.gain)` → `deck.gain.connect(analyser)`，`deck.gain` 另外并一条到 `beatAnalyser`；共享尾链 `analyser → replayGainNode → audioChain.input → audioChain.output → gainNode → audioCtx.destination` 一行未动，**`gainNode.gain` 继续独占 `targetVolume` 与全部既有淡入淡出，deckGain 只做接续/交叉**，两套增益语义不许混用。`deck.gain` 的中性值恒为 `1.0`（IEEE754 下位精确透明），因此符合祖训「整条链常驻音频图，绝不在运行时改接线」——切歌只调增益、永不 `connect`/`disconnect`。全局 `var audio` 被重指向到当前可闻的那个 deck（`audioDeckList` / `audioDeckActive` 声明在 `public/app.js` 最前面第 11 行，必须早于任何用到 `audio` 的代码），所以 UI、进度、听歌统计、media session、桌面歌词、迷你播放器这些消费方全部零改动跟着走；新增消费方也请继续读全局 `audio`、不要自己去 `audioDeckList` 里挑。行为分三档且必须保持：无缝关 + 交叉 `0` = 100% 老逻辑（不预取、不换 deck、保留 `460ms` 主淡入）；无缝开 + 交叉 `0`（出厂默认）= 预取 + 接管，deck 斜坡 `HANDOFF_RAMP_SECONDS = 0.012`，**任何时刻只有一路出声**（相邻两首之间那段可闻的空档，根因就是主淡入的 `460ms`，不是解码慢）；交叉 `> 0` = 两路按等功率 `sin/cos` 曲线交叠（线性交叉中点会掉约 `3 dB`，别改回线性）。只有 `opts.autoAdvance` / `opts.autoRepeat` 允许接管预取出来的 deck，手动切歌、上一首、下一首一律不接管（`adoptGaplessDeckForSong` 里判定，`tests/gapless-crossfade.test.js` 有 `testAdoptRefusesManualSwitch` 钉死）；随机播放待洗牌时 `gaplessShufflePending()` 为真则既不预取也不交叉（否则会预解码出一首洗牌后根本不该播的歌）。交叉的提交时序不能动：`startCrossfadeHandoff` 起播 → `settleGaplessDeckStart` 等 `play()` 真的 resolve 才 `commitCrossfadeHandoff` 对交并推进队列，起播被拒/抛错/不返回 Promise 都走 `abortCrossfadeStart` 原样退回、当前那首继续播、失败的 URL 进黑名单；收尾定时器一律带序号自增守卫，作废的那一轮不许停掉新接管的 deck。WebAudio 坑：`setValueCurveAtTime(curve, now, span)` 在 `now` 已经有别的自动化事件时会抛 `NotSupportedError`，所以 `rampAudioDeckGain` 是 cancel → curve，抛了再 cancel 一次并退回 `setValueAtTime` + `linearRampToValueAtTime`。设置存独立 `GAPLESS_STORE_KEY = 'mineradio-gapless-v1'`（已登记进 `PERSISTENT_UI_STATE_KEYS`），**绝不写进视觉预设 `fx`**，`gapless-crossfade` 滑杆也不进 `bindFxPanel` 的显式 `ids` 白名单。界面只在 `public/index.html` 的 `fx-volume-fold` 与 `fx-eq-fold` 之间新增一个同构折叠区 `fx-gapless-fold`（一个 `fx-toggle` + 一个 `fx-slider` + 两行 `mini-player-collapse-hint`），`fxPanelTargetForNode` 里归到 `advanced`、`organizeFxPanel` 的数组里登记，`public/app.css` 一行未动。已知取舍：`replayGainNode` 只有一个且在 `analyser` 之后，也就是**均衡增益作用在两路求和之后**，交叉那几秒两首歌共用同一个 ReplayGain 增益（要做 per-deck 均衡就得把 `replayGainNode` 拆成两个塞到 `deck.gain` 前面，本轮按「够用就不动」没做）。引擎块整段夹在 `var GAPLESS_CROSSFADE_MIN_SECONDS` 与 `var REPLAY_GAIN_PREAMP_MIN` 之间，`tests/gapless-crossfade.test.js` 就按这对锚点切进 `node:vm`，所以**这段里新增任何裸标识符都会在 vm 里 `ReferenceError`**（被调用方的 `.catch` 吞掉 → 测试全绿但行为错），跨切片调用一律 `typeof x === 'function'` 守卫；两个 vm 常识：ES6 简写方法不可 `new`，桩 `new Audio()` 必须写成 `Audio: function () {}`；`require('node:assert/strict')` 把 `deepEqual` 别名成 `deepStrictEqual`，跨 realm 容器断言前必须 `Object.assign({}, x)` / `Array.from` 拷回本 realm。`tests/auto-playback-startup.test.js` 的启动顺序正则加了可选组 `(?:initGaplessControls\(\);\s*)?`，启动顺序现在是 `initAutoPlaybackControls(); initReplayGainControls(); initAudioChainControls(); initGaplessControls();` 再接 `if (LOCAL_ONLY_MODE) scheduleSavedLocalMusicFolderRestore(700);`。`tests/gapless-crossfade.test.js` 27 例，覆盖交叉秒数封顶、单路出声、主路径提交、起播被拒退回、手动切歌先压到 0 再停、作废定时器、同一首不交叉、待洗牌不预取、播完即停释放预取、交叉中改秒数不截断、无 WebAudio 与曲线不可用两条退路。全量回归 `834/834`。）
- 历史续版：`v1.7.24`（音乐库升级成外层标签页。`#playlist-panel` 顶部 `.panel-tabs` 现在是 `当前队列 / 歌单 / 音乐库` 三个并排 tab（`public/index.html` 新增 `id="tab-library"` → `switchPlaylistTab('library')`，顺序必须是 `tab-queue` → `tab-pl` → `tab-library`）。**tab 名 `'library'` 与播放列表 kind `'library'`（全部音乐）不是同一层概念**：前者指外层那一整页智能分类，后者是 `normalizeLocalPlaylistKind` 认的一个选中项，读代码时别把两个 `'library'` 当同一个东西。两个 tab **共用同一个 `#pl-pane` / `#pl-list` 和同一份 `localLibraryPlaylistSelection`**，所以 `switchPlaylistTab` 必须在切 tab 时把选中项摆到对应那一层：切到 `library` 且当前选中不是分类就置 `LOCAL_LIBRARY_CATEGORY_HOME_KIND`，切回 `playlists` 且当前选中是分类就退回 `'library'`（全部音乐），两种迁移都要跟一次 `resetPlaylistPanelRenderLimit()`；切到 `queue` 一律不动选中项，否则停在某位艺术家上看一眼队列再回来就串页了。**`switchPlaylistTab` 只许改状态 + 渲染，绝不能调 `selectLocalPlaylist`**：后者内部走 `safeSwitchPlaylistTab`，而 `switchPlaylistTab` 又会 `refreshUserPlaylists()` → `renderLocalLibraryPlaylistPanel()`，互调必递归。所有「面板正在铺卡片列表」的判断统一收口到 `isPlaylistListTab(tab)`（`playlists` 或 `library` 都算），四个调用点：3D 歌单架 `currentItems()` 里的 `showPlaylists`、`toggleLikeSong` 里的刷新、面板打开时的入场动画分支、以及 `switchPlaylistTab` 自己的 `#pl-pane` 显隐与动画；其中 `toggleLikeSong` 那处**必须写成 `typeof isPlaylistListTab === 'function' ? isPlaylistListTab() : queueViewTab === 'playlists'`**，因为 `tests/special-liked-playlist.test.js` 把 `toggleLikeSong` 单独切进 `node:vm`、只注入了 `queueViewTab`，裸调会 `ReferenceError`；`tests/local-library-categories.test.js` 另有一条按行过滤的断言钉死「除这条守卫行外，源码里不许再出现 `queueViewTab === 'playlists'`」，以后新增判断请一律走 `isPlaylistListTab`。工具条也是共用的，`applyPlaylistPaneToolbarMode(tab)` 按 tab 切 `#pl-pane-chip` 文案（`音乐库智能分类` / `本地音乐与独立歌单`）并在音乐库那页隐藏 `#pl-pane-create-btn`（「新建」只对独立歌单有意义，「导入」保留），这两个 id 是本版为此新加的、别再改名。渲染侧同步删掉根视图最上面那张「音乐库」卡（`歌单` 页恢复成 特别喜欢 → 独立歌单 → 全部音乐），并让分类首页不再画 `view-head`（标题由外层 tab 承担，`mode === 'home'` 直接只铺 `localLibraryCategoryHomeCardsHtml()`），分组层与歌曲层的 `localLibraryCategoryHeadHtml` 不变——注意它是 `directory ? '' : 播放全部`，所以**只有歌曲层有「播放全部」**，首页与分组层从来没有，文档别再写成「每层都带返回与播放全部」。`queueViewTab` 不持久化、默认 `'queue'`，冷启动永远从当前队列进。三个 `.panel-tab`（`padding:6px 12px` / `font-size:11px` / `gap:8px`）合计约 187px，装得进面板 340px 去掉内边距后的 304px，**`public/app.css` 一行未动**。`tests/local-library-categories.test.js` 扩到 25 例，其中两例用 `node:vm` 跑真实 tab 逻辑（切片锚点 `function normalizePlaylistPanelTab(tab)` → `function setMiniQueueOpen(open)`，桩齐 `tab-queue` / `tab-pl` / `tab-library` / `queue-pane` / `pl-pane` / `pl-list` / `queue-list` / `playlist-panel` / `pl-pane-chip` / `pl-pane-create-btn` 十个节点），覆盖两向迁移、`library-value:artist:周杰伦` 经队列 tab 往返不丢、`local-playlist:abc` 切音乐库落首页、`special-liked` 不被动、以及 `LOCAL_ONLY_MODE` 下 `podcasts` 折成 `playlists` 时选中项归 `'library'`。全量回归 `655/655`。）
- 历史续版：`v1.7.23`（音乐库智能分类。三层 kind 命名空间全部挂在 `library-` 前缀下：`library-cat:<id>`（歌曲视图，`all/recent-added/recent-played/most-played/never-played`）、`library-group:<field>`（分组目录，`artist/album/albumArtist/genre/decade`）、`library-value:<field>:<value>`（分组里的某一项，只在**第一个**冒号处切分，所以专辑名里带冒号也不会被切坏），外加 `library-cat:home` 当音乐库首页；唯一入口是 `normalizeLocalPlaylistKind`，认不出的 `library-*` 一律回落 `'library'`，任何新 kind 都必须先过这道门。**`localPlaylistSongs` 的分支顺序不能动**：特别喜欢 → 独立歌单 → 智能分类 → `return localSearchPool()`，因为 `tests/special-liked-playlist.test.js:117` 用 `strictEqual` 钉死了 `localPlaylistSongs('library')` 和 `localSearchPool()` 是同一个数组，把分类分支提前或让 `library` 走 `slice()` 都会当场红。分类模块（`LOCAL_LIBRARY_CATEGORY_*` 常量、`localLibraryCategoryStore`、`localLibraryGroupEntries`、`localLibraryCategorySongs`、`localLibraryValueSongs`、`localLibraryCategoryView`、`localLibraryCategoryDomSignature`）整段放在 `function normalizeLocalPlaylistKind(kind)` → `function localSongIndexByKey(songs, key)` 这对锚点之间，因为 `tests/local-playlists.test.js`、`tests/special-liked-playlist.test.js`、`tests/local-library-categories.test.js` 都按这对锚点在 `node:vm` 里跑真实源码；跨切片调用一律 `typeof x === 'function'` 守卫（`ensureListenStatsState`、`queueItemKey`、`LOCAL_LIBRARY_NAME_COMPARE`——后者声明在 `public/app.js` 靠后位置，vm 里缺它时 `localLibraryGroupNameCompare` 退成码点比较，测试要显式注入 `Intl.Collator('zh-Hans-CN')` 才是界面真实的拼音顺序）。`setLocalPlaybackPlaylistSelection` 里的持久化门写成内联字面量 `nextSelection.indexOf('library-') !== 0` 而不是用常量，因为 `tests/special-liked-playlist.test.js:211` 从 `function setLocalPlaybackPlaylistSelection(kind)` 起切片，常量在它上面、切不进来。动态分类只在内存里当播放来源：`setLocalPlaybackPlaylistSelection` 不写 `LOCAL_PLAYBACK_SOURCE_STORE_KEY`、`readSavedLocalPlaybackPlaylistSelection` 把存进去的分类读成 `'library'`、`openLocalLibraryQueue` 在分类为空时落回全部音乐（否则冷启动分类还没水合就是 `[]`，会误弹"导入本地音乐"）。索引是一次遍历建好的单槽缓存 `localLibraryCategoryCache`，签名是 `length|首localKey|末localKey|listenStats.updatedAt`；`invalidateLocalPlaylistSongLookup()` 末尾追加了 `invalidateLocalLibraryCategoryIndex()`，一次覆盖导入/恢复/自动同步/监控四条路径，`localLibraryValueSongs` 的单槽缓存在重建时跟着清。入库时间戳走渲染层的 `mineradio-local-added-at-v1`（按 pathKey 记，`LOCAL_LIBRARY_ADDED_AT_LIMIT = 4000`，超了先裁最旧）——**SQLite 曲库表没有迁移通道**（`FILE_INDEX_COLUMNS` 丢未知字段、`FILE_UPSERT_SQL` 的 keep 分支在指纹变化时清列、`ensureOpen()` 只有 `CREATE TABLE IF NOT EXISTS` 加 `PRAGMA user_version = 1`），所以不要往 `desktop/local-library-store.js` 里加 `added_at`；用 pathKey 而不是 `localKey` 是因为后者含大小与 mtime，改一次标签就变。只给 `stats.hasIndex` 为真时的 `localLibraryChangeState === 'new'` 盖章，首次整库导入不盖（否则两万首同一时刻，"最近添加"没有意义），没盖过的回落 `localFileLastModified`；监控路径用 `stampLocalLibraryAddedAtSong` 逐首盖章、`finishLocalLibraryAutoSync` 里 `flushLocalLibraryAddedAtMap()` 一次落盘，两处都 `typeof` 守卫因为 `tests/local-library-auto-sync.test.js` 会执行到那段。这个模块**故意插在 `function localLibraryAssetStatus(` 之前**，避开 `tests/local-lyric-cache-residency.test.js` 的切片。渲染只加 JS：根视图最上面多一张"音乐库"卡（顺序必须是 音乐库 → 特别喜欢 → 独立歌单 → 全部音乐），分类视图复用 `.local-playlist-view-head` / `.local-playlist-view-actions` / `.pl-card`，`public/app.css` 与 `public/index.html` 一行未动；`renderLocalLibraryPlaylistPanel` 的 `domSignature` 末尾必须带 `localLibraryCategoryDomSignature(selectedCategory)`，否则早退分支会吃掉重绘；分组卡片按 `playlistPanelRenderLimit` 分页并复用 `data-pl-load-more`，`localLibraryPlaylistPanelItemCount()` 同时喂给 `growPlaylistPanelRenderLimit` 与 `schedulePlaylistPanelLazyCheck`；`#pl-list` 点击链里 `data-library-kind-play` → `data-library-kind` → `data-library-back` 三个钩子必须排在 `closest('.pl-card')` 兜底之前，否则整卡会被 `openPlaylistPanelDetail` 抢走。`tests/local-library-categories.test.js` 22 例；vm 跨 realm 断言前必须 `Array.from` 复制容器，vm 里造的数组原型不同、`deepStrictEqual` 会判不等。全量回归 `652/652`。）
- 历史版本摘要：`v1.7.22`（真正的音效链 `预设 → EQ → Preamp → Limiter → Spatial → Output`。整条链常驻音频图，绝不在运行时改接线：`initAudio()` 里 `source.connect(analyser)` → `analyser.connect(replayGainNode)` → `replayGainNode.connect(audioChain.input)` → `audioChain.output.connect(gainNode)` → `gainNode.connect(audioCtx.destination)`，`audioChain = createAudioEffectChain(audioCtx)` 建在 `analyser.connect` 之前，末尾用 `applyAudioChainToNodes(true)` 把已算好的参数补位（同 `setReplayGainNodeGain(replayGainActive.linear, true)` 的理由：`attemptAudioPlay` 先跑 `playLocalQueueItem` 再 `initAudio()`，不补位第一下会漏掉音效链）。之所以能常驻不改接线：每一级都有数学上完全透明的中性值——biquad `gain=0` 是恒等、`DynamicsCompressorNode` 在 `ratio=1` 时压缩曲线退化成直线且 Blink 的补偿增益 `fullRangeGain = saturate(1.0, k)` 在 slope=1 时 `k→0` 恒等于 `1.0`（若它会补偿，关掉音效链会让所有人音量变化）、`width=1` 的中/侧矩阵逐样本精确还原 L/R，所以切开关不可能有咔哒声。已知取舍：`DynamicsCompressorNode` 恒带约 `6ms` 前置延迟，analyser 抽头因此比输出早约 `6ms`（60fps 下不到半帧，可忽略，源码里有注释）。引擎块在 `public/app.js` 里从 `var AUDIO_CHAIN_BAND_FREQUENCIES` 起、到 `function applyVolumeToAudio(` 止，**故意放在 ReplayGain 切片（`var REPLAY_GAIN_PREAMP_MIN` → `function flushVolumePreference(`）之外**，两个 vm 切片互不侵入；`tests/audio-effect-chain.test.js` 按前一对锚点切片，在这段里新增任何裸标识符都会在 vm 里抛 `ReferenceError`（被调用方的 `.catch` 吞掉 → 测试全绿但行为错），所以这段刻意不用 `Array.isArray`（改判 `typeof raw.length === 'number'`）也不用 `Infinity`（改用 `bestDelta = -1` 哨兵）。EQ 是 10 段 ISO 频段 `[31,62,125,250,500,1000,2000,4000,8000,16000]`，两端 `lowshelf`/`highshelf` 托住整个低/高频区、中间八段 `peaking` 且只给 peaking 设 `Q=1`，每段 `±12 dB` 按 `0.5 dB` 取整（`Math.round(x*2)/2`）。预设不存盘、由曲线反推：`matchAudioChainPreset(gains)` 逐条比对内置曲线（容差 `0.001`），对不上就是 `custom`，所以拖任意频段自动落到自定义、把曲线改回预设形状又会自动认回——存预设 id 会立刻和曲线脱钩。自动预增益 `autoDb = -max(gains)`（只可能 ≤0），与用户 `preampDb` 相加后夹在 `[-24, +12]`。限幅关掉时只把 `ratio` 推回 `1` 且 `thresholdDb` 归 `0`，不动接线。声场用真中/侧矩阵：`splitter` 的 0/1 都进 `mid`（`gain=0.5`），0 进 `side`、1 经 `sideInvert(-1)` 进 `side`（`gain=0.5`），`side → width → widthInvert(-1)`，`mid` 与 `width` 都并进 `merger` 输入 0、`mid` 与 `widthInvert` 都并进输入 1（ChannelMergerNode 对同一输入的多条连线求和），得到 `L=mid+w·side`、`R=mid-w·side`。**`widthInvert.gain` 必须恒为 `-1`、只自动化 `width.gain`**：`widthInvert` 是从 `width` 取信号的，两个都推 `width` 会把宽度乘成 `w²`（第一版就是这个 bug，已在源码里留注释）。参数写入统一走 `setAudioChainParam`，`immediate` 直写、否则 `AUDIO_CHAIN_RAMP_SECONDS = 0.08` 短斜坡。档案导出 `xxx.eq.json`：`{format:'mineradio.eq', version:1, name, eqEnabled, bands:[{frequency,gain}×10], preamp, autoPreamp, limiter:{enabled,thresholdDb}, spatial:{enabled,width}}`，导入必须 `format === 'mineradio.eq'` 且能解出频段，`{frequency,gain}` 按最接近频段归位、纯 dB 数组也收、缺的段补 0；文件对话框复用既有 `mineradio-export-json-file` / `mineradio-import-json-file` IPC（`desktop/preload.js`、`desktop/main.js`，`extensions:['json']`、16 MiB 上限），主进程零改动。设置存独立 `AUDIO_CHAIN_STORE_KEY = 'mineradio-audio-chain-v1'` 并登记进 `PERSISTENT_UI_STATE_KEYS`，绝不写进视觉预设 `fx`（`fx` 会随预设与用户档案导入导出，写进去等于别人的预设能改你的 EQ）；13 个新滑杆一律不进 `bindFxPanel` 的显式 `ids` 白名单，所以不可能被写成 `fx` 字段。界面只在 `public/index.html` 新增一个与既有折叠区同构的 `fx-eq-fold`（复用 `fx-fold`/`fx-toggle-grid`/`fx-toggle`/`fx-section-label`/`fx-seg`/`fx-slider`/`lyric-color-row`/`fx-mini-btn ghost`/`mini-player-collapse-hint`，`public/app.css` 一行未动），`fxPanelTargetForNode` 里加 `id === 'fx-eq-fold'` 归到 advanced；注意 `public/app.css:1416`–`1421` 是无条件规则：`.fx-fold-head` 被 `display:none`、所有 `.fx-fold-body` 恒 `display:block`，`.open` 与折叠头 `onclick` 都是历史残留，所以 `organizeFxPanel` 的强制展开清单对新区块没有视觉意义、故意没加。预设按钮排成三行 `fx-seg`（3/3/2）是因为 `.fx-seg{display:flex}` 下 `flex:1` 的按钮在 `11.5px` 字号挤 8 个会压扁，行内顺序严格保持用户给的 Normal/Rock/Pop/Classical/Jazz/Bass Boost/Vocal/自定义。`tests/audio-effect-chain.test.js` 14 例：桩 AudioContext 把每条 `connect` 记成 `id>id:out/in` 字符串逐条比对整张图，并按链路上真实写入的增益逐样本核对中/侧矩阵；vm 跨 realm 断言前必须 `Array.from` / `Object.assign({}, x)` 复制容器，vm 里的 `var` 绑定要靠 `this.setSettings` / `this.setChain` 这类导出的 mutator 才能改。全量回归 `630/630`。）；`v1.7.21`（音量均衡 ReplayGain。渲染层音频链路插入独立增益节点：`initAudio()` 里 `source.connect(analyser)` → `analyser.connect(replayGainNode)` → `replayGainNode.connect(gainNode)` → `gainNode.connect(audioCtx.destination)`，均衡节点必须排在 analyser 之后（否则可视化与节拍频谱会跟着增益忽明忽暗）、`gainNode` 之前（`gainNode.gain` 继续独占 `targetVolume` 与全部淡入淡出，`currentAudioOutputGain()` 与每个淡入淡出点语义不变）；`attemptAudioPlay` 是先跑 `playLocalQueueItem` 再 `initAudio()`，所以 `initAudio()` 末尾要用 `setReplayGainNodeGain(replayGainActive.linear, true)` 把已算好的增益补位，否则重建节点后第一下会漏掉均衡。增益引擎在 `public/app.js` 约 `20142`–`20300`，夹在 `applyVolumeToAudio` 与 `flushVolumePreference` 之间（`tests/replay-gain-normalization.test.js` 就按这两个锚点切片，中间插新函数会改变切片范围）：`resolveReplayGain(info, settings)` 是纯函数，返回 `{linear, source, gainDb, appliedDb, clipped, peak}`，`linear = 10^((gainDb + preampDb)/20)`，防削波按 `min(linear, 1/peak)` 封顶并置 `clipped`，峰值标签缺失时与 foobar2000 一致不做额外衰减，最后 `clampRange(linear, 0.05, 4)`；`mode==='album'` 缺 `albumGainDb` 时回退 `trackGainDb`、`mode==='track'` 缺 `trackGainDb` 时回退 `albumGainDb`，峰值跟着基准一起回退，两个增益都没有时 `source='none'` 保持原始电平（绝不猜一个增益）；`normalizeReplayGainSettings` 把 Preamp 夹在 ±12 dB 并按 0.1 dB 取整、`clipGuard` 缺省 `true`；`setReplayGainNodeGain(linear, immediate)` 切歌走 `immediate`、播放中改设置走 `REPLAY_GAIN_RAMP_SECONDS = 0.08` 短斜坡。标签采集全部挂在既有解析器上，没有新增 extractor：`parseFlacMetadataVorbisPayload`（FLAC 与 Ogg Vorbis/Opus/OggFLAC 共用，两处调用点，改一处覆盖全部 Vorbis comment 格式）、`parseId3v2Tags` 的 `TXXX` 分支与新增 `readId3v2Rva2MasterGain`（RVA2 增益 = 有符号 int16 BE `/512` dB，只认主音量声道 `0x01`，`bitsRepresentingPeak` 各家归一化不一致所以故意不取峰值——取错了会在防削波开启时静默衰减）、`parseApeTagItems`、`parseM4aIlst` 新增 `----` 分支加 `readM4aFreeformName`（`name` atom 的 payload 前 4 字节是 version/flags）；`replayGainTagField` 把键名去掉空格/下划线/点再大写比对，`R128_TRACK_GAIN` / `R128_ALBUM_GAIN` 按 Q7.8 `/256` 再补 `REPLAY_GAIN_R128_REFERENCE_OFFSET_DB = 5`（`-23 LUFS` → `-18 LUFS`），`iTunNORM` 的响度参考不同故意返回 `''`；`putReplayGainTag` 用 `hasOwnProperty` 实现「首个可解析值胜出」，所以真实 `REPLAYGAIN_*` 压过同文件里的 R128 折算值；`finalizeLocalMetadataReplayGain` 在 `extractLocalMetadataTags` 出口统一归一化，`_mineradioScanComplete === false`（轻量扫描没读到 moov）时整块丢掉而不是写半截错增益。持久化刻意绕开 `LOCAL_METADATA_VALUE_FIELDS`（它的真值判定会把合法的 `0 dB` 增益丢掉）：`applyLocalMetadataTags` 只用两行内联赋值交接（该函数落在多个 `node:vm` 测试切片里，加新标识符会 `ReferenceError`），且不计入 `changed`、避免为音量数据触发列表重绘；`localAssetCacheSnapshot` / `applyLocalAssetCacheToSong` 走 `assets.extra` JSON 列（`mergeExtraFields` 对嵌套对象无损往返，无需数据库迁移、无需升 `LOCAL_METADATA_TAG_SCHEMA`，老记录不会整库回落重解析）；老缓存没有该字段时由 `prepareReplayGainForSong(song, token)` → `ensureLocalReplayGainForSong` 在首次播放惰性补齐一次并 `scheduleLocalAssetCacheWrite`，`trackSwitchToken` 加 `isCurrentLocalQueueSong` 丢弃过期结果，确认无标签的歌置 `localReplayGainResolved` 不再重扫。设置存 `mineradio-replay-gain-v1`（已在 `PERSISTENT_UI_STATE_KEYS`）而不是 `fx`，否则会被视觉预设与用户存档导入导出带走；`rg-preamp` 不在 `bindFxPanel` 的滑杆白名单里所以永远不会写进 `fx`，`fxPanelTargetForNode` 与 `relabelFxPanelControls` 各加一处 `fx-volume-fold` 让新折叠区归到「高级」页并保持输出等价。`public/index.html` 只在 `fx-playback-fold` 之后新增一个同构折叠区（复用 `fx-fold` / `fx-toggle-grid` / `fx-toggle` / `fx-section-label` / `fx-seg` / `fx-slider` / `mini-player-collapse-hint` 既有类），`public/app.css` 一行未动。新增 `tests/replay-gain-tag-parsing.test.js`（10 例，自建 FLAC / ID3v2 / RIFF / APEv2 / MP4 真实字节夹具，覆盖 R128 折算、RVA2 主声道、`iTunNORM` 排除与轻量扫描未读全）与 `tests/replay-gain-normalization.test.js`（12 例，`node:vm` 跑真实增益实现，最后一例用源码正则钉死音频链路顺序、存档键与界面入口）；`tests/auto-playback-startup.test.js` 的两条正则按新增折叠区放宽（仍钉死 `fx-playback-fold` 在 relabel 列表里、`initAutoPlaybackControls()` 紧接启动恢复）。`616/616` 全绿，UI 零改动）。；`v1.7.20`（音乐文件夹自动监控。新增 `desktop/local-library-watcher.js`：`createLocalLibraryWatcher({ fs, setTimer, clearTimer, pathLimit, isTrackedPath, onFlush, onStatusChange })` → `{ setRoots, getStatus, flushNow, close, isClosed }`，`fs.watch(root, { recursive: true, persistent: false })` + `unref()`（不 unref 会被 watcher 吊住退出），防抖 `900ms` 与最长等待 `4500ms` 双阀门（每条事件重排防抖、**不**重排最长等待，所以长时间拷贝也能中途上报），递归不支持的平台降级成 `recursive: false` 只看根目录一层，`EPERM` / `ENOENT`（权限拒绝、移动硬盘掉线）按 `5000ms → 60000ms` 指数退避重试、恢复后自动接回；无扩展名的相对路径（目录改名）按整根粗粒度变更处理，文件名缺失或积压超 `512` 条置 `overflow` 让渲染层退回整库比对；`normalizeWatchRoots` 去重并丢掉被父目录递归覆盖的子目录，`isWatchRootInside` 只在分隔符边界判定（`E:\Music2` 不属于 `E:\Music`）且两者相等时返回 `false`。主进程侧 `applyLocalLibraryWatchRoots` 逐个根走 `rememberLocalMusicRoot` 授权并原样回报 `rejected`（否则渲染层能借监控读任意目录），两个 IPC `mineradio-local-library-watch-set-roots` / `mineradio-local-library-watch-status` 由 `trustedMainFrameHandler` 包裹，变更经 `broadcastLocalLibraryWatchChange` 推 `mineradio-local-library-watch-changed`，退出时 `closeLocalLibraryWatcher()` 必须排在 `closeLocalLibraryStore()` 之前。渲染层 `applyOwnedLocalLibraryRefresh` 在有播放队列时不再弹「下次启动会自动同步」然后作废本轮结果，改走 `applyLocalLibraryAutoSync`：`localLibraryAutoSyncDiff` 按 `pathKey` 认人、按 `pathKey|size|mtime` 判断是否被改过（所以改标签换封面只落进 `changed`，不会被当成「删一首再加一首」），`localLibrarySongs` 只做原地增删（`playbackSource === 'library'` 时它与 `playQueue` 是同一个数组，换数组会让全部 `!==` 归属判定失效），改动过的歌只改字段不换对象（`currentLocalSong` / `playQueue[currentIdx]` / 迷你播放器 / 桌面歌词握着的引用继续有效），正在播放那首即使文件被删也原位保留、`currentIdx` 按 `indexOf` 重算并夹紧；接管一首时把 `customCoverMap` 从 `local:<旧 localKey>` 迁到新键（`localKey = 绝对路径 + ':' + 大小 + ':' + 修改时间`，不迁移用户手挑的封面就会因一次改标签变成孤儿）、清空 `localUrl`（否则 `ensureLocalSongUrl` 会继续返回旧 blob）、旧 blob 交既有 `revokeDiscardedLocalSongObjectUrls` 回收但跳过正在播放那首（它的 blob 还是 `audio.src`）、标签被删时 `album` / `albumArtist` / `genre` / `trackNumber` / `year` 如实清空而 `duration` 故意保留（避免闪 `0:00`）、`local*Promise` / `local*Loading` 显示有在途解析时本轮不接管、留到下一轮以免过期结果写不回去。搜索框下方指示器 `已同步 N 首歌曲`：`reportLocalLibrarySyncedCount` 懒建 `#local-sync-badge` 并挂进 `#search-stack`（取不到那一叠才退回 `body`），`position:absolute` 贴着那一叠的底边（`top:100%` + `margin-top:8px`，与 `#search-results` 同一个间距：搜索框下面没东西时就落在搜索框正下方，结果列表一开就顺到列表下面，压不到它上面。**别按 `.search-mode-tabs` 的行高算死坐标** —— `#search-mode-tabs` 在 `body.local-only-mode` 下是 `display:none` 的，而 `LOCAL_ONLY_MODE` 恒为 `true`，那条坐标正好落在结果列表第一行上），千分位分组、`4200ms` 自动淡出、`role=status` / `aria-live=polite` / `pointer-events: none`。**顶部搜索区是「鼠标 `y<66` 才探头」的**，指示器挂在它里面，所以显示期间由 `holdLocalLibrarySyncBadgePeek` 把 `.peek` 按住、淡出时再放开（用户自己划开的不接管也不替他关，输入框有焦点、`#search-results.show` 或 `#upload-tip.show` 时同样不关；沉浸模式下 `setPeek` 本来就拒绝开搜索区，所以叫完要回读 `.peek` 再决定记不记按住状态；放开时还要看那条 mousemove 写进 `localLibrarySyncBadgeSearchZoneHot` 的留住区标记和 `emptyHomeActive`——鼠标正停在顶部却被收掉，下一次 mousemove 之前就一直是收着的），`public/app.js` 那条 mousemove 收起分支里 `!(typeof localLibrarySyncBadgeHoldingPeek === 'function' && localLibrarySyncBadgeHoldingPeek())` 的豁免删掉就会让它一闪就没；配色只走令牌通道 `--th-search-bg` → `--th-chip-bg` → `--saved-panel-glass-bg` 三级回落、描边 `--th-chip-border` → `transparent`、文字 `--th-text-strong`、语义色只留在那颗 `--fc-accent-rgb` 圆点上，`html.control-glass-svg-ok #local-sync-badge` 单独跟邻居升级到同一支玻璃 SVG 滤镜（没去动 `#search-box,#search-results,.search-mode-tabs,…` 那两条长选择器列表），`public/index.html` 一行未动。顺手修掉 `.ape` / `.dsf` 一直在 `LOCAL_LIBRARY_EXTS` 里却漏在 `LOCAL_LIBRARY_AUDIO_EXTS` 外、导致 APE / DSD 被扫到但不计入 SQLite 的 audio 计数与曲库签名（即指示器上那个数字）的老问题。渲染层曲库仍是单根（`LOCAL_LIBRARY_FOLDER_STORE_KEY` 只存一个标量路径），watcher 模块、IPC 与主进程接线已按多根设计，真要合并多个根需要重写快照/索引/hydrate/扫描全链路并新增文件夹列表设置面板，本轮按 `能不动 UI 就不动 UI` 未做。新增 `tests/local-library-watcher.test.js`（19 例，注入 `fs` 与定时器桩）、`tests/local-library-auto-sync.test.js`（12 例，`node:vm` 跑 `public/app.js` 真实实现；vm 跨 realm，断言前必须 `Array.from` / `Object.assign({}, x)` 拷回本 realm）、`tests/local-library-watch-wiring.test.js`（5 例，钉死扩展名集合与主进程/preload 接线），`594/594` 全绿，UI 零改动）。；`v1.7.19`（新增 Ogg Vorbis / Ogg Opus / Ogg FLAC、WAV（含 RF64 / BW64）、APE、DSF 的本地标签、封面、内嵌歌词与时长解析。解析实现全部落在 `public/app.js` 的 `base64ChunksToBytes` ~ `applyLocalMetadataTags` 这一段里，自成闭包，由 `extractLocalMetadataTags` / `extractEmbeddedCoverSource` / 新增 `extractEmbeddedLyricsText` 三个分发器按扩展名路由，能力门 `canReadEmbeddedLyrics` / `canReadEmbeddedCover` 与两个 `canReadTruncatable*` 同步放开（都不含 mp3，MP3 按完整标签长度读、失败即真的没有）；Ogg FLAC 拿到 STREAMINFO 的 `totalSamples` 就直接算时长、**不读尾部**，Vorbis / Opus 才回读末 `64KB` 反向找同 serial 且 `granule > 0` 的页，Opus 还要扣 `pre-skip`；`readId3v2TagBytes` 的 `256KB` 探针语义（探针覆盖整段标签就 `subarray` 复用、超出才发第二次 Range 读）由 MP3、WAV 的 `id3 ` chunk、APE 文件头 ID3v2、DSF 尾部 ID3v2 共用；超预算（后台 `4MB` 的 `LOCAL_ASSET_LIGHT_SCAN_BYTES` / 前台 `24MB` 的 `LOCAL_MAX_TAG_BYTES`）统一置 `_mineradioScanComplete = false` 让前台完整重试，不返回半截标签。播放侧 `desktop/audio/wav-stream.js` 把 APE / DSF 包成「虚拟 WAV」——size 可精确算、任意区间可解码，`/api/local-file` 的 Range / 416 / `raw=1` 全部保留；`desktop/audio/ape-decoder.js` 支持 3800–3990，`desktop/audio/dsf-decoder.js` 走字节查表 FIR 抽取，两者都只接受 `read(offset, length)`、不碰 fs。**授权：`ape-decoder.js` 是 FFmpeg `libavformat/ape.c` + `libavcodec/apedec.c` 的逐行移植，属 `LGPL-2.1-or-later`**，按 LGPL v2.1 第 3 条在本项目内以 `GPL-3.0` 分发，声明写在新增的根目录 `THIRD-PARTY-NOTICES.md`（该文件与 `LICENSE` 已加入 `build.files`）；DST 压缩的 `.dff` 明确不在范围内。新增 `tests/local-format-tag-parsing.test.js`（18 例真实字节夹具），`558/558` 全绿，UI 零改动）；`v1.7.18`（本地曲库落到 `node:sqlite`：`desktop/local-library-store.js` 用「路径键 + 大小 + 修改时间」文件指纹 + 路径索引常驻 `local-library.db`，保存歌曲 ID、路径、大小、修改时间、时长、格式、Artist/Album/Genre/Year、封面与歌词缓存、播放次数、最近播放、收藏状态；扫描先问数据库走增量、索引按行摘要增量回写、解除历史 `16000` 条截断；`(root_id, song_key)` / `(root_id, fingerprint)` 组合索引修掉整根扫描的查询计划陷阱（两万行 `295,319ms` → `1,640ms`），已用 `EXPLAIN QUERY PLAN` 钉死；`localStorage` 仍是听歌统计与「特别喜欢」的唯一权威，数据库只做无回读的持久镜像，UI 零改动；缺 `node:sqlite` 时整层一次性 latch 降级回 IndexedDB。`540/540` 全绿）；`v1.7.17`（修复标准迷你播放器收回态封面命中后立即右键仍落到桌面的异步竞态；优先同步解除原生穿透；两套外壳固定 `no-drag`，拖动走独立 IPC，系统右键仅作可信主 frame 兜底）；`v1.7.16`（本版固定迷你播放器右键命中边界：标准收回态只有封面热区（外扩 `6px`）可点可右键，透明空白交还桌面；标准展开态与极简外壳整窗可右键；封面拖动首个增量、换边连续、跨显示器移动与展开方向持久化已修复；迷你页面 URL、frame、preload 与特权 IPC 信任边界已加固；极简模式停止封面和脉冲负载传输，失败穿透 IPC 可重试，原生菜单设置与异步主题状态保持同步。`v1.7.15` 将深海微光、暗焰余晖、冷杉夜雾从纯背景包升级为蓝青/熔岩红/冷杉绿三份完整主题，并新增浅底深字的雪昼白；六份内置主题默认不启用、保持互斥，用户自定义背景与取色器强调色优先级不变；`v1.7.14` 把标准迷你播放器收回态的穿透判据从 `pointerInsideWindow` 改回 `pointerInCoverHotRegion`：收回只是 CSS 把面板收成 `54 × 54` 封面、OS 窗口一直是 `360 × 84`，`v1.7.12` 那版「指针进入窗口就整窗命中」会让封面以外那截看不见的窗体吞掉桌面点击与桌面右键；现在只有封面热区（外扩 `6px`）参与命中，其余交还桌面，`pointerInsideWindow` 变量整体删除。由此右键语义固定为「收回态只有封面能弹应用菜单、空白处弹桌面菜单；展开态整窗可右键；极简无收回态与穿透故整窗恒可右键」，**该取舍由用户拍定，不要再翻回整窗命中**；`tests/mini-player-visual.test.js` 两条穿透用例改回热区时序，483 例全绿；`v1.7.13` 把迷你播放器右键菜单和任务栏托盘统一到同一份模板 `buildAppContextMenuTemplate()`：菜单项从 3 项扩成托盘那 6 项（显示 Mineradio / 关闭按钮最小化到托盘 / 最小化时显示迷你播放器 / 迷你播放器样式 / 开机自动启动 / 分隔线 / 退出 Mineradio），勾选态实时读真实设置、改完立刻 `refreshTrayMenu()`，`main.js` 里 `Menu.buildFromTemplate` 只剩托盘和迷你两处调用且都传这一份模板；`-webkit-app-region: drag` 区域的右键在 Windows 上被当成非客户区、弹的是几乎全灰的窗口系统菜单（迷你窗口 `resizable/minimizable/maximizable` 全 false，只剩「关闭」可点）而 renderer 连 `contextmenu` 都收不到，现由窗口侧 `win.on('system-context-menu')` + `preventDefault()` 换成同一份应用菜单，非拖拽区仍走 `webContents.on('context-menu')`，`popup({ window: win })` 不传 x/y 以沿用「当前光标位置」默认值；两套外壳整块 `.mini-shell` 都是拖拽区又共用同一个窗口工厂，所以极简同样整窗任意位置可右键；`tests/mini-player-context-menu.test.js` 扩到 6 例，直接在 `vm` 里执行模板并逐项点击（vm 跨 realm，断言数组必须先 `Array.from` 拷回本 realm），483 例全绿；`v1.7.12` 让迷你播放器收回态整窗可右键：旧穿透规则「收回后只有封面热区参与命中、其余交还桌面」是 `v1.7.11` 右键菜单只能在封面上弹出的根因；现改为「指针在窗口内 = 整窗可交互，离开窗口 = 立即恢复穿透」——转发/真实 `mousemove` 只在指针位于窗口上时到达，收到即置 `pointerInsideWindow`，`document mouseleave` 清零在窗与热区标记并恢复穿透；封面热区（外扩 `6px`）仍单独负责悬停展开触发与收回排定；极简外壳无穿透天然整窗可右键；穿透行为约定已重写进 `.context/conventions/mineradio-mini-player-collapse.md`，`tests/mini-player-visual.test.js` 两条穿透用例改写为新时序，480 例全绿；`v1.7.11` 给迷你播放器加右键原生菜单：主进程 `createMiniPlayerWindow` 里 `webContents.on('context-menu')` 统一接管 + `preventDefault`，`showMiniPlayerContextMenu(win)` 弹出与托盘一致的 `显示播放器`（`focusMainWindow()` 同恢复路径）/ `迷你播放器样式` 子菜单（标准/极简 radio → `setMiniPlayerMode`）/ `退出播放器`（`appQuitting = true` + `app.quit()` 同托盘路径），标准与极简共用窗口工厂天然覆盖，收回态穿透期间右键落不到窗口；`tests/mini-player-context-menu.test.js` 3 例，约定文档补右键菜单条目，480 例全绿；`v1.7.10` 修搜索结果面板入场生硬：`#search-results` 原是 `display:none → block` 硬切无过渡，用户反馈「点击搜索界面出现太僵硬」；`.show` 现挂 `search-results-in` 入场动画 `260ms` `opacity 0→1` + `translateY(-8px)→0`，缓动 `cubic-bezier(.2,.7,.2,1)` 与搜索区下滑同族；只动画 `transform`/`opacity` 合成器属性，玻璃 `backdrop-filter:blur(40px) saturate(1.4)` 与配色零改动；`.show` 重复添加不重播，`prefers-reduced-motion: reduce` 跳过，关闭侧保持即时隐藏；`tests/search-results-entrance.test.js` 3 例钉死 display:none / 玻璃滤镜 / 关键帧属性 / reduced-motion；`v1.7.9` 修掉一次纯空转的启动开销：`migratePrimaryProfileState()` 在打包版每次启动、主窗口创建前都会「读出 `localStorage` 再原样写回」，各开一个隐藏 `BrowserWindow` 拦截 `http` 做导航 + `executeJavaScript` + `flushStorageData`，微基准实测一趟写 `~113ms` / 读 `~20ms+`，稳态下结果与现状相同；现在新增 `ui-state-origin-marker.json`（临时文件 + `rename` 原子落盘）记录上次完整迁移的目标 `origin`，「旧档迁移已完成 + 端口没变」即短路跳过全部探测，端口变化或旧档待迁移仍走完整路径，`preload` 的 `restorePersistentUiState()` 文件兜底不变；`tests/profile-state-migration.test.js` 守卫短路必须在隐藏窗口读取之前、旧档待办不得短路、收尾必须记录标记；474 例全绿；`v1.7.8` 做启动首帧优化：`three.js`（603KB）/ `gsap.min.js`（73KB）两个阻塞脚本从 `<head>` 挪到 `</body>` 前、`app.js` 之前，脚本相对执行顺序不变（`app.js` 顶层就 `new THREE.Scene()`，vendor 必须先求值），head 不再被阻塞、HTML/CSS 先解析完；`server.js` 的 `/vendor/*` 静态缓存头从 `no-cache` 放宽为 `public, max-age=604800`（vendor 随安装包版本走、不单独热替换，7 天新鲜期内免 304 重验证），其余静态文件仍 `no-cache`；`tests/frontend-html-script-syntax.test.js` 新增「head 零外链脚本 + vendor 先于 app.js」顺序断言，`tests/static-resource-cache.test.js` 的 `loadServeStatic` 切片标记跟着新签名 `serveStatic(req, res, filePath, cacheControl)` 更新并补 vendor 缓存头断言；默认外观与交互零改动；`v1.7.7` 把 `v1.7.6` 那段看不出变化的收缩过渡修成看得见的一段动作：交接延时 `150ms` → `240ms`、`scale` 终态 `.86` → `.6`、`transform` 改 `cubic-bezier(.3,0,.8,.15)` 加速进入、淡出 `.17s` 提前到 `190ms` 收尾，实测外壳在 `~222ms` 已经全透明，`240ms` 交接时窗口里没有可见内容（`html` / `body` 本身透明），系统最小化动画不再盖住过渡；最大化态收缩补 `26px` 圆角；`transform-origin` 改为过渡期间只缓存、收尾复位时才落地，消除预热 IPC 回包造成的中途掉头；迷你播放器入场加大到 `280ms` / `260ms`；`tests/mini-player-collapse-transition.test.js` 15 例，钉死「淡出与缩放必须在交接前至少 `30ms` 结束」与「终态 `scale ≤ .7`」；`v1.7.6` 给主界面收缩到迷你播放器加了连续过渡：`collapseToMiniPlayer()` 先经 `mineradio-mini-player-prepare-transition` 预热迷你窗口并取回归一化收缩原点，主界面朝迷你角落收拢后才交给系统最小化，悬停最小化按钮即开始预热、`2600ms` 未进入迷你模式则丢弃，迷你播放器两套外壳改为窗口显示且首帧状态到位后再淡入；默认外观与布局零改动；`v1.7.5` 新增 `--th-bg-color` / `--th-bg-tint` / `--th-bg-tint-opacity`，内置主题扩为两份完整主题 + 三份纯背景主题，用户自定义背景仍优先；`v1.7.4` 起插件系统收窄成只有主题一种：`source` / `playlist` 两类连同 `search` / `url` / `lyric` / `cover` / `playlists` / `playlistDetail` 六个钩子、`mineradio.request` / `requestJson`、`@host` 白名单与 `PLUGIN_NO_HOST` 等错误码、根目录 `plugin-proxy.js` 与 `/api/plugin/stream`、搜索结果插件区与插件歌单面板全部删除，插件从此没有任何网络与播放通道；`.js` 脚本主题与 Worker 沙箱保留，但只认 `theme` 一个钩子，返回值走与声明式负载完全相同的清洗；旧存档里的 `source` / `playlist` 记录在 `normalizePluginRecords()` 阶段整条丢弃；`tests/plugin-proxy.test.js` 删除，`tests/plugin-system.test.js` 重写成 24 例并正向断言这些能力已经不存在）；`v1.7.3`（主题变量族 `--th-*` 就地变量化 `public/app.css` 的 `!important` 字面值，主题覆盖面 18 → 63 处/79 探针，默认外观零变化；迷你播放器两套外壳接 `--th-mini-*`，由 `plugin-runtime.js` 的 `themeVars()` → `desktop/main.js` 签名去重 → `mineradio-mini-player-state` 整表转发，主进程侧 `normalizeMiniPlayerThemeVars()` 二次清洗；三份示例主题各 55 变量、抬到 `1.4.0`）；`v1.7.2`（安装包自带 `午夜靛蓝` / `暖琥珀` 两份声明式主题，见 `public/plugin-builtin-themes.js`，默认不启用；主题插件改为互斥，同一时刻只允许一个主题启用，历史多启用存档在 `init()` 时收敛成最近启用的那个；移除两份档案馆示例脚本插件，`source`/`playlist`/`lyric` 三类能力本身不变）；`v1.7.1` 修复了 `v1.7.0` 安装包因 `plugin-proxy.js` 漏进 `build.files` 白名单无法启动的问题，并新增 `tests/packaging-file-whitelist.test.js` 守卫；在 `v1.6.3` 基础上新增插件系统（当时是主题 / 音源 / 歌单三类，Worker 能力沙箱 + `@host` 白名单 + 本地 SSRF 防护代理，后两类与全部出网能力已在 `v1.7.4` 移除，见 `.context/architecture/mineradio-plugin-system.md` 与 `docs/PLUGIN_AUTHORING.md`）；保留壁纸模式入口与「壁纸展示位置」切换（`桌面壁纸层` / `播放器背景板`，共用 `public/wallpaper-effect.js`）、MP2、M4B、AIF/AIFF/AIFC 本地音频识别、扫描和代理 MIME 支持、标准迷你播放器隐藏播放时低平滑分析器回退、`0.00 ~ 3.00` 独立强度、开关、窗口几何上限、软件内更新线路选择与取消更新、Wallpaper Engine 生命周期、主窗口导航/IPC 信任边界、本地文件授权、本地歌单、全屏与用户数据迁移能力。`v1.7.15` 将深海微光、暗焰余晖、冷杉夜雾从纯背景包升级为蓝青/熔岩红/冷杉绿三份完整主题，并新增浅底深字的雪昼白；六份内置主题默认不启用、保持互斥，用户自定义背景与取色器强调色优先级不变；`v1.7.14` 把标准迷你播放器收回态的穿透判据从 `pointerInsideWindow` 改回 `pointerInCoverHotRegion`：收回只是 CSS 把面板收成 `54 × 54` 封面、OS 窗口一直是 `360 × 84`，`v1.7.12` 那版「指针进入窗口就整窗命中」会让封面以外那截看不见的窗体吞掉桌面点击与桌面右键；现在只有封面热区（外扩 `6px`）参与命中，其余交还桌面，`pointerInsideWindow` 变量整体删除。由此右键语义固定为「收回态只有封面能弹应用菜单、空白处弹桌面菜单；展开态整窗可右键；极简无收回态与穿透故整窗恒可右键」，**该取舍由用户拍定，不要再翻回整窗命中**；`tests/mini-player-visual.test.js` 两条穿透用例改回热区时序，483 例全绿；`v1.7.13` 把迷你播放器右键菜单和任务栏托盘统一到同一份模板 `buildAppContextMenuTemplate()`：菜单项从 3 项扩成托盘那 6 项（显示 Mineradio / 关闭按钮最小化到托盘 / 最小化时显示迷你播放器 / 迷你播放器样式 / 开机自动启动 / 分隔线 / 退出 Mineradio），勾选态实时读真实设置、改完立刻 `refreshTrayMenu()`，`main.js` 里 `Menu.buildFromTemplate` 只剩托盘和迷你两处调用且都传这一份模板；`-webkit-app-region: drag` 区域的右键在 Windows 上被当成非客户区、弹的是几乎全灰的窗口系统菜单（迷你窗口 `resizable/minimizable/maximizable` 全 false，只剩「关闭」可点）而 renderer 连 `contextmenu` 都收不到，现由窗口侧 `win.on('system-context-menu')` + `preventDefault()` 换成同一份应用菜单，非拖拽区仍走 `webContents.on('context-menu')`，`popup({ window: win })` 不传 x/y 以沿用「当前光标位置」默认值；两套外壳整块 `.mini-shell` 都是拖拽区又共用同一个窗口工厂，所以极简同样整窗任意位置可右键；`tests/mini-player-context-menu.test.js` 扩到 6 例，直接在 `vm` 里执行模板并逐项点击（vm 跨 realm，断言数组必须先 `Array.from` 拷回本 realm），483 例全绿；`v1.7.12` 让迷你播放器收回态整窗可右键：旧穿透规则「收回后只有封面热区参与命中、其余交还桌面」是 `v1.7.11` 右键菜单只能在封面上弹出的根因；现改为「指针在窗口内 = 整窗可交互，离开窗口 = 立即恢复穿透」——转发/真实 `mousemove` 只在指针位于窗口上时到达，收到即置 `pointerInsideWindow`，`document mouseleave` 清零在窗与热区标记并恢复穿透；封面热区（外扩 `6px`）仍单独负责悬停展开触发与收回排定；极简外壳无穿透天然整窗可右键；穿透行为约定已重写进 `.context/conventions/mineradio-mini-player-collapse.md`，`tests/mini-player-visual.test.js` 两条穿透用例改写为新时序，480 例全绿；`v1.7.11` 给迷你播放器加右键原生菜单：主进程 `createMiniPlayerWindow` 里 `webContents.on('context-menu')` 统一接管 + `preventDefault`，`showMiniPlayerContextMenu(win)` 弹出与托盘一致的 `显示播放器`（`focusMainWindow()` 同恢复路径）/ `迷你播放器样式` 子菜单（标准/极简 radio → `setMiniPlayerMode`）/ `退出播放器`（`appQuitting = true` + `app.quit()` 同托盘路径），标准与极简共用窗口工厂天然覆盖，收回态穿透期间右键落不到窗口；`tests/mini-player-context-menu.test.js` 3 例，约定文档补右键菜单条目，480 例全绿；`v1.7.10` 修搜索结果面板入场生硬：`#search-results` 原是 `display:none → block` 硬切无过渡，用户反馈「点击搜索界面出现太僵硬」；`.show` 现挂 `search-results-in` 入场动画 `260ms` `opacity 0→1` + `translateY(-8px)→0`，缓动 `cubic-bezier(.2,.7,.2,1)` 与搜索区下滑同族；只动画 `transform`/`opacity` 合成器属性，玻璃 `backdrop-filter:blur(40px) saturate(1.4)` 与配色零改动；`.show` 重复添加不重播，`prefers-reduced-motion: reduce` 跳过，关闭侧保持即时隐藏；`tests/search-results-entrance.test.js` 3 例钉死 display:none / 玻璃滤镜 / 关键帧属性 / reduced-motion；`v1.7.9` 修掉一次纯空转的启动开销：`migratePrimaryProfileState()` 在打包版每次启动、主窗口创建前都会「读出 `localStorage` 再原样写回」，各开一个隐藏 `BrowserWindow` 拦截 `http` 做导航 + `executeJavaScript` + `flushStorageData`，微基准实测一趟写 `~113ms` / 读 `~20ms+`，稳态下结果与现状相同；现在新增 `ui-state-origin-marker.json`（临时文件 + `rename` 原子落盘）记录上次完整迁移的目标 `origin`，「旧档迁移已完成 + 端口没变」即短路跳过全部探测，端口变化或旧档待迁移仍走完整路径，`preload` 的 `restorePersistentUiState()` 文件兜底不变；`tests/profile-state-migration.test.js` 守卫短路必须在隐藏窗口读取之前、旧档待办不得短路、收尾必须记录标记；474 例全绿；`v1.7.8` 做启动首帧优化：`three.js`（603KB）/ `gsap.min.js`（73KB）两个阻塞脚本从 `<head>` 挪到 `</body>` 前、`app.js` 之前，脚本相对执行顺序不变（`app.js` 顶层就 `new THREE.Scene()`，vendor 必须先求值），head 不再被阻塞、HTML/CSS 先解析完；`server.js` 的 `/vendor/*` 静态缓存头从 `no-cache` 放宽为 `public, max-age=604800`（vendor 随安装包版本走、不单独热替换，7 天新鲜期内免 304 重验证），其余静态文件仍 `no-cache`；`tests/frontend-html-script-syntax.test.js` 新增「head 零外链脚本 + vendor 先于 app.js」顺序断言，`tests/static-resource-cache.test.js` 的 `loadServeStatic` 切片标记跟着新签名 `serveStatic(req, res, filePath, cacheControl)` 更新并补 vendor 缓存头断言；默认外观与交互零改动；`v1.7.7` 把 `v1.7.6` 那段看不出变化的收缩过渡修成看得见的一段动作：交接延时 `150ms` → `240ms`、`scale` 终态 `.86` → `.6`、`transform` 改 `cubic-bezier(.3,0,.8,.15)` 加速进入、淡出 `.17s` 提前到 `190ms` 收尾，实测外壳在 `~222ms` 已经全透明，`240ms` 交接时窗口里没有可见内容（`html` / `body` 本身透明），系统最小化动画不再盖住过渡；最大化态收缩补 `26px` 圆角；`transform-origin` 改为过渡期间只缓存、收尾复位时才落地，消除预热 IPC 回包造成的中途掉头；迷你播放器入场加大到 `280ms` / `260ms`；`tests/mini-player-collapse-transition.test.js` 15 例，钉死「淡出与缩放必须在交接前至少 `30ms` 结束」与「终态 `scale ≤ .7`」；`v1.7.6` 给主界面收缩到迷你播放器加了连续过渡：`collapseToMiniPlayer()` 先经 `mineradio-mini-player-prepare-transition` 预热迷你窗口并取回归一化收缩原点，主界面朝迷你角落收拢后才交给系统最小化，悬停最小化按钮即开始预热、`2600ms` 未进入迷你模式则丢弃，迷你播放器两套外壳改为窗口显示且首帧状态到位后再淡入；默认外观与布局零改动；`v1.7.5` 新增 `--th-bg-color` / `--th-bg-tint` / `--th-bg-tint-opacity`，内置主题扩为两份完整主题 + 三份纯背景主题，用户自定义背景仍优先；`v1.7.4` 起插件系统收窄成只有主题一种：`source` / `playlist` 两类连同 `search` / `url` / `lyric` / `cover` / `playlists` / `playlistDetail` 六个钩子、`mineradio.request` / `requestJson`、`@host` 白名单与 `PLUGIN_NO_HOST` 等错误码、根目录 `plugin-proxy.js` 与 `/api/plugin/stream`、搜索结果插件区与插件歌单面板全部删除，插件从此没有任何网络与播放通道；`.js` 脚本主题与 Worker 沙箱保留，但只认 `theme` 一个钩子，返回值走与声明式负载完全相同的清洗；旧存档里的 `source` / `playlist` 记录在 `normalizePluginRecords()` 阶段整条丢弃；`tests/plugin-proxy.test.js` 删除，`tests/plugin-system.test.js` 重写成 24 例并正向断言这些能力已经不存在）；`v1.7.3`（主题变量族 `--th-*` 就地变量化 `public/app.css` 的 `!important` 字面值，主题覆盖面 18 → 63 处/79 探针，默认外观零变化；迷你播放器两套外壳接 `--th-mini-*`，由 `plugin-runtime.js` 的 `themeVars()` → `desktop/main.js` 签名去重 → `mineradio-mini-player-state` 整表转发，主进程侧 `normalizeMiniPlayerThemeVars()` 二次清洗；三份示例主题各 55 变量、抬到 `1.4.0`）；`v1.7.2`（安装包自带 `午夜靛蓝` / `暖琥珀` 两份声明式主题，见 `public/plugin-builtin-themes.js`，默认不启用；主题插件改为互斥，同一时刻只允许一个主题启用，历史多启用存档在 `init()` 时收敛成最近启用的那个；移除两份档案馆示例脚本插件，`source`/`playlist`/`lyric` 三类能力本身不变）；`v1.7.1` 修复了 `v1.7.0` 安装包因 `plugin-proxy.js` 漏进 `build.files` 白名单无法启动的问题，并新增 `tests/packaging-file-whitelist.test.js` 守卫；在 `v1.6.3` 基础上新增插件系统（当时是主题 / 音源 / 歌单三类，Worker 能力沙箱 + `@host` 白名单 + 本地 SSRF 防护代理，后两类与全部出网能力已在 `v1.7.4` 移除，见 `.context/architecture/mineradio-plugin-system.md` 与 `docs/PLUGIN_AUTHORING.md`）；保留壁纸模式入口与「壁纸展示位置」切换（`桌面壁纸层` / `播放器背景板`，共用 `public/wallpaper-effect.js`）、MP2、M4B、AIF/AIFF/AIFC 本地音频识别、扫描和代理 MIME 支持、标准迷你播放器隐藏播放时低平滑分析器回退、`0.00 ~ 3.00` 独立强度、开关、窗口几何上限、软件内更新线路选择与取消更新、Wallpaper Engine 生命周期、主窗口导航/IPC 信任边界、本地文件授权、本地歌单、全屏与用户数据迁移能力。
- 统一备份区：`E:\桌面\播放器软件\工作区备份`

## Start Every New Codex Thread Here

新对话开始处理 Mineradio 前，必须先确认当前目录是：

```powershell
C:\Users\Administrator\Desktop\Mineradio-main
```

然后读这些文件：

- `AGENTS.md`
- `docs/PROJECT_MEMORY.md`
- 涉及玻璃 SVG 质感时读取 `docs/GLASS_SVG_TEXTURE.md`
- 涉及发布时读取 `CHANGELOG.md`、`RELEASE.md`、`package.json`

## Repository Layout

```text
Mineradio/resources/app/
├─ public/
│  ├─ index.html        # 主 UI、CSS、歌词、粒子、3D 歌单架、视觉控制台
│  └─ vendor/           # 本地 vendor 依赖
├─ desktop/             # Electron main/preload
├─ build/               # 打包资源和 installer 脚本
├─ docs/                # 项目记忆、设计偏好、长期约束
├─ server.js            # 本地 API、音乐源、更新检查、补丁应用
├─ dj-analyzer.js       # 节奏/音频分析
├─ package.json         # 版本号、构建命令、electron-builder 配置
└─ CHANGELOG.md         # 中文更新说明优先写在顶部
```

## Commands

```powershell
npm start
node --check server.js
npm run build:win:dir
npm run build:win
```

前端主逻辑在 `public/index.html`。当前环境以 `C:\Users\Administrator\Desktop\Mineradio-main` 为可写源码仓库；旧 `E:\桌面\播放器软件\Mineradio\resources\app` 在本环境不存在。改动后至少做：

注意：运行版 `node_modules` 可能只包含运行依赖。如果发布打包时缺少 `electron-builder`，先在当前源码仓库执行 `npm install`，再执行 `npm run build:win`。

```powershell
git diff --check
node --check server.js
```

并用实际 Electron 或浏览器检查关键交互。

## Release Workflow

发布新版本时：

1. 更新 `package.json` 和 `package-lock.json` 版本号。
2. 更新 `CHANGELOG.md` 顶部中文说明。
3. 运行语法/空白检查。
4. 执行 `npm run build:win`。
5. 上传 GitHub Release 资产：
   - `dist/Mineradio-x.y.z-Setup.exe`
   - `dist/Mineradio-x.y.z-Setup.exe.blockmap`
   - `dist/latest.yml`
   - 需要的 `Mineradio-旧版本-x.y.z.json` 轻量补丁
6. 0.9 系列补丁跳过；1.0.x 系列可按需生成跨小版本补丁。

GitHub CLI / `gh auth` / Release 上传或 Electron 打包下载需要代理时，优先使用当前系统代理 `127.0.0.1:7897`；不要再走旧代理 `127.0.0.1:26001` 或 `127.0.0.1:10808`。临时命令可先清空 `HTTP_PROXY`/`HTTPS_PROXY`，再设为 `http://127.0.0.1:7897`。

## User Preferences

- 交流语言：中文。
- 用户偏好：少废话，直接做，修完验证，能发布就一起发布。
- UI 优化边界：能不动 UI 就不动 UI。播放器性能优化默认不改布局、CSS、文案、视觉质感或交互入口；必要的 DOM/HTML 改动必须保持等价输出。
- UI 审美：精致、暗色、高级、流畅，拒绝廉价渐变、过度透明、错位、闪烁和卡顿。
- 视觉质量定义：质感、丝滑度、帧数稳定同时成立；性能优化不能牺牲既有质感。
- 玻璃质感：当前播放器 SVG 玻璃质感是黄金版本，详见 `docs/GLASS_SVG_TEXTURE.md`。
- 备份策略：不要删除旧资料；重复和历史内容移动到 `E:\桌面\播放器软件\工作区备份`。
- 重要：不要再改旧外层源码目录。旧的 `E:\桌面\播放器软件\Mineradio\public` / `desktop` 已经归档；当前环境以 `C:\Users\Administrator\Desktop\Mineradio-main\public` / `desktop` 为准。

## Memory Protocol

当用户说“保留”“这个做得很好”“我喜欢”“记住这个”“保存一下”“以后别忘了”或同类表达时：

1. 判断用户认可的是代码、视觉效果、交互流程、发布流程还是工作习惯。
2. 将结论追加到 `docs/PROJECT_MEMORY.md` 的对应区块。
3. 如果是玻璃 SVG、粒子预设、3D 歌单架等脆弱视觉实现，同时更新对应专项文档。
4. 记录日期、涉及文件、关键参数、不要再改坏的边界。
5. 如果本轮有代码提交，把记忆文档一起提交；如果只是记忆整理，单独提交也可以。

## Guardrails

- 不要随意重写 `public/index.html` 的大块视觉系统；先定位已有函数和状态。
- 不要动电影视觉系统，除非用户明确点名。
- 不要恢复旧的侧边栏闪烁、控制台播放暂停失效、3D 歌单架强制切回星河等问题。
- 不要把搜索结果、左侧歌单、3D 歌单架的性能优化做成一次性渲染全部内容。
- 不要把用户认可的玻璃质感改成普通毛玻璃或廉价透明面板。

## 当前仓库索引

- 遇到 `桌面歌词轮询进程`、`PowerShell 孤儿进程`、`旧进程 exit 覆盖新进程`、`DesktopOverlayStateCache`、`桌面歌词 beatMap 保留` 或 `壁纸封面 data URL 保留` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `旧窗口 closed 覆盖新窗口`、`覆盖层 BrowserWindow 所有权`、`旧 renderer IPC 控制新窗口`、`桌面歌词透明度/位置失效`、`旧 poller stdout` 或 `覆盖层签名未释放` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `localAssetCacheMemory`、`本地资产镜像`、`只写不读缓存` 或 `切换曲库旧歌词封面保留` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `localLibraryPersistentMemory`、`曲库 snapshot 常驻`、`A→B→A ABA`、`旧曲库后台扫描覆盖新曲库`、`空曲库后台资产任务`、`localLibraryProcessToken`、`旧资产队列闭包常驻` 或 `跨曲库索引写错` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `全曲库歌词常驻`、`已播放歌词原文常驻`、`localLyricText O(n)`、`歌词按需恢复`、`歌词播放租约`、`handoffLocalLyricText`、`localLyricResidencyReleased`、`迟到歌词水合`、`原歌词双份克隆`、`originalLyricsState`、`歌词只读快照`、`localLyricCachePromise`、`localLibraryPreviousRecord`、`本地节奏图内存上限` 或 `localBeatMapCache` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `完整本地封面常驻`、`localCoverDataUrl`、`封面所有权`、`迟到封面读取`、`缩略图失败完整图持久化`、`空缩略图已加载` 或 `歌曲克隆复制封面` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `本地媒体 Object URL`、`Blob URL`、`重复入队音频地址`、`封面懒创建`、`revokeObjectURL`、`媒体租约`、`歌单封面缓存`、`loading 图片请求`、`playlistCoverCache`、`图片 waiter 常驻`、`Image 事件闭包常驻`、`失败封面 rec.img`、`缩略图 Promise 所有权`、`缩略图活跃计数不归零`、`缩略图 FIFO 重复键`、`挂起缩略图任务`、`Promise 队列内部空洞` 或 `迟到缩略图任务` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `关闭迷你播放器内存`、`迷你窗口非驻留`、`窗口驻留缓存`、`旧迷你窗口 closed`、`封面 data URL 保留`、`MiniPlayerStateCache` 或 `sync-state` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `迷你播放器锁屏恢复`、`休眠重建`、`powerMonitor` 或 `renderer 锁屏期间重载` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `本地文件范围读取`、`readAuthorizedLocalFileRange`、`base64Chunks`、`完整 Buffer 峰值`、`整段 atob`、`bytes.buffer.slice`、`Uint8Array 视图复制`、`APIC/PIC/PICTURE 图片副本`、`后台内嵌封面完整 data URL`、`ImageBitmap 封面缩略图`、`PICTURE 长度越界`、`ALBUMARTISTSORT`、`元数据缓存版本`、`MP3 标签大内存`、`FLAC 元数据大内存`、`FLAC metadata descriptor`、`大 PADDING`、`readFlacMetadataSession`、`localFlacMetadataSessionReadBatches`、`后台 light 元数据 Promise`、`Vorbis comment 大字段`、`FLAC 空 comment`、`标签范围合并`、`localFileRangeReadBatches`、`歌曲克隆在途 Promise` 或 `跨进程文件字节` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `mineradio-local-assets-v1` 数据库 v3、`LOCAL_LYRICS_CACHE_STORE`、`lyrics store`、`localLyricCacheSnapshot`、`assets/lyrics 分离`、`v2 到 v3 cursor 迁移`、`本地封面流 URL`、`localCoverFile.url`、`readLocalFileDataUrl` 绕过或 `crossOrigin=anonymous` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `verifyUpdateFile`、`安装包校验内存`、`fs.readFileSync 安装器`、`SHA-256/SHA-512 文件校验` 或 `更新安装包 Buffer 峰值` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `/api/local-file`、`本地文件代理越权`、`setLocalFileAuthorizer`、`localFileAuthorizer`、`resolveAuthorizedLocalFile`、`authorizedLocalMusicRoots`、`LOCAL_FILE_NOT_AUTHORIZED`、`本地文件代理授权`、`授权曲库根目录校验`、`路径穿越读取` 或 `MINERADIO_LOCAL_FILE_TOKEN` 时，必须优先读取 `.context/pitfalls/mineradio-local-file-proxy-authorization.md`。
- 遇到 `readRequestBody`、`请求体超限`、`REQUEST_BODY_TOO_LARGE`、`req.destroy 不触发 end`、`promise 永不结算`、`处理器挂起`、`POST 请求体解析`、`8MB 上限`、`客户端中断 socket 泄漏` 或 `单次结算门` 时，必须优先读取 `.context/pitfalls/mineradio-request-body-settlement.md`。
- 遇到 `mineradio-import-json-file`、`存档导入`、`导入 JSON`、`IMPORT_FILE_TOO_LARGE`、`IMPORT_NOT_A_FILE`、`导入文件大小上限`、`readFileSync 无上限`、`主进程 OOM`、`外部文件读取上限` 或 `导入对话框超大文件` 时，必须优先读取 `.context/pitfalls/mineradio-import-file-size-limit.md`。
- 遇到 `IndexedDB 事务 abort`、`onabort`、`事务中止 promise 永挂`、`trimLocalIndexedDbCaches`、`localIndexedDbTrimRunning 锁泄漏`、`IndexedDB 连接泄漏`、`QuotaExceededError`、`versionchange 中止`、`putCustomBackgroundBlob`、`getCustomBackgroundBlob`、`putLocalAssetCacheRecord` 或 `writeLocalLibraryPersistentRecord` 时，必须优先读取 `.context/pitfalls/mineradio-indexeddb-transaction-abort.md`。
- 遇到 `资产预载排序数字键`、`sortLocalAssetPreloadQueue`、`清理阶段全量记录数组`、`游标增量删除` 或 `IndexedDB 清理内存峰值` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `播放器性能优化`、`UI 不动`、`播放启动速度`、`点歌到出声`、`启动恢复`、`迷你播放器`、`迷你播放器恢复`、`迷你播放器内存释放`、`隐藏窗口渲染进程`、`队列渲染`、`Home 首屏`、`听歌统计`、`搜索历史`、`搜索结果缓存`、`搜索玻璃贴图`、`HTML 转义`、`副标题缓存`、`空搜索`、`本地曲库导入`、`FileList 转数组`、`本地曲库缓存`、`本地资产缓存补水`、`元数据字段套用`、`本地歌词加载`、`本地文本解码`、`无歌词占位`、`LRC 解析`、`YRC 解析`、`歌词 source 转换`、`本地节奏缓存`、`3D 歌单详情`、`详情行属性写入缓存`、`主进程本地曲库扫描`、`增量扫描截断`、`扫描回退全量`、`scanLocalMusicFolderIncremental`、`listed.truncated`、`LOCAL_LIBRARY_SCAN_VISIT_LIMIT`、`曲库丢歌`、`残缺快照覆盖`、`IndexedDB 缓存清理`、`IndexedDB`、`封面缩略图`、`重复函数声明` 或 `requestAnimationFrame 调度` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `localSearchWarmupSource`、`scheduleLocalSearchIndexWarmup` 二次筛选、`空查询结果数组复用` 或 `localSearchResultCache` 空查询身份时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `主 3D 空闲 30 FPS`、`isContinuousPlaybackRenderActive`、`隐藏歌单架短路`、`targetVis === 0`、`频谱桶单次扫描`、`getStageLyricLockBounds`、`歌词边界闭包`、`暂停镜头空闲短路` 或 `frequencyValue` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `实时节拍频谱 30 FPS`、`BEAT_ANALYSIS_TARGET_FPS`、`beatAnalysisElapsed`、`consumeRealtimeBeatSpectrumSlot`、`beatBandValueCache` 或 `节拍 FFT 限频` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `主渲染空闲定时器`、`mainRenderScheduleKind`、`idle-timeout`、`wallpaperDrawHandle`、`壁纸覆盖层未播放限频` 或 `覆盖层封面释放` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `frameShelfLook`、`drawPanel` 外观快照、`drawRow` 外观快照、`syncRenderedRows` 重复读取 `shelfSettings`、`详情面板颜色解析` 或 `详情绘制帧快照` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `cameraPoseSyncState`、`cameraPoseNeedsRefresh`、`cameraPoseOverrideActive`、`shelfHoverPointerScratch`、`updateRipples`、`usedMask` 或 `普通相机姿态缓存` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `主循环 performance.now 时间戳复用`、`refreshShelfRenderFrameState`、`frameShelfState`、`updateStageLyrics3D 歌单状态快照`、`shelfManager.getMode 同帧重复`、`hasOpenContent 同帧重复`、`shelfAlwaysVisible 同帧重复` 或 `空 Home 波形时间戳` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `sampleRenderPerf 缓存回收`、`maybeTrimRuntimeCaches 采样边界`、`主循环复用自适应 FPS`、`frameFps` 或 `shouldSkipAdaptiveRenderFrame 时间戳复用` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `M4A 播放失败`、`m4a 元数据`、`moov`、`ilst`、`data atom`、`covr`、`audio/mp4`、`M4A 封面`、`M4A 标签缓存` 或 `ALAC 编码兼容` 时，必须优先读取 `.context/pitfalls/mineradio-m4a-metadata-playback.md`。
- 遇到 `Electron 透明无边框全屏`、`setFullScreen`、`isFullScreen 返回 false`、`windowFullscreenActive`、`htmlFullscreenActive`、`全屏窗口被缩回`、`display.bounds`、`workArea` 或 `WS_THICKFRAME` 时，必须优先读取 `.context/pitfalls/mineradio-electron-transparent-fullscreen.md`。
- 遇到 `安装器无法关闭 Mineradio`、`Failed to uninstall old application files`、`customCheckAppRunning`、`CloseMainWindow`、`SessionId`、`ProcessId = $PID`、`更新安装器启动后退出播放器` 或 `覆盖安装误杀其它实例` 时，必须优先读取 `.context/pitfalls/mineradio-nsis-process-close.md`。
- 遇到 `标准迷你播放器自动收回按钮`、`完整控制栏右上角 ×`、`封面悬停展开`、`封面拖动移动`、`靠右向左展开`、`expandDirection`、`disableAutomaticCollapse`、`收回后空白区仍吞鼠标`、`setIgnoreMouseEvents`、`封面热区恢复交互`、`mineradio-mini-player-set-pointer-passthrough`、`词按钮左下角镜像`、`悬浮展开收回显示错位`、`词按钮飞到面板中间`、`transform 变成绝对定位包含块`、`叉号误改为关闭窗口` 或 `极简模式叉号` 时，必须优先读取 `.context/conventions/mineradio-mini-player-collapse.md`。
- 遇到 `迷你播放器显示器拓扑`、`display-metrics-changed`、`display-added`、`display-removed`、`拓扑变化跳位`、`封面屏幕锚点`、`累计未夹紧坐标`、`虚拟工作区`、`dragGeneration`、`迟到拖动 IPC`、`miniPlayerCoverLayouts` 或 `原生 moved 脏标记` 时，必须优先读取 `.context/conventions/mineradio-mini-player-collapse.md`。
- 遇到 `自动播放开关`、`启动自动继续播放`、`启动随机播放`、`autoPlaybackMode`、`AUTO_PLAYBACK_STORE_KEY`、`startAutoPlayback`、`fx-playback-fold`、`自动播放歌单` 或 `启动恢复只起播一次` 时，必须优先读取 `.context/conventions/mineradio-auto-playback.md`。
- 遇到 `迷你播放器封面律动无效果`、`runMiniPlayerPulseTimer`、`miniPlayerPulseBaseline`、`隐藏窗口 AudioContext suspended` 或 `脉冲长期固定满值` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `封面律动强度`、`封面光晕强度`、`律动光晕不够明显`、`miniPlayerPulseStrength`、`miniPlayerGlowStrength`、`MINI_PLAYER_EFFECT_STRENGTH_MAX`、`MAX_MINI_PLAYER_EFFECT_STRENGTH`、`saturateMiniEffect`、`readMiniEffectStrength`、`--mini-pulse`、`--mini-glow`、`封面放大裁切`、`光晕被窗口裁掉` 或 `360 × 84 几何上限` 时，必须优先读取 `.context/architecture/mineradio-player-performance-seams.md`。
- 遇到 `软件内更新线路`、`自动测速`、`最快线路`、`UPDATE_ROUTE_PROBE_BYTES`、`rankUpdateDownloadCandidates`、`测速超时部分样本`、`镜像摘要门禁`、`手动选择更新线路`、`UPDATE_ROUTE_MODES`、`filterUpdateRouteCandidates`、`UPDATE_ROUTE_UNAVAILABLE`、`本机代理下载更新`、`resolveUpdateProxyTarget`、`CONNECT 隧道`、`取消更新`、`cancelUpdateDownloadJob`、`job.applying 拒绝取消` 或 `canceled 终态` 时，必须优先读取 `.context/architecture/mineradio-update-route-selection.md`。
- 遇到 `代理线路取消无效`、`取消后继续下载`、`Readable.toWeb 不认 signal`、`nodeResponseAsFetchLike`、`逐块 throwIfUpdateJobCanceled`、`隧道 socket 未释放` 或 `res.destroy(err) uncaughtException` 时，必须优先读取 `.context/architecture/mineradio-update-route-selection.md`。
