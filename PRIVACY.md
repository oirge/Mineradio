# 隐私与用户数据说明

Mineradio 是本地桌面应用。项目不应把用户登录状态、Cookie、播放历史、搜索历史、自定义封面、自定义歌词或本地缓存提交到 GitHub。

## 本地数据

应用可能在本机保存以下数据：

- 网易云音乐登录 Cookie
- QQ 音乐登录 Cookie
- 搜索历史
- 自定义专辑封面
- 自定义歌词
- 歌词布局与视觉控制设置
- 本地节奏分析缓存
- 更新安装包下载缓存
- 最近播放记录、单曲播放次数与累计播放时长、每首歌的最后播放位置（断点续播用）

这些数据用于本地体验，不属于开源仓库内容。播放统计与断点写在本机两处：`localStorage`（界面读的就是这份）和本机 SQLite 曲库文件里的 `song_stats` 表（曲库镜像）。可以在「最近播放 / 最常播放」分类顶部的「清空记录」里删掉，两处会一起清：那个确认框分两档，一档只清最近播放时间，一档连播放次数、累计时长和断点一起清。SQLite 那边是把播放列归零而不是删行，因为同一行里还存着「特别喜欢」的收藏状态。

## 系统级鼠标钩子

只有在「热键」里把某个功能绑到鼠标中键或侧键、并且绑在「全局」一栏时，Mineradio 才会加载 `uiohook-napi` 并向系统装一个低层输入钩子（Windows 上是 `WH_MOUSE_LL`，libuiohook 同时也会装 `WH_KEYBOARD_LL` 来跟踪修饰键状态）。关于这个钩子：

- 只有在存在全局鼠标绑定时才加载，一条都没绑的用户全程不会把这个原生模块拉进进程；把绑定全删掉会立刻停掉钩子。
- 只读按键与修饰键状态，用来判断「这次按下是不是你绑的那个组合」。不记录、不落盘、不上传任何输入内容。
- 键盘全局热键走的是 Electron 自带的 `globalShortcut`，不经过这个钩子。
- 部分杀毒软件会对低层输入钩子报警。不想要这个能力就别绑鼠标键，或者只绑在「局内」一栏——局内绑定只用浏览器事件，不装任何系统钩子。

## 不应上传的内容

以下内容不应提交到 GitHub：

- `.cookie`
- `.qq-cookie`
- `updates/`
- `node_modules/`
- Electron 打包产物
- 用户上传的本地音乐文件
- 用户账号信息、Cookie、Token、二维码登录状态

## 第三方平台

用户通过网易云音乐、QQ 音乐等第三方平台登录时，应遵守对应平台的用户协议。Mineradio 不提供绕过付费、绕过会员、破解音质或重新分发音乐内容的能力。
