# Mineradio Local Player

中文 · [English](./README_EN.md)

<div align="center">

**基于 Mineradio 二次修改的本地音乐播放器**

- 支持导入本地音乐文件夹。
- 支持单独导入本地音乐文件。
- 支持音乐文件夹自动监控：新增、删除、改标签、换封面自动同步，不必重启。
- 支持 MP3 / MP2 / FLAC / M4A / M4B / WAV / OGG / OGA / AAC / Opus / WebM / WebA / AIFF / APE / DSD(.dsf) 播放。
- 支持同名 `.lrc` / `.txt` / `.srt` / `.vtt` / `.ass` / `.yrc` / `.krc` / `.qrc` / `.ttml` 歌词。
- 支持加密歌词：KRC（`krc1`）与 QRC（十六进制文本与二进制密文两种形态）都能直接读。
- 支持一首歌配多份同名歌词时按格式优先级自动挑，也可以自己挑一份并记住。
- 支持歌词编码自动识别：UTF-8 / UTF-16 / GB18030 / Big5 / Shift_JIS / EUC-KR / Windows-1252。
- 支持 MP3 / FLAC / OGG / OPUS / WAV / APE / DSF 内嵌歌词标签，包括带时间轴的 LRC 歌词。
- 支持同目录封面图片和音频内嵌封面。
- 移除本地节奏分析环节。
- 支持多格式音频播放、歌词显示、迷你播放器和桌面歌词。

[下载最新版本](https://github.com/oirge/Mineradio/releases/latest) · [报告问题](https://github.com/oirge/Mineradio/issues) · [功能建议](https://github.com/oirge/Mineradio/issues/new?template=feature_request.yml)

</div>

---

## ✨ 功能特性

### 🎵 音频格式支持
- **MP3** - MPEG Audio Layer 3
- **MP2** - MPEG Audio Layer 2
- **FLAC** - 无损音频格式
- **M4A** - MPEG-4 Audio (AAC/ALAC)
- **M4B** - MPEG-4 有声书音频
- **WAV** - 未压缩音频
- **OGG** - Ogg Vorbis
- **OGA** - Ogg 音频
- **AAC** - Advanced Audio Coding
- **Opus** - Ogg Opus
- **WebM/WebA** - WebM 音频
- **AIFF/AIFC** - AIFF 无损音频
- **APE** - Monkey's Audio 无损音频
- **DSD (.dsf)** - DSD Stream File 高解析音频

### 📝 歌词功能
- ✅ 同名 `.lrc` / `.txt` / `.srt` / `.vtt` / `.ass` / `.yrc` / `.krc` / `.qrc` / `.ttml` 歌词文件
- ✅ 逐字歌词：YRC（网易云）/ KRC（酷狗，含 `krc1` 加密二进制）/ QRC（QQ 音乐，含 XML 容器与加密载体）/ TTML（Apple Music 一族）
- ✅ QRC 加密歌词：接口原样落盘的十六进制文本与直接写成二进制的密文两种形态都收，识别只看内容特征，后缀被改过也认得出来
- ✅ 同名多份歌词按格式优先级自动挑：逐字时间轴（`.qrc` / `.krc` / `.ttml` / `.yrc`）> 逐行 `.lrc` > 字幕（`.ass` / `.srt` / `.vtt`）> `.txt`
- ✅ 同名多份歌词也能自己挑：自定义歌词弹窗里列出候选（写着路径与格式），选择会记住，下次重新导入同一批文件仍用你挑的那一份
- ✅ 歌词编码自动识别：UTF-8 / UTF-16LE / UTF-16BE（含无 BOM）/ GB18030 / Big5 / Shift_JIS / EUC-KR / Windows-1252
- ✅ 歌词时间轴兼容在野写法：`[offset:±N]` 全局偏移、`[mm:ss:cc]`、`[hh:mm:ss.fff]`，小数位按实际位数换算
- ✅ MP3 / FLAC / OGG / OPUS / WAV / APE / DSF 内嵌歌词标签
- ✅ 带时间轴的 LRC 格式
- ✅ 歌词翻译自动识别和显示
- ✅ 桌面歌词窗口（可拖动、可调整大小）
- ✅ 双行歌词支持

### 🎨 播放器界面
- ✅ 迷你播放器（固定、拖动、动画效果）
- ✅ 封面显示（同目录图片或内嵌封面）
- ✅ 本地音乐库管理
- ✅ 音乐库智能分类独立标签页（艺术家 / 专辑 / 专辑艺术家 / 流派 / 年代 / 最近添加 / 最近播放 / 播放最多 / 未播放）
- ✅ 音乐文件夹自动监控（新增/删除/改标签/换封面自动同步，搜索框下方提示已同步数量）
- ✅ 播放列表管理
- ✅ 随机/循环播放模式
- ✅ 单曲播放统计（播放次数、完整播放、累计播放时长、最近播放时间）
- ✅ 断点续播（每首歌单独记住上次听到哪儿）与「继续上次播放」
- ✅ 最近播放记录可两档清空（只清最近播放 / 连次数与时长一起清）
- ✅ 全局快捷键可自定义，支持鼠标中键与两个侧键
- ✅ 无缝播放（Gapless）与 `0~10` 秒交叉淡入淡出（Crossfade）
- ✅ 整机备份：导出 `mineradio.backup`（曲库索引 / 歌单 / 收藏 / 听歌统计 / 主题 / 音效 / 播放器设置 / 音乐文件夹），换电脑导入自动按新路径挂回，不含音频与封面缓存

---

## 📦 安装使用

### 下载安装

从 [Releases](https://github.com/oirge/Mineradio/releases/latest) 页面下载最新版本的 Windows 安装包。

### 从源码运行

```bash
# 克隆仓库
git clone https://github.com/oirge/Mineradio.git
cd Mineradio

# 安装依赖
npm install

# 启动应用
npm start
```

### 构建安装包

```bash
# 构建 Windows 安装包
npm run build:win

# 构建产物位于 dist/ 目录
```

---

## 🚀 快速开始

1. **导入音乐**
   - 点击「导入文件夹」批量导入
   - 或「导入文件」单个添加

2. **播放音乐**
   - 双击歌曲开始播放
   - 支持拖拽调整播放顺序

3. **查看歌词**
   - 歌词文件需与音乐文件同名（`.lrc` / `.txt` / `.srt` / `.vtt` / `.ass` / `.yrc` / `.krc` / `.qrc` / `.ttml`）
   - MP3 / FLAC / OGG / OPUS / WAV / APE / DSF 文件可使用内嵌歌词标签
   - 开启桌面歌词窗口显示

---

## 🔧 开发

### 技术栈

- **Electron** v43.4.0 - 桌面应用框架
- **Node.js** 22.x - 运行环境
- **electron-builder** - Windows 打包工具
- **uiohook-napi** v1.5.5 - 唯一的运行时依赖，只为全局鼠标键热键提供系统级低层输入钩子；没有绑定鼠标键时不会被加载

### 开发指南

查看 [CONTRIBUTING.md](./CONTRIBUTING.md) 了解：
- 开发环境配置
- 代码规范
- 提交流程
- 测试要求

### 运行测试

```bash
npm test
```

---

## 📋 变更日志

查看 [Releases](https://github.com/oirge/Mineradio/releases) 页面获取完整变更历史。

### 最新版本 v1.8.4 (2026-09-04)

- **音乐库首页新增第三段卡片「音乐库维护」**：重复检测 / 失效文件 / 无封面 / 无歌词 / 标签异常。每一项点进去就是一份普通歌单，返回、播放全部、懒加载全部照旧；五项与智能分类同族，都按当前曲库现算，**不落盘、不进 SQLite**，曲库一变结论就跟着变，不留过期的「上次检测结果」
- **重复检测按归一化后的「标题 + 艺术家」分组，但故意不剥 `(Live)` / `(Remix)` 后缀** —— 剥掉会把现场版和录音室版判成同一首，宁可漏报也不误报。撞上之后再用文件体积或时长复核：体积完全相同直接算重复，时长都读出来了且差超过 2 秒的会被摘掉，**时长还没读出来时一律不摘**
- **失效文件是唯一必须问磁盘的一项**，只在点进这一项或点「重新检测」时跑。桌面端新增一条只回状态码的通道，主进程逐条确认路径还在不在，只回「还在 / 没了 / 不在已授权的音乐目录里」三种状态，**一个字节的文件内容都不回**；「不在已授权目录」和「文件被删」分开记。渲染层每 400 条一批，几万首也不会把界面按住
- **无封面 / 无歌词 / 标签异常的每条结论都是三态的：有 / 没有 / 还不知道。** 「还不知道」的那部分单独报成「N 首待扫描」，**既不塞进异常名单冒充问题，也不当成正常悄悄咽掉**。同目录没配图、格式本来又读不出内嵌封面，不用读盘就是定论；同目录明明有 `.lrc` 但读出来是空的或解密失败，结论仍然是「没歌词」
- **标签异常查标题 / 艺术家 / 专辑 / 时长四项缺失，外加年份与音轨号的格式**；年份和音轨号只在填了值时才校验。**整库刚导入、标签还没读出来那会儿全部记「待扫描」而不是全部报异常**
- 卡片副标题直接写清数量与待扫描数（如「12 首 · 340 首待扫描 · 内嵌封面与同目录封面都没有」）；失效文件那张按检测状态换话术，并且不给「播放全部」也不给 ▶（文件已经不在了，播只会一路报错），换成「重新检测」
- 四个桶一趟遍历同时喂满，按「曲库长度 + 首尾键 + 资产纪元」签名缓存，两万首曲库也只走一遍；后台扫封面 / 歌词读出结论时数字自动跟着变
- **界面零改动：`public/index.html` 与 `public/app.css` 一行未动**，维护卡片复用现成的卡片、分段标题与小按钮样式
- 已知边界：失效文件依赖桌面壳的磁盘通道，**纯浏览器模式下这一项会直接说「当前环境不支持磁盘检测」**；五张卡片在真实窗口里的观感本轮没有肉眼验证过
- 全量 Node 回归 `905/905` 通过（新增 `tests/library-maintenance.test.js` 24 例）

<details>
<summary>v1.8.3 QRC 加密歌词能读了，同名多份歌词自己挑</summary>

- **QRC 加密歌词能读了**：接口原样落盘的十六进制文本与直接写成二进制的密文两种形态都收。识别只看内容特征，后缀被改过也认得出来，而现有的明文 QRC / LRC 一个不会被误伤
- 解密链是 3DES 但**不是标准 3DES**：QQ 音乐那份 DES 移植里带着几处与规范不同的写法（S 盒笔误、PC-2 的偏移量、按小端 32 位字取密钥、15 轮加半轮、末轮不交换左右半边），只能照在野实现逐位移植。逐分组跑 `D(K3) → E(K2) → D(K1)`，还原出来是 zlib 流，解压后再剥掉 UTF-8 BOM；解压继续用浏览器与 Node 都自带的 `DecompressionStream`，**没有引入任何新依赖**
- **同名多份歌词改成按格式优先级挑**，不再看谁先被枚举到：逐字时间轴（`.qrc` / `.krc` / `.ttml` / `.yrc`）> 逐行 `.lrc` > 字幕（`.ass` / `.srt` / `.vtt`）> `.txt` > 认不出的后缀；同一档里按路径排，同一批文件每次导入结果一致
- **同名多份歌词也可以手动挑**：一首歌配了两份以上歌词时，自定义歌词弹窗里多出一行候选按钮（写着路径与格式），点一下就换。选择会记住，**下次重新导入同一批文件仍然用你挑的那一份**；挑中的文件后来不见了就自动退回优先级第一名。切换时旧歌词的缓存状态一并清掉，并同步到播放队列与歌单里的同一首歌
- **歌词编码自动识别**：带 BOM 的 UTF-8 / UTF-16LE / UTF-16BE 按 BOM 解码并剥掉 BOM；没有 BOM 的 UTF-16 靠字节分布嗅出来；既不是 UTF-16 也不是合法 UTF-8 的按 `gb18030` → `big5` → `shift_jis` → `euc-kr` → `windows-1252` 逐个试，取乱码字符最少的那个。**合法的 UTF-8 一律不重猜**，现有 UTF-8 歌词一个字不会变
- **歌词时间轴的几种在野写法一并修掉**：`[offset:+500]` 全局偏移按 LRC 规范生效（正值提前、负值延后，偏过头的行贴到 0 而不是变成负时间）；`[mm:ss:cc]`（拿冒号当小数点的老写法）与 `[hh:mm:ss.fff]`（带小时）都认；小数位按实际位数换算，`[00:01.1234]` 不再被当成十倍；分钟数放宽到三位，一小时以上的长音频不会被截掉
- 修掉一个只在特定文件名下才犯的缺陷：音频叫 `constructor.mp3`（或 `toString` / `valueOf` / `__proto__`）时，候选歌词查表会把 `Object` 本身当成候选数组取出来，凭 `Object.length === 1` 混进一个空候选；现在查表一律先验自有属性
- `.qrc` 的 MIME 从 `application/xml; charset=utf-8` 改成 `application/octet-stream`（本地文件代理与桌面曲库两处），否则加密二进制在解码前就被当文本处理坏了
- 界面基本没动：`public/index.html` 只在已有的自定义歌词弹窗里多一个候选按钮行（复用现成的 `btn-row` 样式类），**`public/app.css` 一行未动**
- 已知边界：**手上没有真实的加密 QRC 文件可以对照**，解密链的正确性靠一份大整数参考实现逐轮比对、六组已知答案向量与一段全链密文钉住；真实文件在窗口里的显示效果本轮没有肉眼验证过
- 全量 Node 回归 `881/881` 通过（新增 `tests/lyric-format-hardening.test.js` 10 例）

</details>

<details>
<summary>v1.8.2 多格式歌词：KRC / QRC / TTML 一并认，逐字时间轴照旧</summary>

- 歌词支持从 LRC / YRC / SRT / VTT / ASS 扩到**多格式逐字歌词**：新收 **KRC**（酷狗）、**QRC**（QQ 音乐）、**TTML**（Apple Music 一族），三种都直接进原来那套歌词行结构，逐字高亮、桌面歌词、双行、翻译全部照旧
- **KRC** 明文与 `krc1` **加密二进制**都能读：加密文件按格式常量异或还原后解 deflate，用浏览器与 Node 都自带的 `DecompressionStream`，没有引入任何新依赖。词项时间是相对行首的偏移，读进来会加上行起点换成绝对时间
- **QRC** 两种载体都收：`<Lyric_1 LyricType="1" LyricContent="…"/>` 的 XML 容器（会展开 `&#10;` 这类实体）和把正文直接落成文本的裸 body。少数导出工具漏写最后一个词项的时间标记时，剩下的正文按行尾补上，宁可时间粗一点也不丢字
- **TTML** 取 `<p begin= end=>` 当行、最内层 `<span begin= end=>` 当词项，嵌套的整行 span 不会把正文吃两遍；`ttm:role="x-translation"` / `x-roman"` 的译文与罗马音不进正文；时间既认 `hh:mm:ss.fff` 时钟也认 `12.5s` / `500ms` 偏移量
- **分流靠格式自身特征，不靠后缀名**：TTML 与 QRC 容器认标签，正文里的方括号不会被误当成 LRC 时间轴；KRC / QRC / YRC 的行头都是 `[起点,时长]`，只能靠词项分隔符区分（`<偏移,时长,0>正文` / `正文(起点,时长)` / `(起点,时长,0)正文`），KRC 与 QRC 必须排在 YRC 之前，否则会被当成没有逐字信息的整行吞掉
- 行头写明的时长按明确值走，不再被旧 LRC 那条 12 秒上限截断
- 后缀清单四处同步补上 `.krc` / `.qrc` / `.ttml`：前端歌词匹配、桌面曲库扫描与 MIME、本地文件代理 MIME、两个导入 `accept`。`.krc` 走 `application/octet-stream`，避免加密二进制在解码前被当文本处理坏
- **界面一行未动**，`public/index.html` 只有两个 `accept` 属性各多三个后缀，`public/app.css` 没有改动
- 已知边界：**加密的 QRC 网络负载（三重 DES + zlib）本轮不解密**，落到磁盘的 `.qrc` 通常已经是明文 XML 或裸文本，那两种都能直接读
- 全量 Node 回归 `871/871` 通过（新增 `tests/multi-format-lyrics.test.js` 7 例）

</details>

<details>
<summary>v1.8.1 整机备份：导出 mineradio.backup，换电脑一键搬家</summary>

- 设置面板「高级」页新增**「备份」折叠区**，两个按钮：导出、导入。导出会存下一个 `mineradio.backup` 文件，导入会读回来覆盖本机数据
- **备份里装的是你攒下来的东西**：曲库索引（路径、时长、格式、专辑与艺术家、入库时间）、全部歌单、收藏、听歌历史与播放统计，加上主题、歌词布局、音效链与音效档案、以及音量 / 音质 / 无缝播放 / 交叉淡入淡出 / 热键 / 自动隐藏这一族 16 项播放器设置，最后是音乐文件夹路径
- **默认不备份三类东西：音频文件、封面缓存、临时文件。** 音频一个字节都不进去，能重新生成的封面与节拍图不进去，换机后必然指向旧路径的曲库快照 / 队列快照 / 断点续播位置也不进去 —— 几万首歌的备份也就几 MB，可以直接丢进网盘或 U 盘
- **换电脑是这个功能的正经用法，所以歌曲身份存的是「文件夹 + 相对路径」而不是绝对路径**：备份里没有一处写死盘符，导入时按新机器上的音乐文件夹重新拼路径，`D:\Music` 变成 `E:\新盘\音乐` 之后歌单和收藏照样挂得上
- **导入时会先找备份里的音乐文件夹，找不到就让你重选一个**；这一步取消则整个导入放弃，本机数据一个字节都不动
- **导入是覆盖操作，所以要点两次**：第一次点只提示「导入会覆盖本机歌单 / 收藏 / 设置并重启」，12 秒内再点一次才真的开始；完成后自动重启，重启后曲库按新路径重扫一遍。没有加任何弹窗
- 备份文件扩展名是 `.backup`，走独立读写通道，和只认 `.json` 的音效档案 / 插件导入互不干扰；读进来先验版本号，认不出直接拒收，导入文件大小上限 64 MB
- 界面只在设置面板里多一个同构折叠区（两行按钮 + 两行说明），`public/app.css` 一行未动
- 已知边界：每首歌的自定义封面、自定义节拍图与自定义歌词属于「封面缓存」那一类，本轮按要求不进备份
- 全量 Node 回归 `864/864` 通过

</details>

<details>
<summary>v1.7.29 「已同步 xx 首歌曲」移到搜索框下面，配色跟着主题插件走</summary>

- 音乐文件夹自动同步的「已同步 xx 首歌曲」提示从右下角移到**搜索框下面**：搜索框下面没有别的东西时就落在搜索框正下方，搜索结果一出来就顺到结果列表下面，不会盖在列表上，也不会挤动原有布局
- 提示配色改为**跟着主题插件走**：底色、描边、阴影、文字都优先读主题令牌（与搜索框同一族），没装主题时回落到播放器自带的玻璃质感，和紧邻的搜索框看上去是同一种材质；浅色主题下文字同样读得清，只有左边那颗小圆点保留强调色
- 顶部搜索区本来只在鼠标靠上时探出来，所以提示出现的那几秒会把它按住、淡出后再放开；你自己划开的搜索区不会被接管也不会被替你关掉，输入框有焦点、搜索结果或导入提示开着、鼠标还停在顶部时一律不收
- 界面没有新增任何元素，`public/index.html` 一行未动
- 修掉一个曲库清理缺陷：删掉的歌有可能一直留在库里。完整扫描靠「这一行本轮扫描见过没有」剔除已删除文件，原先用毫秒时间戳当本轮标记，两次扫描落在同一个时钟刻度里时旧行会被误判成「见过」而躲过清理；现在扫描代号保证严格递增
- 全量 Node 回归 `846/846` 通过

</details>

<details>
<summary>v1.7.28 无缝播放（Gapless）与 0~10 秒交叉淡入淡出（Crossfade）</summary>

- 新增「无缝播放」开关（默认开启）：自动续播时提前解码下一首，前一首播完直接接上，相邻两首之间没有可闻的停顿
- 新增「交叉淡入淡出」滑杆：`0 ~ 10` 秒、按 `0.5` 秒一档，大于 0 时两首歌按等功率曲线交叠淡入淡出，中点不会掉音量
- 交叉设为 `0` 时保持原来的播放逻辑；手动切歌、上一首、下一首、随机播放与自动播放都不受影响，预解码出来的那一路只允许自动续播和单曲重播接管
- 播放器状态切换不爆音、不重复播放、不突然静音：双 `<audio>` 常驻音频图只调增益不改接线，交叉必须等下一首真的出声才对交并推进队列，起播被拒就原样退回并保留当前那一首继续播
- 界面只在设置面板「高级」页音量区下面多一个折叠区（一个开关 + 一个滑杆），样式表一行未动
- 全量 Node 回归 `834/834` 通过

</details>

<details>
<summary>v1.7.27 播放统计与断点续播，全局热键接上鼠标中键 / 侧键</summary>

- 单曲播放统计：每首歌单独记播放次数、完整播放次数、累计播放时长与最近播放时间，「最近播放 / 播放最多」列表行下面直接显示摘要，歌曲详情弹窗里还有完整的「播放统计」段
- 断点续播：每首歌单独记住上次听到哪儿，下次播到同一首自动接上；只有听过 15 秒以上、且离结尾还有 20 秒以上的位置才记成断点，听完自动清掉
- 「继续上次播放」：设置面板里一个按钮，也可以绑热键；不看自动播放开关，按下就接着上次的歌曲和进度播
- 最近播放记录清空分两档：「只清最近播放」保住播放次数与累计时长，「全部清空」连次数、时长和断点一起归零；两档都不删本地音乐文件，且会同时清掉本机 SQLite 曲库里的统计镜像
- 全局快捷键支持鼠标中键与两个侧键（可叠 `Ctrl` / `Alt` / `Shift` / `Win`），左右键留给正常点击。注意系统级鼠标键只是「监听」不是「拦截」，其它程序里原本的后退 / 前进动作仍会照常发生；只绑「局内」一栏不受影响
- 鼠标钩子按需加载：没有绑定鼠标键时完全不会把原生模块拉进进程，删掉绑定会立刻停掉钩子。它只读按键与修饰键状态，不记录、不落盘、不上传任何输入内容（详见 [PRIVACY.md](./PRIVACY.md)）
- 全量 Node 回归 `807/807` 通过

</details>

<details>
<summary>v1.7.24 音乐库升级成外层标签页</summary>

- 音乐库升级成外层标签页：左侧面板顶部现在是 `当前队列 / 歌单 / 音乐库` 三个并排入口，点「音乐库」直接进智能分类，不必先切到「歌单」再点卡片
- 两个标签页共用同一份列表与选中状态，切换时自动摆位：切到音乐库落在分类首页，切回歌单退回「全部音乐」，不会串页
- 停在某位艺术家或某个分组时切到「当前队列」再回来，仍停在原来那一层
- 音乐库那一页工具条说明改为「音乐库智能分类」并隐藏「新建」，「导入」保留；「歌单」页恢复成 特别喜欢 → 独立歌单 → 全部音乐
- 音乐库智能分类：`所有歌曲 / 最近添加 / 最近播放 / 播放最多 / 未播放` 五个歌曲视图，加 `艺术家 / 专辑 / 专辑艺术家 / 流派 / 年代` 五个分组浏览
- 三层导航「音乐库 → 分组 → 歌曲」，分组层与歌曲层都带返回按钮，歌曲层还有「播放全部」，任意分类都能直接当底部播放来源
- 年代按十年归档（`1990年代` / `2000年代` / `2010年代`），读不出年份的归入「未知年代」并垫底；专辑艺术家缺失时回落到艺术家
- 最近播放 / 播放最多 / 未播放由本地收听统计驱动，播放次数相同再比累计时长与最近播放时间
- 入库时间戳让「最近添加」按真实入库顺序倒序并封顶 200 首；改标签换封面不会让歌重新变「最近添加」，首次整库导入也不会盖成同一时刻
- 整库只扫一遍建索引，分组卡片沿用面板懒加载额度，几千位艺术家不会一次铺完；界面零新增样式
- 真正的音效链，顺序固定为 `预设 → EQ → Preamp → Limiter → Spatial → Output`，整条链常驻音频图，开关切换不会有咔哒声
- 8 个预设：Normal / Rock / Pop / Classical / Jazz / Bass Boost / Vocal / 自定义；10 段图示均衡 `31 Hz ~ 16 kHz`，每段 ±12 dB / 0.5 dB 步进
- 自动预增益按最大提升量反向留余量，末端限幅压住瞬时过冲；立体声扩展用真正的中/侧矩阵，宽度 1 逐样本原样输出
- 音效档案可导出 / 导入 `xxx.eq.json`，含曲线、预增益、限幅与声场参数
- 音量均衡（ReplayGain）：读取歌曲已有的响度标签，支持整轨 / 整专辑基准、±12 dB Preamp 与防削波
- 音乐文件夹自动监控：新增自动入库、删除自动清理、改标签自动更新、换封面自动刷新，不必重启；同步过程不打断播放，搜索框下方提示 `已同步 12,431 首歌曲`，配色跟着主题插件走
- 支持 OGG / OGA / OPUS / WAV / APE / DSD(.dsf) 的标签、封面、内嵌歌词与时长解析，APE 与 DSD 可直接播放
- 本地曲库改用 SQLite + 文件指纹/路径索引，几万首歌启动不再重放整包 JSON，并解除历史 16000 条上限
- 全量 Node 回归 `655/655` 通过

</details>

---

## ❓ 常见问题

### 支持哪些音频格式？
支持 MP3、MP2、FLAC、M4A、M4B、WAV、OGG、OGA、AAC、Opus、WebM/WebA、AIFF/AIFC、APE、DSD(.dsf) 等格式。

### 如何添加歌词？
- 将 `.lrc` / `.txt` / `.srt` / `.vtt` / `.ass` / `.yrc` / `.krc` / `.qrc` / `.ttml` 歌词文件放在音乐文件同目录，保持文件名一致
- 逐字歌词直接认：YRC、KRC（含 `krc1` 加密二进制）、QRC（含 `<Lyric_1 LyricContent="…">` XML 容器与加密载体）、TTML
- 加密的 QRC 不用先解密：十六进制文本与二进制密文两种形态都能直接放进去，识别看内容不看后缀
- 一首歌配了多份同名歌词时，默认按格式优先级挑（逐字 > `.lrc` > 字幕 > `.txt`）；想换成另一份就在自定义歌词弹窗里点候选按钮，选择会被记住
- 歌词文件不是 UTF-8 也没关系：UTF-16、GB18030、Big5、Shift_JIS、EUC-KR、Windows-1252 会自动识别，合法的 UTF-8 一律不重猜
- MP3 / FLAC / OGG / OPUS / WAV / APE / DSF 文件可直接使用内嵌歌词标签（如 FLAC/OGG 的 `LYRICS`、MP3 的 `USLT`）

### 封面图片如何加载？
- 自动读取音频文件内嵌封面
- 或读取同目录下的 `cover.jpg`/`cover.png` 等图片文件

### 是否支持在线音乐？
本版本为**纯本地播放器**，不提供在线音乐搜索、登录、会员音源等功能。

---

## 📄 授权

本项目沿用原项目授权，详见 [LICENSE](./LICENSE)。

原项目地址：[XxHuberrr/Mineradio](https://github.com/XxHuberrr/Mineradio)

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

- [报告 Bug](https://github.com/oirge/Mineradio/issues/new?template=bug_report.yml)
- [建议功能](https://github.com/oirge/Mineradio/issues/new?template=feature_request.yml)
- [贡献指南](./CONTRIBUTING.md)

---

## ⚠️ 免责声明

本仓库为本地播放器版本，主要面向个人本地音乐库播放。

请自行确保导入和播放的音乐文件来源合法，遵守相关版权法律法规。
